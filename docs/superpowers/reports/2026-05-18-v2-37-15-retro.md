# v2.37.15 Retro — `_derive_matches_spec_status` 'warn' 中间态 + subagent-driven 8 task 一次过

**日期**：2026-05-18
**PR**：[#99](https://github.com/proecheng/cad-spec-gen/pull/99)
**merge SHA**：`d85bc96`
**spec**：`docs/superpowers/specs/2026-05-18-v2-37-15-matches-spec-warn-state-design.md` (rev 3, 401 行)
**plan**：`docs/superpowers/plans/2026-05-18-v2-37-15-matches-spec-warn-state.md` (797 行 / 8 task)
**release**：[v2.37.15](https://github.com/proecheng/cad-spec-gen/releases/tag/v2.37.15)

---

## §1 摘要

v2.37.15 是 `JURY_MATCHES_SPEC_STATUS.md` §9.3 follow-up #2 + #3 的闭合。

- **#2**：`_derive_matches_spec_status` 加 `'warn'` 中间态（partial fail = 部分视角失败，passing_views > 0 ∧ failed_views > 0 双条件防御）
- **#3**：`tools/render_qa.py` mirror drift — archeology 验证在 v2.31.1 (`35629fa`) 已隐式闭合，本 spec §1.3 追注

本 retro 集中沉淀：

1. subagent-driven **8 task + 5 spec reviewer + 3 quality reviewer + 1 final reviewer** 工艺数据
2. **7 项工艺 lesson**（reviewer override / spec writer 真闭环验证 / plan-drift / phase 标识 / mirror 模式 / CP-末 batch / brainstorming 数据可达性）
3. **4 项 §11 follow-up 新登记**（N-13 测试 docstring 版本标签 / N-14 测试 assert message 强制性 / N-15 reviewer subagent phase 提示词 / N-16 plan ruff command scope）
4. 项目 **CI 连续一次过纪录 25 → 26 PR** 延续

---

## §2 关键数据

| 维度 | 数据 |
|---|---|
| spec 演进 | rev 1 (310) → rev 2 (381) → rev 3 (401) 行 |
| spec 审查轮数 | 2 轮，共 **L4 cynical + L5 edge-case + scout grep + closure round 2** 4 层 |
| spec fix 总数 | **11**（6 round 1: B-1/B-2/M-1/M-2/N-1/N-2 + 5 round 2: R2-1/R2-2/R2-3/R2-4/R2-5/R2-6） |
| plan | 797 行 / 8 task |
| commit 数 | **11**（3 spec rev + 1 plan + 7 实施） |
| code diff | 1387 insertions / 15 deletions / 8 文件 |
| pytest baseline | **3276 PASS / 17 skipped / 0 fail** (11 min) |
| ruff-strict (pyproject scope) | All checks passed (7s) |
| mypy-strict (CI gate 4 文件) | Success no issues |
| CI 全套 | **9/9 SUCCESS** (~7-8 min wall) |
| **CI 连续一次过 PR** | 25 → **26 PR**（项目纪录延续） |
| **subagent 派发** | 8 implementer (haiku ×7 + sonnet ×1) + 5 spec reviewer + 3 quality reviewer + 1 final reviewer (sonnet) |
| **AC 数** | **9**（AC-1a/b/c/d + AC-2/3/4/5/6/7/8） |
| **主 agent override** | 2 处（Task 1 docstring style preference / Task 2 spec reviewer 误判 RED phase scope） |
| **plan-drift 抓** | 1 处（plan Task 8 ruff command scope，不阻断） |

---

## §3 §11 follow-up 状态

### 3.1 本 PR 闭合

| ID | 项目 | 状态 | 闭合方式 |
|---|---|---|---|
| **§9.3 #2** | `_derive_matches_spec_status` warn 中间态 | ✅ closed v2.37.15 | 实现 'warn' 部分视角失败 + drop 'blocked' 占位（spec §2-§3 + commit `06f8406`）|
| **§9.3 #3** | `tools/render_qa.py` mirror drift | ✅ closed-by-v2.31.1 | archeology 注脚追闭合（`35629fa` packaging cleanup git rm --cached 55 mirror，dev_sync 接管同步）|

### 3.2 仍 open

| ID | 项目 | 触发条件 |
|---|---|---|
| §9.3 #4 | plan-drift 模板纠正（spec review checklist 补） | v2.37 系列后续 PR |
| §9.3 #5 | Task 13 cad-tests README 真 e2e | 下次真 e2e run 触发 |

### 3.3 本 retro 新登记

| ID | 项目 | 推迟优先级 |
|---|---|---|
| **§11-N13** | 测试 docstring 版本标签规则 — v2.37.15 Task 1 reviewer 提议测试 docstring 不带 `v2.37.X 起` 等版本标签（项目惯例不一致）；本 PR plan instruction 含版本标签 → 主 agent override（plan 优先）。未来 plan instruction 写 docstring 时显式 ack 项目惯例（默认不带版本标签），仅在 cross-version semantic shift 强烈时加 | 下次新写测试 docstring 触发 |
| **§11-N14** | 测试 assert message 强制性 — v2.37.15 Task 2 reviewer 提议 7 个 deriver assert 全加 failure message，但 plan instruction 按 bare assert 给出。是否立"测试 assert 默认含 message"规则待项目共识；当前 plan 未要求，记录待立规 | 下次 spec/plan 模板触发 |
| **§11-N15** | spec reviewer subagent 对 TDD RED/GREEN 阶段理解 — Task 2 阶段 spec reviewer 误判"deriver 没改 = SPEC_COMPLIANT_NO"，未识别 plan 显式分 RED/GREEN 两 commit。subagent 提示词应加 **"本 task 是 RED/GREEN/REFACTOR 哪个 phase + 是否预期实现完整"** | 下次类似 RED 阶段 task 触发 |
| **§11-N16** | plan ruff command scope 校准 — v2.37.15 Task 8 step 2 ruff check 命令带显式 path `tools/ src/ tests/`，逾出 pyproject.toml 配的 ruff scope，触发 24 pre-existing 历史债 errors；项目 CI 真用 `ruff check .`（pyproject scope）。**未来 plan ruff/mypy command 必须 verbatim 抄项目 CI 命令** | ✅ **closed v2.37.16**（CLAUDE.md `## Superpowers 插件工作流` 新增 `### Plan 中 verify 命令必抄 CI` 子节 + 7 行白名单 + narrowing-vs-override + sync 责任，见 [spec](../specs/2026-05-18-n16-plan-verify-cmd-whitelist-design.md)） |
| **§11-N17** | 本 retro PR 合并后回填 PR #99 merge SHA + v2.37.15 release URL | ✅ **closed**（本 commit 回填）：PR [#99](https://github.com/proecheng/cad-spec-gen/pull/99) / merge SHA `d85bc96` / release [v2.37.15](https://github.com/proecheng/cad-spec-gen/releases/tag/v2.37.15) |

---

## §4 工艺 lesson 沉淀（v2.37 系列复用）

| ID | lesson |
|---|---|
| **L-1** | **subagent-driven 主 agent override 权** — 当 reviewer 与 plan instruction 冲突，主 agent 必须有 override 权（plan instruction 锁优先）。v2.37.15 实证 2 处：Task 1 style preference（reviewer 建议去 docstring 版本标签，plan 明示要加）+ Task 2 spec reviewer 误判 RED phase scope（reviewer 期待 deriver 一起改，plan 显式分 RED/GREEN 两 commit）。reviewer 反馈非废 — 记入 retro 立规作下次起点（见 N-13/N-14/N-15） |
| **L-2** | **spec 真闭环 gap = 0 评估在 CI 全绿中第三方验证** — round 2 审查抓"下游 chain 早已 anticipated 'warn'"3 处证据（line 565 docstring / line 447 docstring / line 555 fallback）；deriver 改后 528 个 jury 测试全 GREEN **零下游改动**，证实"已 anticipated"是代码真实状态而非文档承诺。后续 spec 工艺：**round 2 闭环审查必跑下游消费方 grep 证据收集** |
| **L-3** | **plan-drift 5 分类预防 (d) 实现细节 bug** — plan Task 8 ruff command 写 `ruff check tools/ src/ tests/` 与项目 CI 真命令 `ruff check .` 不同；触发了 24 errors + 491 format reformat 历史债 false alarm，但不阻断实施。**plan ruff/mypy/pytest command 必须 verbatim 抄项目 CI 命令**（见 N-16） |
| **L-4** | **subagent 提示词显式 phase 标识** — RED/GREEN/REFACTOR phase 在多 task PR 中要让 reviewer subagent 显式知道；否则容易误判"deriver 没改 = NO"。Task 2 spec reviewer 误判根因 = prompt 没说"本 task 是 RED phase，deriver 实现在 Task 3"。**未来主 agent 派发 reviewer 时显式标注 phase + cross-task 上下文**（见 N-15） |
| **L-5** | **mirror gitignored 在 `git show` 中返回 d41d8cd 空 hash** — 不是 drift。final reviewer 注意到这一点显示 reviewer 真懂 canonical/mirror 模式（项目术语 §3）。subagent_type 选 **sonnet** for final review = ROI 高（haiku 可能漏这种项目细节） |
| **L-6** | **CP-末整体 quality review 替代 per-task 重做** — Task 4/5/6/7 4 个 docs/comment 小改动用主 agent inline verify 替代 dispatch quality reviewer，节省 ~30s × 4 = 2 min。Critical/Major task (Task 3 deriver) 仍按需派 reviewer。**reviewer 不是每 task 必派 — 看风险**（memory `feedback_cp_batch_quality_review.md`（摘要：同质机械 task 用 CP-末 batch review 替 per-task）） |
| **L-7** | **brainstorming Q1-Q3 数据可达性对照** — Q1 (warn 语义) / Q2 (blocked 归属) / Q3 (warn 在 delivery 处理) 三个二元/多元选择题锁住设计 surface。每个 Q 都有"现有数据可达性"对照 — 选择不只考虑业务语义还考虑 RunVerdict 数据契约（结果：A/A/A 三个推荐选项全选 → spec scope 最小 + 决策自洽 + zero schema 变动） |

---

## §5 关键工艺亮点

### 5.1 spec 演进 3 rev 真闭环

| 维度 | rev 1 (310) | rev 2 (381) | rev 3 (401) |
|---|---|---|---|
| 触发 | brainstorming 3 Q 锁设计 | Round 1 L4+L5+scout 6 fix | Round 2 闭环审查 5 cosmetic |
| 关键 fix | 初稿 | I-6 撤回 + 双条件防御 + 修+补测试单 | line 555 注释 + scout 结果 cite + AC-1a 层级说明 |
| 真闭环 gap | — | 0（主诉求满足）| 0（cosmetic 增强后无 gap） |

### 5.2 subagent-driven 8 task 分模型选

| Task | 类型 | 模型 | 理由 |
|---|---|---|---|
| Task 0 | scout 验证 | 主 agent | scout 主 agent 自跑（防 plan-drift；memory `feedback_subagent_driven_main_agent_scouts.md`（摘要：主 agent 前置探查 → subagent 精准执行）） |
| Task 1 | mechanical test edit | haiku | 1 file 3 处改动 |
| Task 2 | mechanical test write | haiku | 1 file 8 测试 + 2 helper（机械） |
| Task 3 | implementation | **sonnet** | 真 GREEN 实现 + 多文件协调 + dev_sync |
| Task 4 | test write (copy pattern) | haiku | 复用现有 pass/fail pattern |
| Task 5 | comment edit | haiku | 1 行注释 |
| Task 6 | docstring edit | haiku | 1 函数 docstring 注脚 |
| Task 7 | doc edit | haiku | STATUS doc 2 行 |
| final review | overall review | **sonnet** | 跨 task 整体审 |

**模型分级**节省 token 同时保关键 task（Task 3 + final）由 sonnet 处理。

### 5.3 reviewer 覆盖矩阵

| Task | implementer | spec reviewer | quality reviewer | 主 agent verify |
|---|---|---|---|---|
| Task 0 | — | — | — | scout (内置) |
| Task 1 | haiku ✅ | haiku ✅ | haiku（style override） | — |
| Task 2 | haiku ✅ | haiku（RED phase 误判 override） | haiku（assert message preference override） | — |
| Task 3 | sonnet ✅ | （response truncated → 主 agent 接管） | haiku APPROVED ✅ | 主 agent 实测验证 ✅ |
| Task 4 | haiku ✅ | combined haiku ✅ | combined haiku ✅ | — |
| Task 5 | haiku ✅ | — | — | 主 agent inline ✅ |
| Task 6 | haiku ✅ | — | — | — |
| Task 7 | haiku ✅ | — | — | — |
| CP-末 | — | — | — | sonnet final reviewer FINAL_APPROVED ✅ |

总：9 implementer dispatch + 5 spec reviewer + 3 quality reviewer + 1 final reviewer + 4 主 agent verify。

### 5.4 决策表 8 行 × AC 9 × 单测 8 三路对齐

| 决策表 # | 条件 | AC | 单测 fixture |
|---|---|---|---|
| 1 | 全通过 | AC-3 | (2, 0, True, "pass") |
| 2 | 空 | AC-4 | (0, 0, True, "pass") |
| 3 | partial fail (3,1) | AC-1b | (3, 1, False, "warn") |
| 4 | partial fail (5,2) | AC-1c | (5, 2, False, "warn") |
| 5 | partial fail (5,4) 边界 | AC-1d | (5, 4, False, "warn") |
| 6 | 全失败 (3,3) | AC-2 | (3, 3, False, "fail") |
| 7 | 单视角失败 (1,1) | AC-5 | (1, 1, False, "fail") |
| 8 | defensive 异常 | AC-8 | (1, 0, False, "fail" force_overall=False) |
| —（e2e 层）| AC-1a (2,1) jury subprocess | AC-1a | 修 `test_photo3d_jury_partial_fail_yields_warn_status` |

注：AC-6 (透传) + AC-7 (docs) 不在决策表 — 走 jury → enhance → delivery 透传链 e2e。

---

## §6 关联 memory 与项目术语

- **memory** `feedback_archeology_before_diagnosis.md`（摘要：异常状态先 archeology 再下结论） — §1.3 archeology 闭合 #3 方法论
- **memory** `feedback_subagent_cwd_drift.md`（摘要：subagent 跨命令 cwd 漂移防御） — Task 1-7 用 `git -C` 绝对路径防漂移
- **memory** `project_v2_31_1_packaging_cleanup.md`（摘要：v2.25+ tag-only release 模式） — release 形式
- **memory** `feedback_subagent_driven_main_agent_scouts.md`（摘要：主 agent 前置探查 → subagent 精准执行） — Task 0 主 agent 自跑
- **memory** `feedback_cp_batch_quality_review.md`（摘要：同质机械 task 用 CP-末 batch review 替 per-task） — Task 4/5/6/7 主 agent inline verify
- **memory** `feedback_spec_writer_self_drift.md`（摘要：spec 作者自己会引入 plan-drift） — round 1 L5 edge-case 抓到 I-6 不严密
- **memory** `feedback_third_party_proxy_real_test_finds_bugs.md`（摘要：4 层 review 抓不到的 bug 1 次真测能抓） — 不适用本 PR（无第三方 API 调用，纯内部 deriver 逻辑）
- **项目术语 §6** 5 层 + 1 scout 审查 — 本 PR 跑 brainstorming 3 Q + L4 cynical + L5 edge-case + scout grep + Round 2 closure 5 层
- **项目术语 §7** subagent-driven 模式 — 8 task fresh subagent 验证模式 ROI
- **项目术语 §10** plan-drift 5 分类 — 抓 1 处 (d) 实现细节 bug（plan ruff command）

---

## §7 数据总结

| 维度 | 数据 |
|---|---|
| spec + plan + retro 总行数 | rev 3 (401) + plan (797) + retro (~280) = ~1478 行 |
| code diff | 1387 insertions / 15 deletions / 8 文件 |
| pytest | 3276 PASS / 17 skipped / 0 fail |
| CI 全套 | 9/9 SUCCESS, ~7-8 min wall time |
| **CI 连续一次过纪录** | **26 PR** 延续 |
| §11 follow-up 闭合 | #2 + #3（2 项）|
| §11 follow-up 新登记 | N-13 / N-14 / N-15 / N-16 / N-17（5 项）|
| lesson 沉淀 | 7 项 |
| 主 agent override | 2 处（合理处置 reviewer 误判 / 偏好） |
| plan-drift 抓 | 1 处（不阻断） |

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
