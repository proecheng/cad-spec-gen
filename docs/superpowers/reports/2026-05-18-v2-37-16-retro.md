# v2.37.16 Retro — §11-N16 plan verify 命令必抄 CI 白名单 + retro 闭环

**日期**：2026-05-18
**PR**：[#101](https://github.com/proecheng/cad-spec-gen/pull/101)
**merge SHA**：`3e6bdf4`
**spec**：`docs/superpowers/specs/2026-05-18-n16-plan-verify-cmd-whitelist-design.md` (rev 3, 202 行)
**plan**：`docs/superpowers/plans/2026-05-18-n16-plan-verify-cmd-whitelist.md` (498 行 / 6 task)
**release**：[v2.37.16](https://github.com/proecheng/cad-spec-gen/releases/tag/v2.37.16)

---

## §1 摘要

v2.37.16 闭合 v2.37.15 retro §3.3 **§11-N16**：plan verify step 中所有 ruff / mypy / pytest 命令必须 verbatim 抄 `.github/workflows/tests.yml` 真命令，防 plan-drift 触发 pyproject 配置外 false-alarm。

**核心交付**：

1. `CLAUDE.md ## Superpowers 插件工作流` 新增 `### Plan 中 verify 命令必抄 CI（防 N-16 类 false-alarm）` 子节（+50 行）：适用范围段 / 主规则 / 7 行 verify 白名单 / narrowing-vs-override / sync 责任 / 例外
2. `v2.37.15 retro §3.3 N-16 row` 末附 closure 引用 v2.37.16（同 v2.37.13b → v2.37.14 模式）

**纯 docs append**：零代码 / 零测试 / 零 lint scope 改动。

本 retro 集中沉淀：

1. **用户主导审查机制**作为 plan-drift 第二防御层 — 2 轮"再次审查"触发 rev 2→3 共 **14 处修复**（含 1 CRITICAL EC-DRIFT-1 memory 与 STATUS doc §9.3 真值漂移）
2. **L6 scout grep 真值** vs **memory 假设** — memory 显式说"N-16 在 STATUS doc §9.3"，grep 真值 = 不在；project_current_status.md 自身的 memory 也含漂移
3. **brainstorming 4 Q 锁定法**：scope (A/B/C/D) → placement → granularity → sync 责任，每 Q 收敛一个独立设计自由度
4. 项目 **CI 连续一次过 PR 纪录 27 → 28 PR** 延续

---

## §2 关键数据

| 维度 | 数据 |
|---|---|
| spec 演进 | rev 1 (30 行 draft) → rev 2 (修 7 处 C1/C2/C5/C10/C11/C17/C20) → rev 3 (修 7 处边界 incl. CRITICAL EC-DRIFT-1) → 202 行 |
| spec 审查轮数 | **2 轮用户主导审查**（rev 1→2 防"改完不如改前" / rev 2→3 边界+闭环） |
| spec fix 总数 | **14**（rev 2 修 7 + rev 3 修 7） |
| plan | 498 行 / 6 task（Task 0 scout + 1-2 edit + 3 AC + 4 PR + 5 release）|
| commit 数 | **1** commit 单文件 PR（66f8fd5 → squash 3e6bdf4） |
| code diff | 731 insertions / 1 deletion / 4 文件 |
| pytest baseline | 不跑（pure docs，CI 接管） |
| ruff-strict | All checks passed (7s) |
| mypy-strict (CI gate) | Success no issues (17s) |
| CI 全套 | **9/9 SUCCESS** (~7-8 min wall) |
| **CI 连续一次过 PR** | 27 → **28 PR**（项目纪录延续） |
| **subagent 派发** | **0**（主 agent inline 自跑全 6 task — 见 §4 L-3） |
| **AC 数** | **6**（AC-1 ~ AC-5 grep + AC-6 CI gate） |
| **主 agent override** | 0（无 reviewer 误判） |
| **plan-drift 抓** | 1 CRITICAL（EC-DRIFT-1 STATUS doc §9.3 不含 N-16 — brainstorming 阶段 L6 scout 抓到）|

---

## §3 §11 follow-up 状态

### 3.1 本 PR 闭合

| ID | 项目 | 状态 | 闭合方式 |
|---|---|---|---|
| **§11-N16** | plan ruff/mypy command 必须 verbatim 抄项目 CI 命令 | ✅ closed v2.37.16 | CLAUDE.md 新增 `### Plan 中 verify 命令必抄 CI` 子节 + 7 行白名单 + narrowing-vs-override + sync 责任 |

### 3.2 仍 open

| ID | 项目 | 推迟优先级 |
|---|---|---|
| §9.3 #4 | plan-drift 模板纠正（spec review checklist 补） | v2.37 系列后续 PR |
| §9.3 #5 | Task 13 cad-tests README 真 e2e | 下次真 e2e run 触发 |
| **§11-N13** | 测试 docstring 版本标签规则统一 | 下次新写测试 docstring 触发 |
| **§11-N14** | 测试 assert message 强制性 | 下次 spec/plan 模板触发 |
| **§11-N15** | spec reviewer subagent phase 提示词 | 下次类似 RED 阶段 task 触发 |

### 3.3 本 retro 新登记

| ID | 项目 | 推迟优先级 |
|---|---|---|
| **§11-N18** | retro §3.3 "新登记" follow-up 到 STATUS doc §9.3 migration 工艺缺失 — v2.37.15 retro §3.3 登记 N-13~N-17 但未 migrate 到 STATUS doc §9.3，本 spec rev 1 假设"§9.3 有 N-16"差点 plan-drift；项目缺一个明示约定：retro §3.3 entries 是否 / 何时 migrate 到 STATUS doc | 下次 retro 后 migration 触发 |
| **§11-N19** | 本 retro PR 合并后回填 PR # / merge SHA / release | ⬜ open — 等 retro PR merge 后单 commit 回填本文件 PR + SHA |

---

## §4 工艺 lesson 沉淀（v2.37 系列复用）

| ID | lesson |
|---|---|
| **L-1** | **用户主导审查机制是 plan-drift 第二防御层** — rev 1 设计后用户说"审查防止改完不如改前" → 触发 L4 cynical re-read 抓 7 处（C1 mypy 表数据漂移 / C2 pytest 自相矛盾 / C5 严重度过高 / C10 commit 摇摆 / C11 无关 AC / C17 scope 冲突 / C20 retro 落点）。rev 2 后用户再说"边界问题，是否闭环" → 触发 L5 edge-case 抓 7 处（**含 CRITICAL EC-DRIFT-1** memory 与 STATUS doc 真值漂移）。一审就 commit 会写"更新一个不存在的表行"。**用户语义触发器 = "防 X" / "边界" / "闭环" 是 retro 审查节点信号词** |
| **L-2** | **memory 与代码真值漂移要用 L6 scout 真值锚验证** — memory `project_current_status.md` 显式写"§11-N13~N16 4 项 open"+ 暗示"在 STATUS doc §9.3"，但 grep STATUS doc 真值 → §9.3 只有 #1~#6，N-13~N-17 **完全不在表内**，只在 v2.37.15 retro §3.3 "新登记" 段。memory drift 类型：(a) memory 信息陈旧 / (b) 信息描述与代码语义偏离 / (c) memory 记录的位置/路径错。**信号词：memory 提"在 §X" / "在 path/to/file" → 必 grep 真值锚** |
| **L-3** | **小型 pure docs PR 主 agent inline ROI > subagent dispatch** — 6 task × 平均 4 min = ~24 min；subagent dispatch overhead ~30s × 4 = 2 min，对小型机械 task 占比 8%+；context 切换成本 + reviewer dispatch 不必要。**信号：plan task 全是 mechanical edit + git/gh CLI、单一文件 / 简单 cell 替换、无 cross-file 协调** → 主 agent inline；反之 subagent。本 PR 是项目第 N 个 pure docs 全 inline 实证 |
| **L-4** | **spec rev 演进 audit log 沉淀方法论** — spec §10 表罗列 "rev 1 → rev 2 → rev 3" 各自触发条件 + 关键修复，retro 时直接索引；本 PR 14 fix 一表归零。**模板**：rev N 行 = {触发审查类型，关键修复项 ID 列表，修复 LOC，闭环判定}。未来高复杂度 spec 全 mandatory 加 §10 audit log |
| **L-5** | **AC-2 grep 命中预测偏保守是 self-reference 自然结果** — plan 预测 AC-2 ≥ 7 命中（baseline 1 + 新增 8），实际 10 命中（多 2）。多 1 来源 = 适用范围段引用 `uv run ruff check .` 也含 `ruff check \.` 模式；多 1 来源 = 主规则段 `应改 ruff check .` 反例的命中。**规则文本自带 self-reference 提升 grep 命中是设计正确性的副产物**，不算 false positive。**调整**：未来 AC grep 阈值预测时考虑 spec/CLAUDE.md 自身的 self-reference 增量（+10%~30%）|
| **L-6** | **brainstorming 4 Q 锁定法每 Q 收敛 1 个独立设计自由度** — Q1 scope (A/B/C/D 4 option) / Q2 placement (CLAUDE.md 哪个 §) / Q3 granularity (3 vs 6 vs 抽象规则) / Q4 sync 责任 (明示 vs 隐式 vs hook)。每 Q 答案锁定一个互相独立的设计维度，最后 4 个答案乘积 = 唯一设计。比 1 个综合 Q "你想怎么设计" 高效 N 倍 + 用户认知负担线性 vs 复合 |
| **L-7** | **CRITICAL 漂移在 brainstorming 阶段抓比实施阶段抓便宜 100×** — EC-DRIFT-1 (STATUS doc §9.3 不存在) 如果到 Task 4 实施 commit 阶段才发现：（1）改 4 文件中 1 个 retro target 错误 → rollback commit (2) 重生成 spec rev 3 (3) 重写 plan (4) 重 run AC (5) reviewer 再审 → 估 30+ min。brainstorming 阶段抓 = 单 grep 5s 调整 spec 2 段 30s 总 5 min。**brainstorming 阶段的 L6 scout 强制化（每 spec rev 跑一次真值 grep） = 高 ROI 防御**  |

---

## §5 关键工艺亮点

### 5.1 spec 2 轮用户主导审查抓 14 fix

| rev | 触发 | 关键修复 | LOC |
|---|---|---|---|
| rev 1 | brainstorming Q1-Q4 锁定 + 初设计 | 单 5 行白名单 + sync MAJOR + 单 commit + 单文件 scope（CLAUDE.md only） | ~30 |
| rev 2 | 用户 "审查防止改完不如改前" → L4 cynical | C1 mypy 表数据漂移（无 broker 覆盖）/ C2 pytest 自相矛盾（白名单 vs AC-6）/ C17 §2 scope 与 §8 commit 冲突 / C5 MAJOR→INFO / C10 单/双 commit 摇摆 / C11 删 AC-6 / C20 retro 落点明示 = 7 fix | ~100 |
| rev 3 | 用户 "再次审查，边界问题，是否闭环" → L5 edge + Round 2 + L6 scout | **CRITICAL** EC-DRIFT-1：STATUS doc §9.3 不存在 N-16 → 改 update v2.37.15 retro §3.3（真位）+ EC-2 适用范围段（隔 §技术规范）+ EC-3 narrowing-vs-override / EC-9 cell 去 `\` / EC-17 mypy 拆 broker/jury/render-QA literal / EC-18 去 line 号 / EC-15 含 quote 风格 + EC-MIGRATE-GAP 新登 N-18 = 7 fix | ~202 |

### 5.2 plan 6 task 全主 agent 自跑

| Task | 类型 | 模型 | 时长 |
|---|---|---|---|
| Task 0 | scout grep | 主 agent | 3 min |
| Task 1 | CLAUDE.md edit (50 行 insertion) | 主 agent | 5 min |
| Task 2 | retro line 73 末段替换 | 主 agent | 3 min |
| Task 3 | AC-1~AC-5 grep verify | 主 agent | 3 min |
| Task 4 | branch + commit + push + PR | 主 agent | 5 min |
| Task 5 | CI watch + merge + tag + Release | 主 agent | 8 min |

**总 27 min**（plan 预估 24-27 min 准确）。0 subagent dispatch。

### 5.3 brainstorming 4 Q 锁定

| Q | 维度 | 选项 | 用户答 |
|---|---|---|---|
| Q1 | 闭环范围 | A (CLAUDE.md only) / B (audit tool) / C (A+B) / D (1 sentence rule) | A |
| Q2 | 段落放置 | Superpowers / 技术规范 / 新顶级 § / 项目术语 | Superpowers |
| Q3 | 白名单粒度 | 低 / 中（推荐）/ 高 6 行 | 中 |
| Q4 | sync 责任 | 明示 / 不规定 / pre-commit hook | 明示 |

4 答案乘积 = 唯一设计 = "Superpowers 子节 + 中度白名单 + 明示 sync 责任 + 纯 CLAUDE.md 不接 hook"。

### 5.4 reviewer 覆盖矩阵

| Task | implementer | spec reviewer | quality reviewer | 主 agent verify |
|---|---|---|---|---|
| Task 0 | — | — | — | scout (内置) |
| Task 1 | 主 agent | — | — | grep verify ✓ |
| Task 2 | 主 agent | — | — | grep verify ✓ |
| Task 3 | 主 agent | — | — | AC grep ✓ |
| Task 4 | 主 agent | — | — | gh pr URL ✓ |
| Task 5 | 主 agent | — | — | gh pr checks 9/9 ✓ |

总：0 subagent / 0 reviewer dispatch。pure docs append 主 agent 自跑全程审查替代专门 reviewer 派遣。

---

## §6 关联 memory 与项目术语

- **memory** `feedback_archeology_before_diagnosis.md`（摘要：异常状态先 archeology 再下结论） — L6 scout grep STATUS doc 真值是 archeology 方法论应用
- **memory** `feedback_subagent_driven_main_agent_scouts.md`（摘要：主 agent 前置探查 → subagent 精准执行） — Task 0 主 agent 自跑（无 subagent dispatch 即应用）
- **memory** `feedback_spec_writer_self_drift.md`（摘要：spec 作者自己会引入 plan-drift） — rev 1 引 STATUS §9.3 是 spec writer self-drift 实证
- **memory** `project_v2_31_1_packaging_cleanup.md`（摘要：v2.25+ tag-only release 模式） — release 形式
- **memory** `feedback_cp_batch_quality_review.md`（摘要：同质机械 task 用 CP-末 batch review 替 per-task） — 本 PR 完全无 per-task reviewer，全主 agent 验证
- **项目术语 §4** §11 vs §12 follow-up 轨道 — 实证：本 PR 闭 §11-N16，未触 §12（spec self follow-up 无登记）
- **项目术语 §5** pure refactor / no-RED phase — 完全适用：CLAUDE.md docs append 无 RED/GREEN
- **项目术语 §6** 5 层 + 1 scout 审查 — spec ~80 行 < 100 threshold，跑 L4+L5+L6 三层 + scout 即可
- **项目术语 §7** subagent-driven 模式 — 本 PR 反例：小型 pure docs PR 不适用，主 agent inline ROI 更高（L-3 教训）
- **项目术语 §10** plan-drift 5 分类 — 本 PR scout 抓 (b) 路径假设错（rev 1 引 STATUS §9.3）+ 防 (d) AC-2 grep `-E` 跨平台

---

## §7 数据总结

| 维度 | 数据 |
|---|---|
| spec + plan + retro 总行数 | spec (202) + plan (498) + retro (~250) = ~950 行 |
| code diff | 731 insertions / 1 deletion / 4 文件 |
| pytest | 不跑（pure docs）|
| CI 全套 | 9/9 SUCCESS, ~7-8 min wall time |
| **CI 连续一次过纪录** | **28 PR** 延续 |
| §11 follow-up 闭合 | N-16（1 项）|
| §11 follow-up 新登记 | N-18（migration 工艺）/ N-19（本 retro PR 回填，自闭）= 2 项 |
| lesson 沉淀 | **7 项**（L-1 用户审查 / L-2 memory 真值漂移 / L-3 主 agent ROI / L-4 audit log / L-5 self-ref 多 grep / L-6 4Q 锁定 / L-7 brainstorm 抓漂移 100× ROI） |
| 主 agent override | 0（无 reviewer 误判） |
| plan-drift 抓 | 1 CRITICAL（EC-DRIFT-1，brainstorming 阶段抓）|

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
