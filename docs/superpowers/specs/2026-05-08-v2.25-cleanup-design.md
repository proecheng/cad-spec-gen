# v2.25.x cleanup — §11 follow-up 6 项设计文档

**版本**：rev 4（2026-05-08 — 经第三轮常见疏漏审查后修订）
**日期**：2026-05-08
**目标**：闭一笔 v2.25.0 PR #59 review 阶段登记的 6 项技术债（§11.I-1 / I-2 / I-3 / I-4 / I-5 / M-3），单 PR 集成。

## rev 3 → rev 4 修订记录

第三轮自审重点：常见疏漏（测试 oracle 强度 / 契约缺口 / Unicode 边界 / 错误消息 / placement order / 既有代码契合度）。实测 + grep 后发现 3 BLOCKER + 5 MAJOR + 2 MINOR：

- **F1-真**（BLOCKER）：rev 2 给的 `load_dictionary` 改动代码示例字段名错——写 `subsystem_synonyms=...`，实际 dataclass 字段是 `subsystem_keywords`（实测 `tools/project_guide_dict/__init__.py:17`）；implementer 拷贝即 TypeError。**修：完整重写 load_dictionary 代码示例 + 校准字段名 + 异常类型用既有 `RuntimeError` 不引入 `ValueError`**
- **A-strength**（BLOCKER）：测试 oracle 强度未规范，evidence_token / e2e pin 等测试可能写出"假绿"断言。**修：加测试 oracle 强度规范表 + e2e pin 7 fixture 完整代码示例**
- **F7-fixture**（BLOCKER）：既有 `test_load_dictionary_raises_on_implemented_subsystem_missing_kpis`（line 71）用 tmp_path 写 `kpi_patterns.json = "{}"`——加 schema_version 校验后会先触发 schema_version raise，原 raises match 失败。**修：加既有测试同步清单表，逐条说明改与不改**
- **E-edge**（MAJOR）：`_classify_unsafe_reason("")` "safe" 与 `_safe_cli_token("")` False 含义漂移；docstring 加注语义差异
- **E-cjk-scope**（MAJOR）：实测日文 Kanji 误归 chinese_text；Hangul 归 special_chars——产品边界未明示。修：加 Unicode 范围表 + 归类约定声明
- **B-contract**（MAJOR）：builder 哪些必带 `preview_cli` 字段未分类（`unknown_subsystem` / `not_yet_implemented` 不带）。修：加 builder action dict 契约分类表
- **D-mutate**（MAJOR）：`load_dictionary` `pop()` 是 in-place mutate；spec 加无副作用保证声明
- **F-placement**（MAJOR）：常量/函数 placement order 在文件里未明确。修：加文件内放置顺序图
- **L-edge**（MINOR）：用户场景表加入 `\\` / `\n` / `\t` / 单空格 / 日文 / 韩文 / 引号场景实测结果（含 `"高\"精度\""` 在新优先级下归 chinese_text 不归 special_chars 的修正）
- **M-mirror**（MINOR）：commit 7 镜像 diff reviewer 阅读指引未明（diff 体量大但只是机械同步）

## rev 2 → rev 3 修订记录

第二轮自审重点：数据一致性 / 函数一致性 / 漂移 / 边界。发现 3 BLOCKER + 8 MAJOR + 3 MINOR（多为 rev 2 大批量修订引入的次生不一致）：

- **D-1**（BLOCKER）：测试矩阵标题"+18"实际 6+14=20；guide 子表 header"+12"实际 14。修：标题改 +20 / +14
- **F-1**（BLOCKER）：line 249 函数路径仍指 `src/cad_spec_gen/data/tools/project_guide.py`（rev 2 全文路径修正漏一处）。修：改回 `tools/project_guide.py`
- **S-1**（BLOCKER）：commit 顺序 7→8 后"工作流约束"段仍说"commit 7 更新 PROGRESS.md"，与 commit 表不一致。修：commit 8 docs，commit 7 dev_sync
- **D-2/D-4**（MAJOR）：全量回归"17 新测试"→ 20；commit 表 commit 2 RED 数 18 → 20
- **F-5**（MAJOR）：`_classify_unsafe_reason` docstring 第一行返回值列表顺序与第三行优先级不一致。修：列表按优先级序排
- **F-6**（MAJOR）：line 80 不变量缺一句"`_safe_cli_token` 与 `_classify_unsafe_reason` 共用 `_SAFE_CLI_PATTERN`"。补
- **S-2**（MAJOR）：dev_sync 命令在 commit 6 vs 7 矛盾。修：明确 commit 7 单 commit 跑
- **S-3**（MAJOR）：commit 7 镜像同步后是否再跑 pytest 未明示。修：commit 7 跑 dev_sync 后必再跑全量 pytest（验证 src 镜像 import 路径）
- **S-4**（MAJOR）：I-5 spec line 15/352 改前/改后字面文本未给（与 I-2 不对称）。修：补完整改前/改后块
- **E-3**（MAJOR）：dataclass `__post_init__` 单入口约束未声明（可绕过 pop 直接构造）。修：加 ordering 约束"测试不得直接构造 dataclass with raw kpi_patterns 含 schema_version"
- **E-6**（MAJOR）：CI per-commit vs HEAD-only 假设未声明。修：加约束"CI 默认看 push HEAD"
- **E-1**（MINOR）：`_classify_unsafe_reason(None)` 类型签名约束。修：docstring 加注
- **D-3**（MINOR）：line 580"6 项独立每项 ~15-30 行"已不准确（dataclass / fixture / e2e / shared constant 大改）。修：reword

## rev 1 → rev 2 修订记录

经 3 reviewer 并行对抗审查（5 角色 / 代码-spec 对照 / dry-run）+ 实测验证，修订以下 BLOCKER / MAJOR：

- **B-1**：原 spec 描述的"行程 200 厘米 → 200000mm 生产 bug"经实测**不存在**——`"行程 200 厘米"` 经 `cm|厘米` regex（regex_idx=1）命中，按 `unit_normalize.keys()[0]="cm"` factor=10 → 2000mm 正确。I-1 的 P1 锚点失效，理由降级为"消除位置耦合 + 清死代码（厘米/米 key 永不命中）+ 防未来编辑 bug"
- **B-2**：原 spec 改动方向反——canonical 在 `tools/`（root），`src/cad_spec_gen/data/tools/` 是构建副本；改 src 副本会被 dev_sync root → src 单向镜像反向覆盖。全文路径修正
- **B-3**：原 spec I-3 `_raw_token_for_match` fallback 判定 `len(raw_token) != end-start` 永远 false（切片长度恒等）；真问题是 finditer 跑在 normalized 上，match.start/end 是 normalized 索引域。fallback 改为 `len(raw_text) == len(normalized)` 判断
- **B-4**：commit 3 在 kpi_patterns.json 顶层加 `schema_version` 后，dataclass `ProductGoalDictionary.__post_init__` 会把它当 subsystem 校验红；`load_dictionary` 必须先 pop schema_version；既有 `test_kpi_patterns_json_has_3_kpis_per_implemented_subsystem` 同步
- **B-5**：commit 4 漏改 `parser_evidence` list 的 `"token"` 字段 — `KpiExtraction.evidence_token` 与 evidence list `"token"` 必须双改原文切片，否则 audit 仍部分失真
- **M-1**：commit 2 RED 测试不得在测试模块顶层 import 尚未存在的符号（如 `from tools.project_guide import _action_for_xxx`）——会导致 collection ImportError 全文件红
- **M-2**：M-3 `_classify_unsafe_reason` 优先级反转——`chinese_text` 优先于 `windows_path`；windows_path 判定收紧到 `_WIN_PATH_PATTERN.match`，不 OR `"\\" in value`（避免 `"高\"精度\""` 中转义反斜杠误归 windows_path）
- **M-3**：I-4 测试矩阵加 1 条 e2e 集成 pin（保证 builder 拆分后 `_derive_goal_status_and_next_action` 端到端契约不漂移）
- **M-4**：spec 明确 `unsafe_reason` 与 `preview_cli_unsafe` 同生共灭不变量 + 加 1 条集成测试
- **M-5**：抽 `_SAFE_CLI_PATTERN` 共享常量复用于 `_safe_cli_token` 与 `_classify_unsafe_reason`

---

## 问题陈述

v2.25.0 PR #59 合并时 review 阶段登记了 6 项 follow-up 推迟到 cleanup PR 处理：

| 编号 | 类型 | 摘要 |
|---|---|---|
| I-1 | code | `unit_normalize` 字典按位置对齐 regex 数组，索引脆弱 |
| I-2 | spec | spec rev 4 line 286（硬窗口）vs line 316（软窗口）语义矛盾 |
| I-3 | code | `evidence_token` 取 NFKC 后字符串而非用户原文，audit 失真 |
| I-4 | code | `_derive_goal_status_and_next_action` 80 行偏长，路由与构造混合 |
| I-5 | spec | 降级文案与 spec 模板"等"字面差异 |
| M-3 | code | `_sanitize_preview_cli` 对中文输入与 Windows 反斜杠路径走同一通用提示，无法给精确下一步 |

实测过程中追加发现的"P1 生产 bug"经 reviewer 实测**证伪**：`"行程 200 厘米"` 经 `cm|厘米` regex（regex_idx=1）命中，factor=10 → 2000mm 正确（`python -c "from product_goal_parser import parse_product_goal; print(parse_product_goal('升降平台 升程 200 厘米').kpis['stroke_mm'].value)"` 实测输出 `2000.0`）。但词典 `unit_normalize` 含 4 key（cm/m/厘米/米）而 regex 仅 3 条 — **"厘米"/"米"两 key 是死代码**（按 `regex_idx > 0` + `unit_keys[regex_idx-1]` 的位置算法永不命中）。这不是行为 bug，是隐蔽的脆耦合 + 维护陷阱：未来编辑者若按 dict key 顺序直觉解读会误以为"厘米→10 / 米→1000"已经生效。I-1 schema 重构理由：消除位置耦合 + 清死代码 + 让未来编辑安全。

---

## 北极星 5 gate 检查

| Gate | 设计如何过 |
|---|---|
| 零配置 | 仅改产线内部 `kpi_patterns.json` schema；用户接触的 CLI / `PROJECT_GUIDE.json` 顶层 schema 不变 |
| 稳定可靠 | TDD RED → GREEN；schema_version 硬切让任何不一致立即报错；每项独立单元测试 + e2e pin |
| 结果准确 | I-1 清掉死代码防未来编辑误解；I-3 evidence_token 真实记录用户原文（含全角）|
| SW 装即用 | 不动 SolidWorks / parts 路径 |
| 傻瓜式操作 | M-3 unsafe_reason 分类后用户拿到精确下一步提示而非通用文案 |

---

## 范围

### 影响文件（canonical 在 `tools/`，dev_sync 单向 root → src 镜像）

| 项 | 文件（canonical）| 改动类型 |
|---|---|---|
| I-1 | `tools/product_goal_parser.py` + `tools/project_guide_dict/kpi_patterns.json` + `tools/project_guide_dict/__init__.py` | refactor + schema 升级 + dataclass 校验 |
| I-2 | `docs/superpowers/specs/2026-05-07-product-goal-entry-design.md` line 316 | 文档收敛 |
| I-3 | `tools/product_goal_parser.py` | refactor（双改 `evidence_token` + evidence list `"token"`）|
| I-4 | `tools/project_guide.py` | refactor（拆 7 builder）|
| I-5 | spec line 15 / 352；代码文案 | 文档收敛 + 文案重写 |
| M-3 | `tools/project_guide.py:_sanitize_preview_cli` | refactor + 新增 `_classify_unsafe_reason` + 抽 `_SAFE_CLI_PATTERN` 共享常量 |
| 测试 fixture | `tests/test_kpi_patterns.py` 等所有读 kpi_patterns.json 的既有测试 | schema_version 字段同步 |
| dev_sync 镜像 | `src/cad_spec_gen/data/tools/*.py` | 自动跟随（commit 7 跑 `python scripts/dev_sync.py` 同步）|

### 不在 scope

- 不扩 19 类子系统 / 6 KPI（v2.27+）
- 不动 PROJECT_GUIDE.json 顶层 schema（仅 `next_action` 内部加 `unsafe_reason` 字段）
- 不接真实 AI adapter（Phase 5 在更高优先级队列）
- 不改 photo3d_* / render_* / SolidWorks 任何链路
- 不改 CLI flags

### 不变量保持

- `mutates_pipeline_state=false`、`does_not_scan_directories=true` 在 `write_project_goal_guide` 返回 dict 中保留
- `entry_mode=product_goal` 路径 7 状态完整保留
- `_safe_cli_token` 正则不放宽（M-3 走 reason 路线，不开闸）；与 `_classify_unsafe_reason` 共用 `_SAFE_CLI_PATTERN` 共享常量（M-5 修订）
- `evidence_token` 字段名/类型不变（仅取值变；下游 reader 透明）
- `next_action.preview_cli_unsafe` boolean 字段保留向后兼容
- `unsafe_reason` 与 `preview_cli_unsafe` 同生共灭（M-4）；safe 路径下两字段都不写

---

## 设计

### I-1：`unit_normalize` 重构（消除位置耦合 + 清死代码）

#### `kpi_patterns.json` schema v2

```json
{
  "schema_version": 2,
  "lifting_platform": {
    "stroke_mm": {
      "regex": [
        {"pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:mm|毫米)", "factor": 1},
        {"pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:cm|厘米)", "factor": 10},
        {"pattern": "(\\d+(?:\\.\\d+)?)\\s*m(?![ms])",   "factor": 1000},
        {"pattern": "(\\d+(?:\\.\\d+)?)\\s*米(?!\\s*厘)", "factor": 1000}
      ],
      "context_terms": ["行程", "升高", "升程", "stroke", "travel"],
      "unit": "mm",
      "slot": "capability_2"
    }
    /* 其它 KPI 不需要 unit_normalize 的也按新 schema 写：
       "regex": [{"pattern": "...", "factor": 1}, ...] */
  }
}
```

- 顶层增加 `"schema_version": 2` 字段
- `regex` 从 `string[]` 改为 `[{pattern: string, factor: number}]`
- 删除 `unit_normalize` 字段
- 所有 KPI 都按新 schema 重写（不仅 `stroke_mm`）；不需要换算的 KPI 把 factor=1 显式写出

#### `tools/project_guide_dict/__init__.py` 改动（dataclass 二次校验）

`ProductGoalDictionary.__post_init__` 现遍历 `self.kpi_patterns.items()` 把每个 key 当 subsystem 校验。新 schema 顶层多出 `schema_version` int，必须在 `load_dictionary` 把它**先 pop 再传 dataclass**：

```python
# tools/project_guide_dict/__init__.py
def load_dictionary(*, dict_root: Path | None = None) -> ProductGoalDictionary:
    """从 JSON 文件加载词典；缺文件 / schema 错 → RuntimeError。"""
    root = dict_root or _DEFAULT_ROOT
    keywords_path = root / "subsystem_keywords.json"
    patterns_path = root / "kpi_patterns.json"

    if not keywords_path.is_file():
        raise RuntimeError(f"subsystem_keywords.json 不存在：{keywords_path}")
    if not patterns_path.is_file():
        raise RuntimeError(f"kpi_patterns.json 不存在：{patterns_path}")

    keywords = json.loads(keywords_path.read_text(encoding="utf-8"))
    patterns = json.loads(patterns_path.read_text(encoding="utf-8"))

    # schema_version 顶层校验 + pop（不修改源 dict 缓存：pop 在 json.loads 返回的
    # 临时 dict 上，不被外部缓存引用）
    schema_version = patterns.pop("schema_version", None)
    if schema_version != 2:
        raise RuntimeError(
            f"kpi_patterns.json schema_version 必须是 2，实际：{schema_version!r}"
        )

    return ProductGoalDictionary(
        subsystem_keywords=keywords,    # 实际字段名（不是 subsystem_synonyms）
        kpi_patterns=patterns,           # 已 pop schema_version 的纯子系统 dict
    )
```

**字段名校准**：实际 `ProductGoalDictionary` 只有 2 字段 — `subsystem_keywords` + `kpi_patterns`（rev 2 错写为 `subsystem_synonyms`，已修）。

**异常类型校准**：`load_dictionary` 既有的失败约定是 `RuntimeError`（line 90 / 92），spec 应延续 `RuntimeError`，不要改成 `ValueError` 让既有 `pytest.raises(RuntimeError, match=...)` 测试断。

**无副作用保证**：`patterns.pop(...)` 是 in-place mutate `json.loads(...)` 返回的临时 dict，无外部引用；不会泄漏 mutation 给 caller。

dataclass `__post_init__` 不变（仍按子系统遍历），但要确保它**只在 pop 后被调用**。

#### `tools/product_goal_parser.py` 改动

```python
# _extract_single_kpi 主循环
for regex_entry in spec["regex"]:
    pattern = regex_entry["pattern"]
    factor = regex_entry["factor"]
    compiled = re.compile(pattern)
    for match in compiled.finditer(normalized):
        # ... distance check ...
        if value_shape == "single":
            raw = float(match.group(1))
            value = raw * factor   # factor=1 时不变
        # ...
```

- 删除原 `unit_normalize.keys()` 索引代码（line 252-255）
- 删除 `if regex_idx > 0` 特殊处理
- factor 直接随 regex_entry 携带，无位置耦合

#### 既有测试 fixture 同步（commit 3 必做）— 已 grep 出全清单

实测 `grep -rn "kpi_patterns.json\|load_dictionary" tools/ tests/` 的清单：

| 测试 / 调用 | 现状 | commit 3 修订 |
|---|---|---|
| `tests/test_product_goal_parser.py:35` `test_kpi_patterns_json_has_3_kpis_per_implemented_subsystem` | `assert set(data.keys()) == {"lifting_platform", "end_effector"}`（line 40）+ 遍历 data.items() 校验 KPI（line 46）| 改成 `assert set(data.keys()) - {"schema_version"} == {...}`；`kpi_patterns_data = {k: v for k, v in data.items() if k != "schema_version"}` 后再遍历 |
| `tests/test_product_goal_parser.py:62` `test_load_dictionary_raises_on_missing_file` | tmp_path 写 `subsystem_keywords.json` 但**不写** `kpi_patterns.json` → 期望 `RuntimeError, match="kpi_patterns.json"` | **保持不变**（缺文件路径走 line 92 既有 raise，不到 schema_version 校验）|
| `tests/test_product_goal_parser.py:71` `test_load_dictionary_raises_on_implemented_subsystem_missing_kpis` | tmp_path 写 `kpi_patterns.json = "{}"` → 期望 `RuntimeError, match="lifting_platform.*kpi_patterns"` | **必改**：tmp_path 内容 `{}` 改成 `{"schema_version": 2}` —— 否则 schema_version=None → RuntimeError "schema_version 必须是 2" 先触发，原 implemented-subsystem-missing-kpi raise match 失败 |
| `tools/product_goal_parser.py:56` `dictionary = load_dictionary()` | 默认调用 | 透明（load_dictionary 内部已处理 schema_version pop）|
| `tools/project_guide_dict/__init__.py:97` `return ProductGoalDictionary(...)` | dataclass 实例化 | 已在 load_dictionary 内 pop 后传入；dataclass 自身不需改 |

无 `tools/project_guide_dict/_smoke.py` 启动校验脚本（实测仓库内不存在）。

**plan 阶段 task 0 必做**：commit 3 实施前再 grep 一遍 `kpi_patterns.json` / `load_dictionary` 全仓，与上表 diff 比对——如新出现引用必须补入 ordering 约束。

#### 死代码消除

新 schema 下"厘米"被 regex `(?:cm|厘米)` 单条捕捉 + factor=10；"米"被 regex `米(?!\s*厘)` 单条捕捉 + factor=1000；不再依赖 dict 顺序。原 dict 中"厘米"/"米"两个永不命中的死 key 自然消除。

### I-2：spec line 316 软窗口 → 硬窗口

**改前**：
```
- ±20 char 是软窗口；超出窗口仍允许，但 parser_evidence 标 weak
```

**改后**：
```
- ±20 char 是硬窗口；超出窗口直接不抽（status=missing, rule=no_match）
- 这与代码 `tools/product_goal_parser.py:242 if min_dist > _DISTANCE_WINDOW: continue` 一致
```

仅文档改动，无代码 / 测试影响（既有硬窗口路径已被 `test_no_match_when_distance_exceeds_window` 等测试覆盖）。

### I-3：`evidence_token` 取原文切片（双改）

#### 改动位置

`tools/product_goal_parser.py:_extract_single_kpi` **两处都要改**（reviewer 抓到只改一处会导致下游 audit 仍部分失真）：

1. `KpiExtraction.evidence_token=match.group(0)`（line 268，函数返回值）
2. `evidence.append({..., "token": match.group(0), ...})`（line 259，evidence list `"token"` 字段）

#### 实现（修正 fallback 设计）

reviewer B 抓到原 fallback 设计有 bug：finditer 跑在 `normalized` 上（line 234 `compiled.finditer(normalized)`），`match.start()/end()` 是 normalized 索引域；直接用此索引去切 `raw_text` 在 NFKC 拆分场景（如 `⅔ → 2/3`）会错位。原 spec `len(raw_token) != end-start` 永远 false，fallback 永不触发。

修正：在归一化时检查 raw 与 normalized 长度，相等才走原文切片：

```python
def _raw_token_for_match(
    raw_text: str,
    normalized: str,
    match: re.Match[str],
) -> str:
    """从原文取 match 区间对应的子串。

    NFKC 在 ASCII 路径与全角→半角场景下保持 char 数 1:1（len(raw)==len(normalized)），
    此时 match.start()/end() 在两个串上索引等价，可直接切 raw_text。
    极少数 NFKC 拆分（⅔ → 2/3、㎡ → m2）会让 normalized 比 raw_text 长，索引域错位
    → fallback 到 match.group(0)（normalized 串上 token，与现状等价不优于 normalized）。
    """
    if len(raw_text) == len(normalized):
        return raw_text[match.start():match.end()]
    return match.group(0)
```

调用点（双改）：
```python
# evidence list "token" 字段（line 259）
raw_token = _raw_token_for_match(raw_text, normalized, match)
evidence.append({
    "token": raw_token,
    "matched": kpi_name,
    ...
})
# KpiExtraction.evidence_token（line 268）
return KpiExtraction(
    ...,
    evidence_token=raw_token,
    ...
)
```

#### 用户场景

输入 `"载荷 ５０ｋｇ 平台"`（全角数字 + 全角字母）：
- NFKC 后：`"载荷 50kg 平台"`（len 相等：全角 char 1:1 → 半角 char）
- 旧实现：`evidence_token = "50kg"`（用户没看见自己打的 ５０ｋｇ）
- 新实现：`evidence_token = "５０ｋｇ"`（保留原文，audit 友好）

输入含 NFKC 拆分（罕见，如 `"载荷⅔kg"`）：
- NFKC 后：`"载荷2/3kg"`（len 不等）
- fallback：`evidence_token = match.group(0)`（normalized 串上 token，等价旧行为）

### I-4：拆 7 个 per-status builder

#### 新增 7 个函数（`tools/project_guide.py`）

```python
def _action_for_needs_product_goal(parse_result: Any) -> dict[str, Any]:
    return {
        "kind": "supply_product_goal",
        "preview_cli": (
            'python cad_pipeline.py project-guide --product-goal "<描述你的产品>"'
        ),
    }

def _action_for_needs_subsystem_confirmation(parse_result: Any) -> dict[str, Any]:
    return {
        "kind": "confirm_subsystem",
        "preview_cli": (
            'python cad_pipeline.py project-guide --product-goal "..." '
            "--confirm-subsystem <lifting_platform|end_effector>"
        ),
    }

def _action_for_unknown_subsystem(parse_result: Any) -> dict[str, Any]:
    return {
        "kind": "list_supported_subsystems",
        "supported": ["lifting_platform", "end_effector"],
    }

def _action_for_not_yet_implemented(parse_result: Any) -> dict[str, Any]:
    return {
        "kind": "wait_for_implementation",
        "alternatives": {
            "implemented_subsystems": ["lifting_platform", "end_effector"],
            "switch_example": (
                'python cad_pipeline.py project-guide --product-goal "做升降平台 50kg"'
            ),
            "feedback_url": "https://github.com/proecheng/cad-spec-gen/issues/new",
        },
    }

def _action_for_needs_kpi_confirmation(
    parse_result: Any, missing: list[str]
) -> dict[str, Any]:
    return {
        "kind": "supply_missing_kpis",
        "missing_kpis": missing,
        "preview_cli": _build_kpi_preview_cli(parse_result.subsystem_class, missing),
    }

def _action_for_needs_design_doc(parse_result: Any) -> dict[str, Any]:
    return {
        "kind": "supply_design_doc",
        "preview_cli": (
            f"python cad_pipeline.py project-guide "
            f'--product-goal "{parse_result.raw_text}" '
            f"--design-doc docs/design/<chapter>-{parse_result.subsystem_class}.md"
        ),
    }

def _action_for_ready_for_cad_spec(
    parse_result: Any, design_doc: str | Path
) -> dict[str, Any]:
    return {
        "kind": "run_cad_spec",
        "preview_cli": (
            f"python cad_pipeline.py spec --subsystem {parse_result.subsystem_class} "
            f"--design-doc {design_doc}"
        ),
    }
```

#### `_derive_goal_status_and_next_action` 重写为纯路由

```python
def _derive_goal_status_and_next_action(
    parse_result: Any,
    design_doc: str | Path | None,
    root: Path,
) -> tuple[str, dict[str, Any]]:
    """从 parse 结果派生 status + next_action。

    路由层：决定 status，调对应 builder，最后统一过 _sanitize_preview_cli。
    """
    if not parse_result.raw_text or not parse_result.raw_text.strip():
        status = "needs_product_goal"
        action = _action_for_needs_product_goal(parse_result)
    elif parse_result.subsystem_status == "ambiguous":
        status = "needs_subsystem_confirmation"
        action = _action_for_needs_subsystem_confirmation(parse_result)
    elif parse_result.subsystem_status == "unknown":
        status = "unknown_subsystem"
        action = _action_for_unknown_subsystem(parse_result)
    elif parse_result.subsystem_status == "not_yet_implemented":
        status = "not_yet_implemented"
        action = _action_for_not_yet_implemented(parse_result)
    else:
        # implemented 分支
        missing = [
            name for name, k in parse_result.kpis.items() if k.status != "extracted"
        ]
        if missing:
            status = "needs_kpi_confirmation"
            action = _action_for_needs_kpi_confirmation(parse_result, missing)
        elif not design_doc:
            status = "needs_design_doc"
            action = _action_for_needs_design_doc(parse_result)
        else:
            status = "ready_for_cad_spec"
            action = _action_for_ready_for_cad_spec(parse_result, design_doc)

    return status, _sanitize_preview_cli(action, parse_result)
```

主函数从 80 行降至 ~30 行；每 builder 可独立单元测试。`_sanitize_preview_cli` 路由层统一调一次（builder 内部不再调）。

#### Builder action dict 契约分类（B-contract 修订）

不是所有 builder 都返回 `preview_cli` 字段——`_sanitize_preview_cli` 已 short-circuit on `not isinstance(cli, str)`，但 plan 阶段 implementer 容易误以为每 builder 必带 preview_cli：

| builder | 必带字段 | 可选字段 | 不带 preview_cli? |
|---|---|---|---|
| `_action_for_needs_product_goal` | `kind="supply_product_goal"` + `preview_cli` | — | 带 |
| `_action_for_needs_subsystem_confirmation` | `kind="confirm_subsystem"` + `preview_cli` | — | 带 |
| `_action_for_unknown_subsystem` | `kind="list_supported_subsystems"` + `supported` | — | **不带** |
| `_action_for_not_yet_implemented` | `kind="wait_for_implementation"` + `alternatives` | — | **不带** |
| `_action_for_needs_kpi_confirmation` | `kind="supply_missing_kpis"` + `missing_kpis` + `preview_cli` | — | 带 |
| `_action_for_needs_design_doc` | `kind="supply_design_doc"` + `preview_cli` | — | 带 |
| `_action_for_ready_for_cad_spec` | `kind="run_cad_spec"` + `preview_cli` | — | 带 |

builder 单元测试断言 `kind` + 必带字段；缺 `preview_cli` 的 2 个 builder 不要断言 `preview_cli` 存在。

### M-3 + I-5：`_classify_unsafe_reason` + 文案随 reason 变

#### 共享常量（M-5 修订）+ 文件 placement order（F-placement 修订）

避免 `_safe_cli_token` 与 `_classify_unsafe_reason` 两处独立写正则导致字面发散。

**文件内放置顺序**（`tools/project_guide.py`）：
```
模块级常量 (_SAFE_CLI_PATTERN / _CJK_PATTERN / _WIN_PATH_PATTERN / _UNSAFE_MESSAGE_TEMPLATES)
    ↓
def _safe_cli_token (line 631 既有位置)
    ↓
def _classify_unsafe_reason (新增，紧跟 _safe_cli_token)
    ↓
def _sanitize_preview_cli (line 681 既有位置)
    ↓
def _action_for_* x 7 (新增，紧跟 _sanitize_preview_cli 之前或之后)
    ↓
def _derive_goal_status_and_next_action (line 710 既有位置，重写为路由)
```

```python
# tools/project_guide.py 顶层
_SAFE_CLI_PATTERN = re.compile(r"[A-Za-z0-9_.\-]+")
_CJK_PATTERN = re.compile(r"[一-鿿㐀-䶿]")  # CJK Unified (4E00-9FFF) + Ext-A (3400-4DBF)
_WIN_PATH_PATTERN = re.compile(r"^[A-Za-z]:[\\/]")

def _safe_cli_token(value: str) -> bool:
    return bool(_SAFE_CLI_PATTERN.fullmatch(value or ""))
```

字符类正则用 Unicode 转义而非字面 `一-鿿`，让 grep / diff 友好。

#### Unicode 范围与 chinese_text 命名声明（E-cjk-scope 修订）

| Unicode 块 | 是否归 chinese_text | 实测 |
|---|---|---|
| CJK Unified（U+4E00-U+9FFF）含中文 / 日文 Kanji | ✅ chinese_text | "升降" / "昇降" 都归 chinese_text |
| CJK Ext-A（U+3400-U+4DBF）罕见汉字 | ✅ chinese_text | 罕用字归 chinese_text |
| Hangul（U+AC00-U+D7AF）韩文 | ❌ special_chars | "승강기" 归 special_chars（产品边界：本管线针对中文用户，韩文走通用文案）|
| Hiragana / Katakana 假名（U+3040-U+30FF）| ❌ special_chars | "あいうえお" 归 special_chars（仅日文 Kanji 命中 CJK Unified）|

**reason 名 `chinese_text` 略有误导**（实际含日文 Kanji），但保留命名不改——日文 Kanji 输入命中"中文专用文案"的提示在产品语境下仍合理（让用户用 --confirm-X 直传值）。如未来需更精确，可改名 `cjk_text`，留作 v2.27+ follow-up 不在本 PR scope。

**Hangul / 假名等不在 CJK 范围**：归 special_chars 是 acceptable degradation（仍触发降级，文案通用但不阻断流程）。

#### 新增 `_classify_unsafe_reason`（优先级修订）

reviewer C 抓到原优先级反例：用户在中文里随手敲反斜杠（如 `"做\升降"` 或 `"高\"精度\""` 转义引号）会被误分到 `windows_path`。修订：

1. **CJK 优先于 windows_path**：含中文一律走 chinese_text 提示
2. **windows_path 收紧**：只看 `^[A-Za-z]:[\\/]` 起始（盘符冒号），不再 OR `"\\" in value`

```python
def _classify_unsafe_reason(value: str) -> str:
    """分类 user_text 不安全原因，给出精确下一步提示。

    返回值（按优先级）：'safe' | 'chinese_text' | 'windows_path' | 'special_chars'。
    优先级（互斥）：safe > chinese_text > windows_path > special_chars。

    Empty string 与 None：value 必须为 str；调用方（_sanitize_preview_cli）已用
    `or ""` 兜底 None。empty 时返 "safe"——含义"无 unsafe 内容需要降级"，与
    _safe_cli_token("") = False 不同（后者用于 token 校验，empty 不算合法 token）。
    本函数只在 sanitize 调用且 raw_text 非 empty 时执行，故 empty 路径实际不可达；
    保留 short-circuit 是防御性写法。

    日文 Kanji 命中 CJK Unified 范围会归 chinese_text；Hangul / 假名归
    special_chars——本管线针对中文用户的产品边界。
    """
    if not value:
        return "safe"
    if _SAFE_CLI_PATTERN.fullmatch(value):
        return "safe"
    if _CJK_PATTERN.search(value):
        return "chinese_text"
    if _WIN_PATH_PATTERN.match(value):
        return "windows_path"
    return "special_chars"
```

`"D:\Work\做平台.md"`（中文 + 盘符路径）会归 `chinese_text`——用户主要痛点是中文（forward slash 改写后仍有中文段），文案"用 --confirm-X flag 直接传值"对此场景更合适。

#### `_sanitize_preview_cli` 改写

```python
_UNSAFE_MESSAGE_TEMPLATES = {
    "windows_path": (
        "<Windows 路径含反斜杠/冒号；请用 forward slash 或加双引号包裹路径>"
    ),
    "chinese_text": (
        "<user_text 含中文；请用 --confirm-X flag 直接传值，不通过自然语言>"
    ),
    "special_chars": (
        "<user_text 含特殊字符（引号/换行等）；"
        "请用 --confirm-X flag 直接传值，不通过自然语言>"
    ),
}

def _sanitize_preview_cli(
    action: dict[str, Any],
    parse_result: Any,
) -> dict[str, Any]:
    """检查 next_action.preview_cli 是否含 user text；含且 unsafe 则降级。

    不变量（M-4 修订）：preview_cli_unsafe 与 unsafe_reason 同生共灭——
    不存在 preview_cli_unsafe=True 但 unsafe_reason 缺失的状态；
    safe 路径下两个字段都不写。
    """
    raw_text = getattr(parse_result, "raw_text", None) or ""
    if not raw_text:
        return action
    cli = action.get("preview_cli")
    if not isinstance(cli, str):
        return action
    if raw_text not in cli:
        return action
    reason = _classify_unsafe_reason(raw_text)
    if reason == "safe":
        return action
    action["preview_cli_unsafe"] = True
    action["unsafe_reason"] = reason
    action["preview_cli"] = _UNSAFE_MESSAGE_TEMPLATES[reason]
    return action
```

#### 不变量声明（M-4 + reader 兼容性）

- `unsafe_reason` 字段值域闭集：`{"windows_path", "chinese_text", "special_chars"}`
- `preview_cli_unsafe=True` ⇔ `unsafe_reason` 存在；safe 路径下两个字段都不写
- 既有 `tests/test_project_goal_guide.py:126-141`（仅断言 `preview_cli_unsafe is True`）保持兼容
- 新增字段 `unsafe_reason` 不破坏既有 reader（grep 全仓未发现 PROJECT_GUIDE.json schema validator；新字段是 additive 无禁用 enum）

#### Spec 修订（`docs/superpowers/specs/2026-05-07-product-goal-entry-design.md` line 15 / 352）

##### line 15 DR-4 改前
```
- **DR-4（MINOR）：场景 5 preview_cli_unsafe 降级文案未规范**——补 spec 显式说明降级文案模板：`"<user_text 含特殊字符，请用 --confirm-X flag 直接传值，不通过自然语言>"`。
```

##### line 15 DR-4 改后
```
- **DR-4（MINOR）：场景 5 preview_cli_unsafe 降级文案未规范**——补 spec 显式说明降级文案模板（按 `_classify_unsafe_reason` 三类）：
  - windows_path: `"<Windows 路径含反斜杠/冒号；请用 forward slash 或加双引号包裹路径>"`
  - chinese_text: `"<user_text 含中文；请用 --confirm-X flag 直接传值，不通过自然语言>"`
  - special_chars: `"<user_text 含特殊字符（引号/换行等）；请用 --confirm-X flag 直接传值，不通过自然语言>"`
```

##### line 352 测试矩阵 case 5 改前
```
| 5（含转义字符） | preview_cli_unsafe 降级 | `"<user_text 含 \" 等特殊字符；请用 --confirm-X flag 直接传值，不通过自然语言>"` |
```

##### line 352 测试矩阵 case 5 改后（拆 3 行 + 落地新文案）
```
| 5a（chinese_text）| preview_cli_unsafe + unsafe_reason=chinese_text | 文案见 _UNSAFE_MESSAGE_TEMPLATES["chinese_text"] |
| 5b（windows_path）| preview_cli_unsafe + unsafe_reason=windows_path | 文案见 _UNSAFE_MESSAGE_TEMPLATES["windows_path"] |
| 5c（special_chars）| preview_cli_unsafe + unsafe_reason=special_chars | 文案见 _UNSAFE_MESSAGE_TEMPLATES["special_chars"] |
```

模板单点定义在 `tools/project_guide.py:_UNSAFE_MESSAGE_TEMPLATES`，spec 不再字面 inline 文案——避免文案漂移再次发生。

#### 用户场景（含实测边界 input）

| 输入 | 旧 reason | 新 reason | 文案 |
|---|---|---|---|
| `"做升降平台"` | unsafe（通用）| `chinese_text` | 中文专用 → `--confirm-X flag` |
| `"D:\Work\design.md"` | unsafe（通用）| `windows_path` | 路径专用 → forward slash 或双引号包裹 |
| `"D:\Work\做平台.md"`（中文+路径）| unsafe（通用）| `chinese_text` | 走中文路径（CJK 优先）|
| `"做\升降"`（中文里随手反斜杠）| unsafe（通用）| `chinese_text` | 走中文路径（不误归 windows_path）|
| `"高\"精度\""`（含中文+引号）| unsafe（通用）| `chinese_text` | 走中文路径（CJK 优先于 special_chars）—— 用户先解决"全角转半角"再考虑 quote 转义 |
| `"\"abc\""`（纯 ASCII 引号）| unsafe（通用）| `special_chars` | 通用提示 |
| `"昇降"`（日文 Kanji 在 CJK Unified）| unsafe（通用）| `chinese_text` | 走中文路径（reason 名误导但行为合理）|
| `"승강기"`（韩文 Hangul，不在 CJK）| unsafe（通用）| `special_chars` | 通用提示（产品边界：针对中文用户）|
| `"\\"`（单反斜杠）| unsafe（通用）| `special_chars` | 通用提示（不命中 windows_path 起始）|
| `"\n"` / `"\t"`（换行 / tab）| unsafe（通用）| `special_chars` | 通用提示 |
| `""`（空串）| —（sanitize short-circuit）| safe（理论返值）| 不触发降级（调用方 raw_text 为空时已 short-circuit）|
| `"lifting_platform"` | safe | `safe` | 不动 |

---

## 测试矩阵（共 +20 测试）

### 测试编写约束（M-1 修订）

- **所有新测试必须用函数内 import 引用 commit 5 才存在的符号**（如 `_action_for_needs_product_goal`），**禁止模块顶层 import**——否则 commit 2 测试 collection 会 ImportError 把全文件 19 个既有 GREEN 测试一起红
- 函数内 import 模式（项目惯例，与 `tests/test_project_goal_guide.py:11` 之后路径一致）：
  ```python
  def test_action_for_needs_product_goal():
      from tools.project_guide import _action_for_needs_product_goal
      ...
  ```
- 共用 helper（如 `_make_parse_result_fixture`）也走函数内 import 等号"自包含"模式

### 测试 oracle 强度规范（第三轮新增）

避免 plan 阶段 implementer 写出"假绿"测试（断言太弱让 bug 漏过）：

| 测试 | 必含强断言 | 不允许的弱断言 |
|---|---|---|
| `test_evidence_token_preserves_fullwidth` | `kpi.evidence_token == "５０ｋｇ"`（**全角等价比较**）| ❌ `"５０" in kpi.evidence_token`（包含即可，可能命中 normalized 串）|
| `test_evidence_list_token_preserves_fullwidth` | `result.parser_evidence[0]["token"] == "５０ｋｇ"` | ❌ 仅断言 `"token" in result.parser_evidence[0]` |
| `test_stroke_200_厘米_to_2000mm` | `kpi.value == 2000.0` 且 `kpi.unit == "mm"` 且 `kpi.status == "extracted"` | ❌ 只断言 `kpi.value > 0` |
| `test_kpi_patterns_schema_version_2` | 直接 `load_dictionary(dict_root=tmp_path)` 走完整入口；`pytest.raises(RuntimeError, match="schema_version")` 含未来错误消息字面 | ❌ 直接 `assert json.loads(...).get("schema_version") == 2` 不走 dataclass 校验 |
| `test_classify_unsafe_chinese_with_backslash` | `_classify_unsafe_reason("做\\升降") == "chinese_text"`（精确字符串比较）| ❌ `"chinese" in result` |
| `test_action_for_*` | 每 builder 断言 `action["kind"] == "<expected>"` + 关键 dict 键齐全（如 `unknown_subsystem` 必有 `supported`，不可缺）| ❌ `isinstance(action, dict)` |
| `test_derive_goal_status_e2e_all_7_paths` | 7 fixture 走真实 `parse_product_goal()` 入口（不 mock）；每 fixture 断言 `(status, action)` 元组完整等价 | ❌ 单独 mock `_action_for_*` 让测试与 builder 实际行为脱钩 |
| `test_sanitize_preview_cli_unsafe_reason_invariant` | 4 reason 各跑一遍：`assert action["preview_cli_unsafe"] is True and action["unsafe_reason"] == reason and action["preview_cli"] == _UNSAFE_MESSAGE_TEMPLATES[reason]`；safe 路径断言 `"preview_cli_unsafe" not in action and "unsafe_reason" not in action` | ❌ 仅断言其中一个字段 |

### e2e pin 测试 7 fixture 构造方式（N-fixture 修订）

`test_derive_goal_status_e2e_all_7_paths` 必须**走真实 `parse_product_goal()` 入口**构造 7 种 ParseResult，禁止 mock builder：

```python
def test_derive_goal_status_e2e_all_7_paths():
    from tools.product_goal_parser import parse_product_goal
    from tools.project_guide import _derive_goal_status_and_next_action
    from pathlib import Path

    cases = [
        # (input, design_doc, expected_status)
        ("",                                   None, "needs_product_goal"),
        ("升降",                                None, "needs_subsystem_confirmation"),  # supporting only
        ("xyzzy 不存在的产品",                   None, "unknown_subsystem"),
        ("做导航 SLAM",                         None, "not_yet_implemented"),
        ("做升降平台",                          None, "needs_kpi_confirmation"),  # KPI 缺
        ("做升降 50kg 200mm 350x230mm 平台",    None, "needs_design_doc"),
        ("做升降 50kg 200mm 350x230mm 平台",    Path("docs/x.md"), "ready_for_cad_spec"),
    ]
    for text, design_doc, expected_status in cases:
        parse_result = parse_product_goal(text=text)
        status, action = _derive_goal_status_and_next_action(
            parse_result, design_doc, root=Path(".")
        )
        assert status == expected_status, f"text={text!r}"
        assert isinstance(action, dict) and "kind" in action
```

不允许：mock `parse_product_goal` 返回构造的 ParseResult；mock `_action_for_*` builder。e2e pin 的价值在于"端到端契约"，mock 即失效。

### `tests/test_product_goal_parser.py`（+6）

| 测试名 | 类型 | 覆盖 |
|---|---|---|
| `test_kpi_patterns_schema_version_2` | RED → GREEN | I-1 schema 校验 |
| `test_stroke_200_厘米_to_2000mm` | guard | I-1 不回归（reviewer 实证现状已正确）|
| `test_stroke_0_2_米_to_200mm` | guard | I-1 中文"米"独立 regex 路径 |
| `test_stroke_200mm_basic` | guard | I-1 ASCII 路径不回归 |
| `test_evidence_token_preserves_fullwidth` | RED → GREEN | I-3 双改之函数返回值 |
| `test_evidence_list_token_preserves_fullwidth` | RED → GREEN | I-3 双改之 evidence list `"token"` 字段 |

### `tests/test_project_goal_guide.py`（+14）

| 测试名 | 类型 | 覆盖 |
|---|---|---|
| `test_action_for_needs_product_goal` | RED → GREEN | I-4 builder #1 |
| `test_action_for_needs_subsystem_confirmation` | RED → GREEN | I-4 builder #2 |
| `test_action_for_unknown_subsystem` | RED → GREEN | I-4 builder #3 |
| `test_action_for_not_yet_implemented` | RED → GREEN | I-4 builder #4 |
| `test_action_for_needs_kpi_confirmation` | RED → GREEN | I-4 builder #5 |
| `test_action_for_needs_design_doc` | RED → GREEN | I-4 builder #6 |
| `test_action_for_ready_for_cad_spec` | RED → GREEN | I-4 builder #7 |
| `test_derive_goal_status_e2e_all_7_paths` | guard | **I-4 e2e 集成 pin**（M-3 修订）—— 7 状态全路径走 `_derive_goal_status_and_next_action` 端到端断言 |
| `test_classify_unsafe_windows_path` | RED → GREEN | M-3 |
| `test_classify_unsafe_chinese_text` | RED → GREEN | M-3 |
| `test_classify_unsafe_chinese_with_backslash` | RED → GREEN | M-3 优先级 — `"做\升降"` 必须归 chinese_text 不归 windows_path |
| `test_classify_unsafe_special_chars` | RED → GREEN | M-3 |
| `test_classify_unsafe_safe_passthrough` | guard | M-3 不回归 |
| `test_sanitize_preview_cli_unsafe_reason_invariant` | RED → GREEN | **M-4 不变量** — `preview_cli_unsafe=True` ⇔ `unsafe_reason` 存在；safe 时两字段都不写 |

### 全量回归

- `pytest tests/`（既有 ~2470 测试 + 20 新测试 = ~2490 全绿）
- `ruff check . && ruff format --check .`
- `mypy --strict src/`
- `python scripts/dev_sync.py --check`

---

## Commit 顺序（TDD 节奏）

| # | type | 摘要 | canonical 文件 |
|---|---|---|---|
| 1 | docs | 增加 v2.25.x cleanup 设计文档（本 spec rev 2）+ spec line 316 软窗口→硬窗口（I-2）+ spec line 15/352 文案模板按 reason 分类（I-5）| 本 spec + 2026-05-07-product-goal-entry-design.md |
| 2 | test | 增加 §11 cleanup 20 RED 测试（红，函数内 import）| `tests/test_product_goal_parser.py` / `tests/test_project_goal_guide.py` |
| 3 | refactor | I-1 unit_normalize → list[{pattern,factor}] schema v2 + dataclass `__post_init__` 兼容（先 pop schema_version）+ 既有 fixture 同步 | `tools/product_goal_parser.py` + `tools/project_guide_dict/__init__.py` + `tools/project_guide_dict/kpi_patterns.json` + 既有测试 fixture |
| 4 | refactor | I-3 evidence_token + evidence list `"token"` 双改原文切片 | `tools/product_goal_parser.py` |
| 5 | refactor | I-4 拆 7 per-status builder + e2e pin 路径不变 | `tools/project_guide.py` |
| 6 | refactor | M-3 _classify_unsafe_reason（CJK 优先 + windows_path 收紧）+ I-5 文案落地 + 抽 _SAFE_CLI_PATTERN 共享常量 + unsafe_reason 不变量 | `tools/project_guide.py` |
| 7 | chore | 跑 `python scripts/dev_sync.py` 同步 root → src 镜像；提交镜像 diff | `src/cad_spec_gen/data/tools/*.py` |
| 8 | docs | 记录 v2.25.x cleanup PR + 更新 PROGRESS.md / superpowers/README.md | docs/PROGRESS.md / docs/superpowers/README.md |

每个 refactor commit 把对应 RED 测试转绿；**commit 6 完成后跑全量 `pytest` + `ruff check` + `mypy --strict`**（root tools/ 改完，但 src/ 镜像仍旧版）；commit 7 跑 `dev_sync.py` 同步 src，再跑 `dev_sync.py --check` + 重跑全量 `pytest`（确认 src 镜像也通过 pkg 路径下的 import / 测试入口）。

### 关键 ordering 约束（dry-run 修订）

- **commit 3 必须同时改**：`kpi_patterns.json` 加 `schema_version: 2` + `load_dictionary` 先 pop + 既有 `test_kpi_patterns_json_has_3_kpis_per_implemented_subsystem` 同步过滤 schema_version。**任何遍历 kpi_patterns.json 顶层 key 的既有测试 / 启动校验脚本都要同步**（grep `kpi_patterns.json` 全仓清单）。
- **commit 3 dataclass 单入口约束**：`load_dictionary` 是唯一允许传入"含 schema_version 的 raw kpi_patterns"的入口；**测试不得直接构造 `ProductGoalDictionary(kpi_patterns={"schema_version": 2, ...})` 旁路 pop**（dataclass `__post_init__` 不会 pop）。如要测 dataclass，构造时必须传已 pop 后的纯子系统 dict。
- **commit 3 不得动 `evidence_token=` / `evidence list "token"` 行**（让 commit 4 双改职责清晰）；测试断言 evidence_token 时不要 hardcode 任何特定字符串，留给 commit 4 转绿。
- **commit 5 不得动 `_sanitize_preview_cli`**（让 commit 6 单点改）；I-4 仅拆 builder 不动 sanitize。
- **commit 6 必须保**：`preview_cli_unsafe=True` 字段（既有 test 硬编码断言）；新增 `unsafe_reason` 字段。
- **commit 7 dev_sync 单 commit**：root → src 单向镜像；不要 commit 5/6 顺手 sync（避免 dev_sync diff 与功能 diff 混在一起）。
- **commit 7 reviewer 阅读指引**：commit 7 diff = commit 3 + 4 + 5 + 6 全部改动的 src/ 镜像化，diff 体量大；reviewer **跳过 commit 7 直接看 commit 5/6 的 root tools/** 即可，commit 7 只验证机械同步无逻辑漂移（hash 匹配 hatch_build COPY_DIRS 即过）。
- **CI per-commit 假设**：CI 只对最终 push HEAD 状态校验（不对 push 序列里的中间 commit per-commit 跑测试）。如启用 per-commit CI，commit 2-6 中间状态会红——本 PR 默认 push 后 CI 看 commit 8 状态。

---

## 风险与回滚

| 风险 | 缓解 |
|---|---|
| I-1 schema 硬切误伤回退 | 仅一个产线内部文件 + loader assert；revert commit 3 + commit 1 spec 段即可 |
| I-1 dataclass 二次校验漏改 | commit 3 必同时改 `__init__.py` `load_dictionary`；测试矩阵 `test_kpi_patterns_schema_version_2` 直接走 `load_dictionary` 入口（不绕过 dataclass）|
| I-3 NFKC 长度不等场景 | fallback 到 `match.group(0)`（与现状 normalized 串等价；不优于但不更差）|
| M-3 reason 优先级反例 | `test_classify_unsafe_chinese_with_backslash` 锁定 CJK 优先 |
| dev_sync 反向覆盖 | 所有改动在 `tools/`（root）；commit 7 单 commit 跑 `dev_sync.py` 后比对 src 镜像 |
| commit 2 RED 引发 ImportError 连坐 | 测试编写约束：所有未存在符号用函数内 import；spec 测试矩阵已声明 |
| commit 2 RED 阶段 push 触发 CI 红 | 本地 commit 2-7 顺序跑通后再 push（避免中间状态对外可见，但也不是硬性要求 — 本 PR 流转为"全 commits 一次 push"）|

---

## 边界与不在 scope

- 不扩 19 类子系统 / 6 KPI（v2.27+ 入口前移深化）
- 不接真实 AI adapter（Phase 5 在更高优先级队列）
- 不动 photo3d_* / render_* / SolidWorks
- 不改 CLI flags / PROJECT_GUIDE.json 顶层 schema
- 不改任何渲染或视觉链路（这些是 Phase 4-5）

---

## 工作流约束（项目 CLAUDE.md）

- **TDD 铁律**：commit 2 先全 RED；commit 3-6 每个 refactor 把对应 test 转绿
- **superpowers 工作流**：本 spec 是 brainstorming 产物；下一步 writing-plans skill 出 plan
- **subagent-driven**：本 PR 涉及 6 项 follow-up + 5 BLOCKER 修订（dataclass / fixture / 双改 / e2e pin / shared constant），实际改动量比初估大；plan 阶段酌情局部并行
- **中文输出 + 中文 commit message 描述**
- **dev_sync**：commit 7 单 commit 跑 `python scripts/dev_sync.py && python scripts/dev_sync.py --check`
- **每轮收尾**：commit 8 更新 `docs/PROGRESS.md` + `docs/superpowers/README.md`

---

## 后续（v2.27+）

本 PR 不解决但已在 backlog：
- `kpi_patterns.json` 进一步扩展（多轮渐进 KPI 确认 / 项目向导记忆）
- evidence_token 升级为对象 `{raw, normalized, offsets}`（audit 完整化）
- Phase 5 真实 AI adapter（最高优队列）
