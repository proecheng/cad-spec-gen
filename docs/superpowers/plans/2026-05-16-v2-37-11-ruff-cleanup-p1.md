# v2.37.11 ruff cleanup P1 — F401+F541+F811+E401 154 条清零 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development`（推荐 — 与项目 v2.37.X 连续 20 PR CI 一次过零 hotfix 实证一致）or `superpowers:executing-plans`（inline batch）实施本 plan。Steps 用 `- [ ]` checkbox。

**Goal**：清掉 ruff 421 errors 里 F401+F541+F811+E401 共 154 条 — 双步 `--fix`（default 149 + `--unsafe-fixes` 多 3）+ 剩余加 noqa（估 2）；PR squash merge → tag `v2.37.11`（不 bump pyproject）。

**Architecture**：双步 `ruff --fix` → 全套件 `pytest` 守门 → commit-impl-1（工具产物）→ 剩余 noqa 标注 + RUF100 兜底 → `pytest` → commit-impl-2（人工注解）→ retro → PR → CI 8/8 → squash merge → tag → release。

**Tech Stack**：ruff（项目当前 default ruleset，无 `[tool.ruff]` config）/ pytest / git / gh CLI / Python 3.11+。

**Spec**：`docs/superpowers/specs/2026-05-16-v2-37-11-ruff-cleanup-p1-design.md`（rev 3 / 311 行 / 2 BLOCKER + 5 MINOR cascade 闭合）。

**关键决策汇总**：

| 项 | 决定 |
| --- | --- |
| spec/plan 拓扑 | 3 独立 spec + 3 plan（贴项目 v2.37.X 惯例）；本 plan 仅覆盖 P1 |
| commit 二分 | commit-impl-1（双步 `--fix` 产物） + commit-impl-2（人工 noqa） — §4 AC-4 强制 |
| review 档位 | 轻档（self-review + Task 0 scout grep） — 用户 brainstorming 第 3 问明确授权；spec rev 1→2→3 已抓 2 BLOCKER + 5 MINOR 全闭 |
| TDD | 不适用（pure 工具产物 + 0 语义改动 — spec §7 明示豁免）；回归保护层 = 全套件 pytest + CI 8/8 |
| release | v2.25+ tag-only release（不 bump `pyproject.toml`，停 `2.24.0`） |

**CI 实测 8 jobs**（v2.37.10 PR #89 验证）：mypy-strict + regression + test matrix 2 OS × 3 Python = 8 checks。

**Subagent 工作模式约束**：每 implementer subagent 在 Bash 调 git 必用 `git -C D:/Work/cad-spec-gen <cmd>`（防 cwd 漂移 — memory `feedback_subagent_cwd_drift.md`），且开始任一 git write 前必先 `git -C D:/Work/cad-spec-gen rev-parse --abbrev-ref HEAD` 验证当前在 `chore/v2-37-11-ruff-cleanup-p1` 分支（防污染 main）。

**总 commit 数 = 7**：spec rev 1（已落） + spec rev 2（已落） + spec rev 3（已落） + plan（本 task 落） + impl-1 + impl-2 + retro。

---

## Checkpoint 总览

| Checkpoint | Tasks | 验收门 |
| --- | --- | --- |
| **CP-0** plan 落地 | Task 0（scout + plan commit）| plan 文档 commit；scout 三表登记 |
| **CP-1** 工具 commit | Task 1a, 1b, 2, 3 | commit-impl-1 落地；pytest 3244 PASS |
| **CP-2** noqa commit | Task 4, 5, 6, 7, 8, 9 | commit-impl-2 落地；ruff exit 0 含 RUF100；pytest 3244 PASS |
| **CP-3** self-review + 2-stage subagent review | Task 10 | 2 reviewer subagent 报告 zero CRITICAL |
| **CP-4** retro + PR + release | Task 11, 12 | tag v2.37.11 push；GitHub Release publish |

---

## Task 0: Scout — 实跑校准 + 写 plan §scout 表 + 提交 plan

**Files**:
- 修改：`docs/superpowers/plans/2026-05-16-v2-37-11-ruff-cleanup-p1.md`（本文件 §scout-data 节落地三表）
- 新建：`/tmp/ruff_unsafe.txt`（临时，不进 git）

**Spec 引用**：§5 Task 0 (a)(b)(c) + §5.1 fallback "Task 0 (a) statistics ≠ 154"

- [ ] **Step 0.1：验证当前分支 + working tree clean**

```bash
git -C D:/Work/cad-spec-gen rev-parse --abbrev-ref HEAD
git -C D:/Work/cad-spec-gen status --short
```

Expected：分支 = `chore/v2-37-11-ruff-cleanup-p1`；working tree 仅 2 个原 untracked（`docs/assets/` + `docs/系统全景图_v2.html`），无其他变动。

**Fallback**：分支 ≠ `chore/v2-37-11-ruff-cleanup-p1` → 立刻停。`git checkout chore/v2-37-11-ruff-cleanup-p1`，再 verify。若分支不存在 → spec rev 3 已 commit 在该分支不可能丢，必为环境异常 → ask user。

- [ ] **Step 0.2：跑 ruff 全统计校准 == spec 声明**

```bash
ruff check . --statistics 2>&1 | head -20
```

Expected（与 spec rev 3 §2.1 一致）：

```
128 F401 [-] unused-import
 74 E402 [ ] module-import-not-at-top-of-file
 47 E741 [ ] ambiguous-variable-name
 39 F405 [ ] undefined-local-with-import-star-usage
 39 F841 [ ] unused-variable
 25 F403 [ ] undefined-local-with-import-star
 24 E731 [ ] lambda-assignment
 18 F541 [*] f-string-missing-placeholders
 16 E702 [ ] multiple-statements-on-one-line-semicolon
  7 F811 [*] redefined-while-unused
  3 F821 [ ] undefined-name
  1 E401 [*] multiple-imports-on-one-line
Found 421 errors.
```

P1 4 类合计 = F401 128 + F541 18 + F811 7 + E401 1 = **154**。

**Fallback**：实跑数字 ≠ 154 → spec §5.1 "Task 0 (a) fallback"：(1) 停 plan；(2) 更新 spec 起 rev 4 含真值；(3) 用户 review 后 resume。

- [ ] **Step 0.3：跑 `--show-fixes` 提前识别 unsafe-fixes 3 条 F401**

```bash
ruff check . --select=F401 --fix --unsafe-fixes --show-fixes . 2>&1 | tee /tmp/ruff_unsafe.txt | grep -E "(F401|Safe|Unsafe)" | head -40
```

Expected：输出包含 `Unsafe fix` 标签的行（identifies 3 条 default 不修但 unsafe-fixes 修的 F401 边界 case）。

或如果 ruff 版本 `--show-fixes` 在 stderr：

```bash
ruff check . --select=F401 --fix --unsafe-fixes --show-fixes . 2>&1 > /tmp/ruff_unsafe.txt
grep -E "Unsafe fix" /tmp/ruff_unsafe.txt | head -10
```

**Fallback**：实跑发现 `--show-fixes` 输出格式与本 plan 假设不同 → 直接读完整 `/tmp/ruff_unsafe.txt`，肉眼 enumerate 3 条 `file:line` + 当前 import 字面 + 类型判定。

- [ ] **Step 0.4：本 plan 文档落 §scout-data 表**

在本 plan 文档末尾（"§scout-data — Task 0 实测落地"节）登记 3 表：

**表 1 — Statistics 校准**

| 规则码 | spec 声明 | 实跑 | 状态 |
| --- | --- | --- | --- |
| F401 | 128 | <fill> | <ok/drift> |
| F541 | 18 | <fill> | <ok/drift> |
| F811 | 7 | <fill> | <ok/drift> |
| E401 | 1 | <fill> | <ok/drift> |
| **P1 合计** | 154 | <fill> | <ok/drift> |

**表 2 — `--unsafe-fixes` 3 条 F401 enumerate**

| # | file:line | 当前 import 字面 | 类型判定 |
| --- | --- | --- | --- |
| 1 | <fill> | <fill> | <side-effect / unused / re-export / typing-only> |
| 2 | <fill> | <fill> | <...> |
| 3 | <fill> | <fill> | <...> |

**表 3 — 4 类涉及 file 总数**

| 规则码 | 涉及 file 数 | top 5 files (errors 数倒序) |
| --- | --- | --- |
| F401 | <fill> | <fill> |
| F541 | <fill> | <fill> |
| F811 | <fill> | <fill> |
| E401 | <fill> | <fill> |

3 表填完后下一步 commit。

**Fallback**：表 2 任一条类型判定为 `side-effect` → 后续 Task 1b skip + 那 3 条进 Task 5 noqa（按 spec §5.1 fallback "Task 0 (b) unsafe-fixes 3 条任一是 side-effect import 嫌疑"）。

- [ ] **Step 0.5：commit plan 文档**

```bash
git -C D:/Work/cad-spec-gen add docs/superpowers/plans/2026-05-16-v2-37-11-ruff-cleanup-p1.md
git -C D:/Work/cad-spec-gen commit -m "docs(plan): v2.37.11 ruff cleanup P1 — 13 task 2-5 min granularity plan + Task 0 scout 三表

按 spec rev 3 (311 行) 13 task + §5.1 fallback 拆 2-5 min subagent-driven plan。

Task 0 scout 实测三表登记:
- 表 1 statistics 校准 == 154 (spec 声明)
- 表 2 --unsafe-fixes 3 条 F401 enumerate (file:line + 字面 + 类型判定)
- 表 3 4 类涉及 file 总数 + top 5

总 commit 7: spec rev 1+2+3 + plan + impl-1 + impl-2 + retro
回归保护: 全套件 pytest 3244 PASS + CI 8/8 SUCCESS

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

**Acceptance**：plan 文件 commit；`git -C D:/Work/cad-spec-gen log --oneline` 显示新 commit；§scout-data 三表全 fill（无 `<fill>` 残留）。

---

## Task 1a: Default `--fix` 跑 + diff 检视（149 fix）

**Files**：~90 个 `.py` 文件（实测 Task 0 表 3）

**Spec 引用**：§3.1 改动 1 Step A / §5 Task 1a / §5.1 fallback "Task 1a 实际 fix 数差 spec 预期 > 5"

- [ ] **Step 1a.1：跑 default `--fix`**

```bash
ruff check . --fix --select=F401,F541,F811,E401 . 2>&1 | tail -3
```

Expected：末行 `Fixed 149 errors.`（与 Task 0 表 1 数字一致）。

**Fallback**：实际 fix 数差 spec 预期 > 5 → spec §5.1 fallback "Task 1a 实际 fix 数差 > 5"：(1) 警觉调查（可能 main 在 chore branch 起手后有 new commit 漂移）；(2) `git fetch origin main && git rebase origin/main` 同步；(3) 若仍漂移 → Task 0 fallback (a)。

- [ ] **Step 1a.2：检视 `git diff --stat`**

```bash
git -C D:/Work/cad-spec-gen diff --stat | tail -5
```

Expected：行 `XX files changed, YY insertions(+), ZZZ deletions(-)`；`ZZZ` 应在 100-200 区间（删 import / f 前缀 / 重复 def）。

**Fallback**：diff stat 显示 `pyproject.toml` / `.github/` / `__init__.py` 被改 → 立即调查（spec §3.2 explicit out-of-scope），用 `git checkout -- pyproject.toml .github/` 撤回。

- [ ] **Step 1a.3：抽查一个 fix 例子 sanity check**

```bash
git -C D:/Work/cad-spec-gen diff tests/test_cad_lib_local.py | head -10
```

Expected：显示 `- import os` 之类一行删除，无其他改动。

**Fallback**：抽查发现非预期内容（例如改了 function body）→ 重大风险。立即 `git checkout -- .` reset 全部，回 Task 0 排查 ruff version / config 异常。

---

## Task 1b: `--unsafe-fixes` 跑 + diff 检视（多 3 fix）

**Files**：~3 额外 `.py` 文件（Task 0 表 2 已 enumerate）

**Spec 引用**：§3.1 改动 1 Step B / §5 Task 1b / §5.1 fallback "Task 1b unsafe-fixes 3 条任一是 side-effect import 嫌疑"

- [ ] **Step 1b.0：check Task 0 表 2 — 是否触发 skip fallback**

读 plan §scout-data 表 2 "类型判定" 列。若任一条 = `side-effect` → 触发 fallback：

**Fallback path**：
1. **skip** Task 1b（不跑 `--unsafe-fixes`）
2. Task 3 commit-impl-1 message body 注明 "skipped Step B; reason: 3 F401 hidden 中 N 条 = side-effect import 见 plan §scout-data 表 2"
3. Task 5 决策表多 3 条（归 `side-effect` 类）→ Task 6 加 3 处 noqa
4. 跳过本 Task 剩余 step，直接进 Task 2

若全 3 条 = `unused` / `re-export` / `typing-only` → 继续 Step 1b.1。

- [ ] **Step 1b.1：跑 `--fix --unsafe-fixes`**

```bash
ruff check . --fix --unsafe-fixes --select=F401,F541,F811,E401 . 2>&1 | tail -3
```

Expected：末行 `Fixed 3 errors.`（多修 3 条 F401 hidden）。累计 149 + 3 = 152 fixed，剩 154 - 152 = 2 候选 noqa。

**Fallback**：实际 fix < 3 → 可能 default `--fix` 已修部分（ruff version 差异）；累计 = 152 即可继续。若累计 < 150 → 警觉，Task 0 fallback (a)。

- [ ] **Step 1b.2：检视 Step B 多动的文件**

```bash
git -C D:/Work/cad-spec-gen diff --stat | tail -3
```

Expected：相比 Step 1a.2 多约 3 个文件或 3 行改动。

---

## Task 2: 全套件 pytest 守门（commit-impl-1 前）

**Files**：（不改）

**Spec 引用**：§4 AC-2 / §5 Task 2 / §6 R1 / §5.1 fallback "Task 8 pytest 挂"（同 Task 8）

- [ ] **Step 2.1：跑全套件 pytest**

```bash
pytest tests/ -q --tb=short 2>&1 | tail -10
```

Expected：尾行 `=== 3244 passed, 17 skipped in <T>s ===`（基线 v2.37.10 main@`992c791`）。

**Fallback**（pytest 挂）：spec §5.1 fallback "Task 8 pytest 挂"：
1. `git -C D:/Work/cad-spec-gen diff HEAD` 看 Task 1a+1b 修改集
2. `pytest tests/ -x --tb=short` 定位首挂 test
3. 关联失败 test 与 ruff 删除的 import → 该 import 实际是 side-effect / re-export
4. 把该 import 加回 + `# noqa: F401  # <类型>` 注释 → 重跑 pytest
5. 若 ≥3 处需 fallback → 警觉 Task 1b skip 是否漏了某种 case

- [ ] **Step 2.2：记录 PASS 数字到 plan §scout-data 表 4**

在 plan 文档 §scout-data 加 "表 4 — pytest baseline"：

| 测点 | 数字 | 状态 |
| --- | --- | --- |
| Task 2 (commit-impl-1 前) | <fill> PASS / <fill> SKIP | <ok/drift> |
| Task 8 (commit-impl-2 后) | <pending> | <pending> |

只填 Task 2 行，Task 8 留 pending。

---

## Task 3: commit-impl-1（双步 `--fix` 产物）

**Files**：所有 Task 1a + 1b 修改的 ~90 文件

**Spec 引用**：§4 AC-4 commit 二分 / §5 Task 3 / §6 R4

- [ ] **Step 3.1：staged diff sanity — 不含 noqa 行**

```bash
git -C D:/Work/cad-spec-gen add -A
git -C D:/Work/cad-spec-gen diff --cached | grep -c "+.*# noqa:"
```

Expected：`0`（commit-impl-1 不应含任何新增 noqa 行 — noqa 在 Task 6 单独 commit-impl-2）。

**Fallback**：grep 计数 > 0 → 立即调查（说明 Task 1a 或 1b ruff 自动加了 noqa，超出预期）→ `git reset` 后 review 具体哪行，决定是否进 commit-impl-1。

- [ ] **Step 3.2：staged diff sanity — 不含 out-of-scope 文件**

```bash
git -C D:/Work/cad-spec-gen diff --cached --name-only | grep -E "(pyproject\.toml|\.github/|__init__\.py)"
```

Expected：空（spec §3.2 explicit out-of-scope）。

**Fallback**：grep 输出非空 → `git restore --staged <file>` 撤回这些文件 → 重跑 Step 3.1。

- [ ] **Step 3.3：commit-impl-1**

按 Task 1b 是否 skip 选 commit message：

**Case A** — Task 1b 完整跑（149 + 3 = 152 fixed）：

```bash
git -C D:/Work/cad-spec-gen commit -m "chore(ruff): apply --fix (default + --unsafe-fixes) F401+F541+F811+E401

双步 ruff --fix 工具产物（149 default + 3 --unsafe-fixes = 152 errors fixed）。

- F401 unused-import: 128 条 (125 default fix + 3 --unsafe-fixes hidden case)
- F541 f-string-missing-placeholders: 18 条 (全 [*] safe auto-fix)
- F811 redefined-while-unused: 7 条 (全 tests/ method-internal re-import shadow)
- E401 multiple-imports-on-one-line: 1 条

零语义改动；下一 commit (impl-2) 处理剩 2 条 ruff 不可修案例加 noqa。
回归: 全套件 pytest 3244 PASS / 0 regression.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

**Case B** — Task 1b skip（只 149 default fix）：

```bash
git -C D:/Work/cad-spec-gen commit -m "chore(ruff): apply --fix (default only, --unsafe-fixes skipped) F401+F541+F811+E401

Step B (--unsafe-fixes) skipped: 3 F401 hidden case 经 Task 0 scout enumerate
判定含 side-effect import (见 plan §scout-data 表 2)，不安全；
那 3 条进 commit-impl-2 加 noqa 处理。

default --fix 修 149 errors:
- F401: 125 (排除 3 unsafe hidden)
- F541: 18
- F811: 7
- E401: 1

零语义改动；下一 commit (impl-2) 处理剩 (2 + 3 = 5) 条加 noqa。
回归: 全套件 pytest 3244 PASS / 0 regression.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

**Acceptance**：commit-impl-1 落地；`git log -1 --stat` 显示 ~90 文件改 / 100-200 行删；commit message body 明示 Case A 或 B。

---

## Task 4: 收集剩余条数 N + enumerate 到 §decision 表

**Files**：修改 `docs/superpowers/plans/2026-05-16-v2-37-11-ruff-cleanup-p1.md` § decision 节

**Spec 引用**：§3.1 改动 2 / §5 Task 4 / §5.1 fallback "Task 4 N > 7"

- [ ] **Step 4.1：跑 ruff check 收集剩余**

```bash
ruff check . --select=F401,F541,F811,E401 . 2>&1 | tee /tmp/ruff_residual.txt | tail -5
```

Expected：末行 `Found N errors.`，N 估 2（Case A）或 5（Case B，含 3 skipped unsafe）；±5 容差。

**Fallback**：N > 7 → spec §5.1 "Task 4 N > 7"：(1) 警觉（spec 估测严重偏差）；(2) 逐条 review 是否 ruff 行为漂移 / spec 假设错；(3) 必要时 spec rev 4 校准。

- [ ] **Step 4.2：enumerate N 条到本 plan § decision 表**

读 `/tmp/ruff_residual.txt`，提取所有 file:line:col + 规则码，在 plan 文档 § decision 节加表：

```markdown
## § decision — Task 4 残留 enumerate + Task 5 归类

| # | file:line | 规则码 | 当前 import 字面 / f-string / 行 | 归类（待 Task 5 填）|
| --- | --- | --- | --- | --- |
| 1 | <fill> | <fill> | <fill> | <pending> |
| 2 | <fill> | <fill> | <fill> | <pending> |
| ... | | | | |
```

所有 N 行 file:line 填实。

**Acceptance**：§ decision 表 N 行 enumerate；无 `<fill>` 残留（"归类"列 pending 是预期）。

---

## Task 5: 逐条归类 N 案例

**Files**：修改 plan § decision 表（"归类"列）

**Spec 引用**：§3.1 改动 2 noqa 3 类约定 / §5 Task 5 / §6 R6 method re-import shadow 类

- [ ] **Step 5.1：逐条 review file:line context 决定类**

对 § decision 表每行：

1. `sed -n '<line-5>,<line+5>p' <file>` 看上下文
2. 决定类（4 类，按 spec rev 3 §3.1）：
   - `re-export` — `__init__.py` / 模块公开接口转出口（spec rev 3 实证：项目 19 个 `__init__.py` 全无 F401，此类应空）
   - `side-effect` — 触发 register / monkey-patch / 模块初始化（如 `import params`）
   - `fixture 触发` — 测试模块导入触发 pytest fixture / conftest 钩子
   - `method re-import shadow` — F811 method-internal re-import shadow module-level（rev 3 R6 新增）
   - 罕见自由文案：5-15 字中文不抽象（如"v2.X.Y 占位"非"暂保留"）
3. 填表 "归类" 列

**Acceptance**：表 N 行 "归类" 列全填，无 pending。

- [ ] **Step 5.2：草中文注释文案**

为每条加 plan 表 "中文注释" 新列：

```markdown
| # | file:line | 规则码 | 当前字面 | 归类 | 中文注释（5-15 字）|
```

按归类映射：
- `re-export` → `re-export 给下游`（11 字）
- `side-effect` → `触发模块初始化`（7 字）/ `monkey-patch 注册`（13 字）
- `fixture 触发` → `pytest fixture 触发`（15 字）
- `method re-import shadow` → `method re-import shadow`（不到 15 字英文）/ 改 `测试本地重导入`（7 字）
- 自由：按上下文写

**Acceptance**：表 "中文注释" 列全填，每条 5-15 字。

---

## Task 6: 加 noqa 标注

**Files**：修改 Task 4 enumerate 的 N 文件

**Spec 引用**：§3.1 改动 2 noqa 路径 / §5 Task 6 / §5.1 fallback "Task 6 加 noqa 后 RUF100"

- [ ] **Step 6.1：按 § decision 表逐条加 noqa**

对每行 `file:line:col` + 规则码 + 中文注释：

```bash
# 查看当前行内容
sed -n '<line>p' <file>
```

然后用 Edit 工具或 `sed` 在行末加 `  # noqa: <code>  # <中文注释>`：

```python
# 例子：F541
print(f"Use --json to dump all data as JSON")  # noqa: F541  # 模板占位预留
```

Note：保持原行字符不变，仅加 noqa 后缀；缩进 / 引号 / `(` 等不动。

- [ ] **Step 6.2：grep 验证 noqa 计数 == N**

```bash
git -C D:/Work/cad-spec-gen diff | grep -cE "^\+.*# noqa: (F401|F541|F811|E401)"
```

Expected：N（Task 4 enumerate 行数）。

**Fallback**：计数 ≠ N → 漏加或多加。`git diff` 逐条 check，补漏或删多。

---

## Task 7: ruff check exit 0 + RUF100 兜底

**Files**：（不改）

**Spec 引用**：§4 AC-1 / §5 Task 7 / §5.1 fallback "Task 6 RUF100"

- [ ] **Step 7.1：跑 ruff check + RUF100**

```bash
ruff check . --select=F401,F541,F811,E401 --extend-select=RUF100 . 2>&1 | tail -3
```

Expected：`All checks passed!` 或 `Found 0 errors.`，exit code 0。

**Fallback**（RUF100 报）：spec §5.1 "Task 6 RUF100"：
1. ruff 直接指出哪行 noqa code 拼错（如 `# noqa: F540` 错为 F541）
2. 回 Task 5 § decision 表更正该行 "中文注释" 或 "规则码"
3. 重跑 Task 6.1 修该行 noqa
4. 重跑本 Step 7.1，最多 retry 2 次

**Fallback**（exit ≠ 0 但非 RUF100）：说明 Task 4 enumerate 漏了某条 → 重跑 Task 4.1 看 `/tmp/ruff_residual.txt`，补 Task 5+6。

---

## Task 8: 全套件 pytest 守门（commit-impl-2 前）

**Files**：（不改）

**Spec 引用**：§4 AC-2 / §5 Task 8 / §5.1 fallback "Task 8 pytest 挂"

- [ ] **Step 8.1：跑全套件 pytest**

```bash
pytest tests/ -q --tb=short 2>&1 | tail -10
```

Expected：`=== 3244 passed, 17 skipped in <T>s ===`（与 Task 2 相同，noqa 加完后零行为差）。

**Fallback**（pytest 挂）：spec §5.1 fallback "Task 8 pytest 挂"：
1. `git diff HEAD` 看 noqa 加的位置
2. 若 noqa 加错文件 / 行（极少）→ Edit 修
3. 若 noqa 加对但仍挂 → Task 2 已 PASS，问题在 Task 1a/1b 工具改动 → `git reset --soft HEAD~1` 撤回 commit-impl-1 → 重跑 Task 1a/1b 排查

- [ ] **Step 8.2：更新 plan §scout-data 表 4 Task 8 行**

读 §scout-data 表 4，填 "Task 8 (commit-impl-2 后)" 行 = `<N> PASS / <M> SKIP`。

**Acceptance**：表 4 两行全填；两行数字一致。

---

## Task 9: commit-impl-2（人工 noqa 标注）

**Files**：所有 Task 6 改动的 ~N 文件（估 2-5）

**Spec 引用**：§4 AC-4 / §5 Task 9 / §6 R4

- [ ] **Step 9.1：staged diff sanity — diff 全为 `+ # noqa:` 行**

```bash
git -C D:/Work/cad-spec-gen add -A
git -C D:/Work/cad-spec-gen diff --cached | grep -E "^[+-]" | grep -v "^+++" | grep -v "^---"
```

Expected：所有 `+` 行都含 `# noqa:`；无 `-` 行（不删任何代码）。

**Fallback**：diff 含 `-` 行或 `+` 行非 noqa → 立即调查，`git restore --staged <file>` 撤回，回 Task 6 重做。

- [ ] **Step 9.2：commit-impl-2**

```bash
git -C D:/Work/cad-spec-gen commit -m "chore(ruff): add noqa for ruff-not-fixable cases (N=<actual>)

ruff 双步 --fix 跑完后剩余 N 条 case 加 # noqa: <code>  # <5-15 字中文> 注释。

分类:
- re-export 给下游 (F401): <count>
- 触发模块初始化 / monkey-patch (F401): <count>
- pytest fixture 触发 (F401): <count>
- method re-import shadow (F811): <count>
- 罕见自由文案: <count>

AC-1: ruff check --select=F401,F541,F811,E401 --extend-select=RUF100 . exit 0
AC-3: noqa 5-15 字中文注释全合规
零语义改动，diff 全为 + # noqa: 行。
回归: 全套件 pytest 3244 PASS / 0 regression.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

替换 `<actual>` 与 `<count>` 为实际数字（Task 5 § decision 表统计）。

**Acceptance**：commit-impl-2 落地；`git log -1 --stat` 显示 ~N 文件 / ≤N+5 行；message body 含 N 与归类分布。

---

## ⚓ CP-3: Self-review + 2-stage subagent review

完成 commit-impl-2 后**暂停**派发新 implementer subagent，主 agent 跑：

### Task 10: self-review + Task 0 scout 复跑 + 2 reviewer subagent

**Files**：（不改 — 验证性）

**Spec 引用**：§5 Task 10 / §7 review 与流程

- [ ] **Step 10.1：自跑 AC-1 ~ AC-6 6 条**

```bash
# AC-1
ruff check . --select=F401,F541,F811,E401 --extend-select=RUF100 . 2>&1 | tail -3
# AC-2 (Task 8 已跑，复看 §scout-data 表 4)
# AC-3 (人工 review noqa 文案 grep)
git -C D:/Work/cad-spec-gen diff main...HEAD | grep -E "^\+.*# noqa:" | head
# AC-4 (commit 二分检视)
git -C D:/Work/cad-spec-gen log main..HEAD --oneline
# AC-5 (CI Task 12 跑后再验)
# AC-6 (8 类未触动 statistics 不变)
ruff check . --statistics 2>&1 | head -15
```

Expected：
- AC-1：exit 0
- AC-2：表 4 两行 3244 PASS 一致
- AC-3：每条 noqa 后跟 `# <5-15 字中文>`
- AC-4：commit 数 = 6（rev 1+2+3 + plan + impl-1 + impl-2，retro 在 Task 11 加）
- AC-5：留 Task 12
- AC-6：E402 74 / E741 47 / F405 39 / F841 39 / F403 25 / E731 24 / E702 16 / F821 3 — 全不变

**Fallback**：任一 AC 不过 → 回对应 Task 修。

- [ ] **Step 10.2：派发 spec compliance reviewer subagent**

主 agent 用 Agent 工具派发 `general-purpose` subagent（fresh context），prompt 大意：

```
你是 fresh subagent，任务 = review feat branch chore/v2-37-11-ruff-cleanup-p1 
对照 spec docs/superpowers/specs/2026-05-16-v2-37-11-ruff-cleanup-p1-design.md (rev 3)
是否合规。

具体检查:
1. spec §4 AC-1 ~ AC-6 6 条是否全过 (用 git diff main...HEAD + 命令验证)
2. spec §3.1 改动 1 双步法 + 改动 2 noqa 路径是否实施
3. spec §3.2 explicit out-of-scope 8 项是否 全部未触
4. spec §5.1 fallback 路径表 7 行任一是否被触发；若触发是否 plan/retro 登记
5. commit 二分 (§4 AC-4)：impl-1 全工具产物 / impl-2 全 +noqa 行
6. §6 R1~R7 风险缓解措施是否生效

输出格式: zero CRITICAL / N MAJOR / M MINOR 摘要 + 每条引用 spec 节号 + 命令证据
```

**Acceptance**：subagent 返回 zero CRITICAL；任何 MAJOR 必须修后 resume（按 spec §5.1 对应 fallback）；MINOR 可登记进 retro §12 follow-up。

- [ ] **Step 10.3：派发 code quality reviewer subagent**

主 agent 另派发 fresh subagent：

```
你是 fresh subagent，任务 = review feat branch 代码质量。

具体检查:
1. commit-impl-1 diff 抽 5 文件看 ruff --fix 产物是否合理（无误删函数 / 函数体未动）
2. commit-impl-2 diff 全文件看 noqa 注释中文文案是否符合 spec §3.1 3 类约定
3. 中文文案是否 5-15 字 + 不抽象 (非"暂保留" / "等修")
4. 是否有 # noqa 后跟 code 错位 (RUF100 自带兜底但人工再 verify)
5. 是否有"顺手 cleanup" out-of-scope 漂移（spec §3.2 第 8 项 "不做顺手 cleanup"）

输出格式: zero CRITICAL / N MAJOR / M MINOR + 每条文件+行号引用
```

**Acceptance**：subagent 返回 zero CRITICAL；任何 MAJOR 修后 resume；MINOR 登记 retro。

---

## Task 11: Retro doc 写 + commit

**Files**：新建 `docs/superpowers/reports/2026-05-16-v2-37-11-ruff-cleanup-p1-retro.md`

**Spec 引用**：§5 Task 11 / §11 §12 follow-up

- [ ] **Step 11.1：写 retro 文档骨架**

按项目惯例（参考 `docs/superpowers/reports/2026-05-16-v2-37-10-rebrand-path-real-retry-retro.md`）写：

```markdown
# v2.37.11 — ruff cleanup P1 (F401+F541+F811+E401 154 条) Retro

> **PR**: #<num>（Task 12 后填）  
> **Merged**: <date> main@<sha>  
> **Tag**: v2.37.11  
> **Release**: https://github.com/proecheng/cad-spec-gen/releases/tag/v2.37.11  
> **CI**: 8/8 SUCCESS（mypy-strict / regression / test 2×3 matrix）  
> **回归**: 全套件 3244 PASS / 17 SKIP / 0 regression  

## 1. 摘要
P1 ruff cleanup 完工 — 154 errors 清零（149 default --fix + 3 --unsafe-fixes + <N> noqa 标注）；commit 二分（impl-1 工具 + impl-2 人工）；零语义改动；全套件 0 regression。

## 2. 实施数据真值（Task 0 + Task 4 + Task 8 实测）

### 2.1 statistics 校准
（从 plan §scout-data 表 1 复制）

### 2.2 unsafe-fixes 3 条 enumerate
（从 plan §scout-data 表 2 复制 + 备注是否 skip）

### 2.3 4 类 file 数 + top 5
（从 plan §scout-data 表 3 复制）

### 2.4 pytest baseline
（从 plan §scout-data 表 4 复制）

### 2.5 § decision 表 N 条
（从 plan § decision 表复制 + 归类分布饼图）

## 3. 闭合 §11 follow-up
- v2.37.11 闭合 "360+ ruff cleanup" follow-up (P1 layer)
- 仍 open: P2 (F841+F405+F403+E731+E702 143 条) / P3 (E402+E741+F821 124 条 + [tool.ruff] config 锁 + CI ruff-strict job)

## 4. spec rev 1 → 2 → 3 cascade lesson

### Layer 1 (rev 1 → rev 2) — cynical re-read + code-spec 对照
1 BLOCKER + 2 MINOR — F401 标号误解 / noqa 估测偏 / 风险假设
教训: ...

### Layer 2 (rev 2 → rev 3) — edge-case hunter + 闭环
1 BLOCKER + 3 MINOR — R6 描述错 / unsafe-fix 不可分 / 5 处 fallback / RUF100 兜底
教训: ...

## 5. P2 入口 baseline（spec §6 R5 要求）

P1 完工后跑 `ruff check . --statistics`：

| 规则码 | P1 前 | P1 后 | Δ |
| --- | --- | --- | --- |
| F841 | 39 | <fill> | <fill> |
| F405 | 39 | <fill> | <fill> |
| F403 | 25 | <fill> | <fill> |
| E731 | 24 | <fill> | <fill> |
| E702 | 16 | <fill> | <fill> |

P2 spec 起手时 Task 0 scout 校准这些数字。

## 6. 项目纪录
- subagent-driven 完成度: <fill>/13 task 一次过 / <fill> task 需 fallback
- spec rev 数: 3
- 累计抓 BLOCKER + MINOR: 2 + 5 = 7 (全 inline fix)
- CI 8/8 一次过零 hotfix: 连续第 21 个 PR (实证)

## 7. §12 self follow-up 落地（spec rev 3 §11 §12 f1-f6）
- f1: ✅ ruff statistics 标号误解 lesson 进本 retro §4
- f2: ✅ Task 0 scout 三步模板可 P2/P3 复用，下游 plan 复制
- f3: ✅ spec R6 描述错 lesson "spec 写 pattern 描述前必 grep 实证 file:line"
- f4: ✅ ruff --diff vs --show-fixes 实证（unsafe-fixes 分离需 --show-fixes）
- f5: ✅ Fallback 路径表 (§5.1) 模板可推广 cleanup spec
- f6: ✅ AC 验收命令应 explicit 加 --extend-select=RUF100 兜底

## Related
- [[project_v2_37_10_done]] — v2.37.10 retro "360+ ruff cleanup" follow-up 出处
- [[feedback_historical_debt_isolation]] — cleanup 独立 PR 不混功能
- [[feedback_spec_writer_self_drift]] — spec ≥100 行 4 层审查实证（本 PR 2 layer 抓 2 BLOCKER + 5 MINOR）
- spec: `docs/superpowers/specs/2026-05-16-v2-37-11-ruff-cleanup-p1-design.md` (rev 3, 311 行)
- plan: `docs/superpowers/plans/2026-05-16-v2-37-11-ruff-cleanup-p1.md` (本 task 出)
```

- [ ] **Step 11.2：填实所有 `<fill>` 标记**

逐项查 plan §scout-data 数据 / git log / ruff statistics 实跑填实。

- [ ] **Step 11.3：commit retro**

```bash
git -C D:/Work/cad-spec-gen add docs/superpowers/reports/2026-05-16-v2-37-11-ruff-cleanup-p1-retro.md
git -C D:/Work/cad-spec-gen commit -m "docs(retro): v2.37.11 ruff cleanup P1 — 154 errors 清零 retro + §11 §12 f1-f6 闭合

P1 完工 retro:
- statistics 校准 / unsafe-fixes 3 条 enumerate / pytest baseline / § decision 表 N 条 全实测落地
- spec rev 1→2→3 cascade 教训沉淀 (2 BLOCKER + 5 MINOR 全闭)
- P2 入口 baseline 表 (§6 R5)
- §12 f1-f6 全闭合 (6 项 lesson)

回归: pytest 3244 PASS / CI 8/8 SUCCESS / 0 regression

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

**Acceptance**：retro 文档落地；6 个 `<fill>` 全填；commit log 显示。

---

## ⚓ CP-4: PR + CI + Merge + Tag + Release

### Task 12: PR 开 / 监 CI / squash merge / tag / release

**Files**：（不改本地 — GitHub 操作）

**Spec 引用**：§5 Task 12 / §8 文档与归档 / §5.1 fallback "Task 12 CI 8/8 fail"

- [ ] **Step 12.1：push 分支到 origin**

```bash
git -C D:/Work/cad-spec-gen push -u origin chore/v2-37-11-ruff-cleanup-p1
```

Expected：push 成功；`Branch 'chore/v2-37-11-ruff-cleanup-p1' set up to track 'origin/chore/v2-37-11-ruff-cleanup-p1'`.

- [ ] **Step 12.2：open PR**

```bash
gh pr create \
  --title "chore(ruff): v2.37.11 — ruff cleanup P1 safe --fix 4 类清零（154 errors → 0）" \
  --body "$(cat <<'EOF'
## 摘要

清掉 ruff 421 errors 里 **F401+F541+F811+E401 共 154 条**（双步 `--fix` + noqa 标注）；不动产品代码 / 不动 `pyproject.toml` / 不动 CI 配置（P2/P3 才动）。

## 验收 AC（全过）

- ✅ AC-1: `ruff check --select=F401,F541,F811,E401 --extend-select=RUF100 .` exit 0（含 RUF100 兜底）
- ✅ AC-2: pytest 3244 PASS / 17 SKIP / 0 regression
- ✅ AC-3: noqa 5-15 字中文注释规范（3 类约定 + 罕见自由文案）
- ✅ AC-4: commit 二分（impl-1 双步 --fix 工具产物 + impl-2 全 + # noqa: 行）
- ⏳ AC-5: CI 8/8 SUCCESS（待 CI）
- ✅ AC-6: 8 类未触动规则 statistics 不变（E402 74 / E741 47 / F405 39 / F841 39 / F403 25 / E731 24 / E702 16 / F821 3）

## 关键事实

- 双步真值：default `--fix` 修 149 + `--unsafe-fixes` 多 3 + 剩 N 加 noqa（实测见 retro §2）
- spec rev: rev 1 → rev 2 → rev 3（cynical + edge-case 2 layer 审查抓 2 BLOCKER + 5 MINOR 全闭）
- 零语义改动：commit-impl-1 全工具产物 / commit-impl-2 全注释；pytest 0 regression
- P2/P3 推迟（spec §3.2 explicit out-of-scope）：F841/F405/F403/E731/E702 143 条 + E402/E741/F821 124 条 + `[tool.ruff]` config 锁

## 文档

- spec: `docs/superpowers/specs/2026-05-16-v2-37-11-ruff-cleanup-p1-design.md`（rev 3 / 311 行）
- plan: `docs/superpowers/plans/2026-05-16-v2-37-11-ruff-cleanup-p1.md`
- retro: `docs/superpowers/reports/2026-05-16-v2-37-11-ruff-cleanup-p1-retro.md`

## §11 follow-up

- ✅ closed: "360+ ruff cleanup" P1 layer
- ⏳ open: P2 (143 条) / P3 (124 条 + config 锁) / §11-N7/N10/N11/N12 / §12 f4 不变

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

**Acceptance**：`gh pr create` 输出 `https://github.com/proecheng/cad-spec-gen/pull/<num>`；记下 PR 号填进 retro Task 11 头部。

- [ ] **Step 12.3：监 CI 8/8**

```bash
gh pr checks <PR-num> --watch
```

或：

```bash
gh pr view <PR-num> --json statusCheckRollup --jq '.statusCheckRollup[] | "\(.name) \(.status) \(.conclusion)"'
```

Expected：8 checks 全 `pass`（mypy-strict + regression + test 6 matrix = ubuntu-latest 3.10/3.11/3.12 + windows-latest 3.10/3.11/3.12）。

**Fallback**（CI 任一 fail）：spec §5.1 fallback "Task 12 CI 8/8 任一 job fail"：
1. `gh run view <run-id> --log-failed` 看根因
2. 若 import-time error → 加 noqa 给某 import；若 pytest assert fail → 调查具体 test
3. `git commit --amend` 或新 commit 修 → `git push` → 重监
4. 最多 retry 3 次；若 ≥3 次仍挂 → ask user 决定是否 spec rev 4 重思路

- [ ] **Step 12.4：squash merge**

```bash
gh pr merge <PR-num> --squash --delete-branch
```

Expected：`Merged pull request #<num> ...`；本地分支自动删；origin 分支删。

- [ ] **Step 12.5：pull main 到本地最新**

```bash
git -C D:/Work/cad-spec-gen checkout main
git -C D:/Work/cad-spec-gen pull --ff-only origin main
git -C D:/Work/cad-spec-gen log --oneline -3
```

Expected：HEAD = squash merge commit；本地 main 与 origin/main 同步。

- [ ] **Step 12.6：tag v2.37.11 + push**

```bash
git -C D:/Work/cad-spec-gen tag -a v2.37.11 -m "v2.37.11 — ruff cleanup P1 (F401+F541+F811+E401 154 errors → 0)"
git -C D:/Work/cad-spec-gen push origin v2.37.11
```

Expected：tag push 成功；`git tag --list "v2.37.*"` 含 `v2.37.11`。

- [ ] **Step 12.7：GitHub Release publish**

```bash
gh release create v2.37.11 \
  --title "v2.37.11 — ruff cleanup P1 (154 errors → 0)" \
  --notes "$(cat <<'EOF'
## v2.37.11 — ruff cleanup P1

清掉 ruff 421 errors 里 **F401+F541+F811+E401 共 154 条**。零语义改动，纯历史债 cleanup。

### 改动

- ruff `--fix` 双步（default 149 + `--unsafe-fixes` 3）+ 剩余 N 条加 `# noqa: <code>  # <5-15 字中文>` 标注
- commit 二分：`commit-impl-1` 全工具产物 / `commit-impl-2` 全 noqa 注释
- AC-1 ruff exit 0 + `RUF100` 兜底（防 noqa 拼错）
- AC-2 全套件 3244 PASS / 0 regression
- CI 8/8 SUCCESS（连续第 21 个 PR 一次过零 hotfix）

### 不动（推迟 P2 / P3）

- P2: F841+F405+F403+E731+E702 ≈ 143 条
- P3: E402+E741+F821 ≈ 124 条 + `[tool.ruff]` config 锁 + CI ruff-strict job
- `pyproject.toml` 版本号停留 `2.24.0`（v2.25+ tag-only release 惯例）

### 文档

- spec: `docs/superpowers/specs/2026-05-16-v2-37-11-ruff-cleanup-p1-design.md` (rev 3, 311 行)
- plan: `docs/superpowers/plans/2026-05-16-v2-37-11-ruff-cleanup-p1.md`
- retro: `docs/superpowers/reports/2026-05-16-v2-37-11-ruff-cleanup-p1-retro.md`

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

**Acceptance**：`gh release view v2.37.11` 显示发布信息；release URL 留给 retro Task 11 头部填。

- [ ] **Step 12.8：补 retro PR# + merge sha + release URL**

回 Task 11 retro 文档，把：
- PR#: 填实际 PR number
- Merged: 填日期 + main@<merge sha>
- Release URL: 填 v2.37.11 release URL

```bash
git -C D:/Work/cad-spec-gen add docs/superpowers/reports/2026-05-16-v2-37-11-ruff-cleanup-p1-retro.md
git -C D:/Work/cad-spec-gen commit -m "docs(retro): v2.37.11 retro — fill PR# + merge sha + release URL"
git -C D:/Work/cad-spec-gen push origin main
```

**Acceptance**：retro 文档头部 3 占位填实；push 成功；`gh release view v2.37.11` 与 retro 一致。

---

## § scout-data — Task 0 实测落地（Task 0 填）

**表 1 — Statistics 校准**

| 规则码 | spec 声明 | 实跑 | 状态 |
| --- | --- | --- | --- |
| F401 | 128 | 128 | ok |
| F541 | 18 | 18 | ok |
| F811 | 7 | 7 | ok |
| E401 | 1 | 1 | ok |
| **P1 合计** | 154 | 154 | ok |

> 实测命令：`ruff check . --statistics 2>&1 | head -20`；总计 `Found 421 errors.`，P1 4 类（F401/F541/F811/E401）合计 = 128+18+7+1 = 154，与 spec rev 3 §2.1 完全一致。

**表 2 — `--unsafe-fixes` 3 条 F401 enumerate**

| # | file:line | 当前 import 字面 | 类型判定 |
| --- | --- | --- | --- |
| 1 | `cad/end_effector/render_config.py:566:16` | `import bpy`（在 `try: import bpy / except ImportError: return` 块内） | **side-effect**（availability probe — 删 import 后 except ImportError 永不触发，等价改变逻辑） |
| 2 | `cad/lifting_platform/render_config.py:566:16` | `import bpy`（同上 probe 块内） | **side-effect**（同 #1） |
| 3 | `render_config.py:566:16` | `import bpy`（同上 probe 块内） | **side-effect**（同 #1） |

> ⚠️ **3 条全部 = side-effect** → 按 plan §Step 0.4 fallback 与 Task 1b skip 规则触发条件：本表任一条 = side-effect 则在产出报告中明确标记，主 agent 决定是否触发 Task 1b skip fallback（spec §3.1.2 + plan Task 3 line 211-215 的 unsafe-fixes 全部 skip 路径）。
>
> 实测命令：
> ```
> ruff check . --select=F401 --fix --unsafe-fixes --show-fixes 2>&1 > /tmp/ruff_show_fixes_unsafe.txt
> git -C D:/Work/cad-spec-gen checkout -- .   # 立即还原 90 个 tracked file
> ```
> 输出尾部 `Found 128 errors (125 fixed, 3 remaining)` → 3 条 remaining 即 ruff 不肯自动修（即使 `--unsafe-fixes`）的 unsafe F401。三条均为 Blender 可用性 probe pattern：
> ```python
> try:
>     import bpy
> except ImportError:
>     return  # Not running in Blender — skip silently
> ```
> 删除 `import bpy` 后 `except ImportError` 永远无法捕获，函数 silent return 行为丢失。

**表 3 — 4 类涉及 file 总数 + top 5**

| 规则码 | 涉及 file 数 | top 5 files (errors 数倒序) |
| --- | --- | --- |
| F401 | 93 | `cad/lifting_platform/draw_top_plate.py` (8) / `cad/lifting_platform/draw_moving_plate.py` (5) / `tests/test_section_walker_unit.py` (4) / `tests/test_sw_preflight_user_provided_provenance.py` (3) / `tests/test_sw_preflight_report.py` (3) |
| F541 | 8 | `cad_spec_reviewer.py` (5) / `prompt_data_builder.py` (4) / `tools/hybrid_render/validate_config.py` (3) / `cad_spec_gen.py` (2) / `tools/synthesize_demo_step_files.py` (1) |
| F811 | 2 | `tests/test_sw_config_broker_e2e.py` (5) / `tests/test_sw_config_broker_integration.py` (2) |
| E401 | 1 | `tests/test_section_walker_real_docs.py` (1) |

> 实测命令（per rule）：
> ```
> ruff check . --select=<RULE> --output-format=concise 2>&1 \
>   | grep "\.py:" | awk -F: '{print $1}' | sort -u | wc -l        # 涉及 file 数
> ruff check . --select=<RULE> --output-format=concise 2>&1 \
>   | grep "\.py:" | awk -F: '{print $1}' | sort | uniq -c | sort -rn | head -5   # top 5
> ```

**表 4 — pytest baseline**

| 测点 | 数字 | 状态 |
| --- | --- | --- |
| Task 2 (commit-impl-1 前) | <fill> PASS / <fill> SKIP | <ok/drift> |
| Task 8 (commit-impl-2 后) | <fill> PASS / <fill> SKIP | <fill> |

---

## § decision — Task 4 残留 enumerate + Task 5 归类（Task 4 + 5 填）

| # | file:line | 规则码 | 当前字面 | 归类 | 中文注释（5-15 字）|
| --- | --- | --- | --- | --- | --- |
| 1 | <fill> | <fill> | <fill> | <pending Task 5> | <pending Task 5> |
| ... | | | | | |

---

## Plan Self-Review（writing-plans skill Step 7 inline）

**1. Spec coverage** — 对 spec rev 3 §3.1 改动 1 双步法 / §3.1 改动 2 noqa 路径 / §4 AC-1~AC-6 / §5 Task 0-12 / §5.1 fallback 7 行 / §6 R6 R7 / §7 review 档位 / §8 文档归档 / §10 self-review / §11 §12 f1-f6 全节，每节都映射到 plan Task：

| spec 节 | plan Task |
| --- | --- |
| §3.1 改动 1 Step A | Task 1a |
| §3.1 改动 1 Step B | Task 1b |
| §3.1 改动 2 noqa | Task 4-6 |
| §4 AC-1 | Task 7 + 10.1 |
| §4 AC-2 | Task 2 + 8 |
| §4 AC-3 | Task 5-6 + 10.1 |
| §4 AC-4 | Task 3 + 9 + 10.1 |
| §4 AC-5 | Task 12.3 |
| §4 AC-6 | Task 10.1 |
| §5.1 fallback Task 0 (a) | Task 0.2 Fallback |
| §5.1 fallback Task 0 (b) | Task 0.4 Fallback + Task 1b.0 |
| §5.1 fallback Task 1a > 5 | Task 1a.1 Fallback |
| §5.1 fallback Task 1b unsafe 嫌疑 | Task 1b.0 |
| §5.1 fallback Task 4 N > 7 | Task 4.1 Fallback |
| §5.1 fallback Task 6 RUF100 | Task 7.1 Fallback |
| §5.1 fallback Task 8 pytest 挂 | Task 2.1 Fallback + Task 8.1 Fallback |
| §5.1 fallback Task 12 CI fail | Task 12.3 Fallback |
| §6 R6 method re-import | Task 5.1 4 类 + R6 noqa 文案 |
| §6 R7 unsafe-fixes 3 条 | Task 0.3-0.4 + Task 1b.0 |
| §7 轻档 review | Task 10 self-review only（无 5 角色 adversarial）|
| §7 subagent-driven | Task 10.2 + 10.3 + Task 11 combined |
| §8 文档归档 | Task 11 retro + Task 12.7 release notes |
| §10 self-review | Task 10.1 |
| §11 §12 f1-f6 | Task 11 retro §7 |

✅ spec 全节覆盖。

**2. Placeholder scan** — 全 plan 唯一 `<fill>` 在 §scout-data 与 § decision 节，这些是 Task 0 / Task 4 实跑数据落地点，**不算 placeholder**。其他 `<num>` `<sha>` `<date>` 是 Task 12 实测后填，runtime 数据，非 plan 设计 placeholder。无"TBD / TODO / implement later / Add appropriate error handling / Write tests for above"。

**3. Type consistency** — 命令字面在 Task 1a / 1b / 7 三处一致（`ruff check . --select=F401,F541,F811,E401`）；commit message 模板 Case A / B 两 variant 数字一致；AC-1 在 Task 7 / 10.1 两处一致（含 `--extend-select=RUF100`）。

✅ plan self-review 通过。

---

## 项目纪录（统计 — 完工后回填）

| 指标 | 目标 | 实测（完工填）|
| --- | --- | --- |
| subagent-driven 一次过 task 数 | ≥10/13 | <fill> |
| 触发 fallback 数 | ≤3 | <fill> |
| pytest baseline 保持 | 3244 PASS | <fill> |
| CI 一次过 | 8/8 | <fill> |
| 连续 PR CI 一次过零 hotfix | 21（v2.37.11 是第 21）| <fill> |
