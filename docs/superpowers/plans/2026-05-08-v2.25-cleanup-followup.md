# v2.25.x cleanup PR — §11 follow-up 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在单 PR 内闭一笔 v2.25.0 PR #59 review 阶段登记的 6 项技术债（§11.I-1/I-2/I-3/I-4/I-5/M-3）。

**Architecture:** 按 spec rev 4 (`docs/superpowers/specs/2026-05-08-v2.25-cleanup-design.md`) 的 commit 顺序：spec → 20 RED 测试 → I-1 schema v2 → I-3 evidence_token 双改 → I-4 拆 7 builder → M-3+I-5 unsafe_reason 分类 → dev_sync 镜像 → PROGRESS 收尾。canonical 在 `tools/`（root），`src/cad_spec_gen/data/tools/` 是构建副本通过 `scripts/dev_sync.py` 单向同步。

**Tech Stack:** Python 3.11 + pytest + ruff + mypy --strict（项目 CLAUDE.md 强制）。

**前置状态:**
- 分支：`feat/v2.25-cleanup-followup`（已建立，4 commits ahead of main）
- spec：rev 4 已 commit `0e5869a`
- 基线：main @ `99c0e93`

---

## File Structure

| 文件 | 责任 | 改动幅度 |
|---|---|---|
| `tools/project_guide_dict/kpi_patterns.json` | KPI regex 词典 | schema v1 → v2（regex string[] → [{pattern, factor}]）|
| `tools/project_guide_dict/__init__.py` | dataclass + load_dictionary | load_dictionary 加 schema_version pop + 校验 |
| `tools/product_goal_parser.py` | KPI 抽取主体 | 删 unit_normalize 索引代码；加 _raw_token_for_match；evidence_token 双改 |
| `tools/project_guide.py` | 路由 + sanitize | 加共享常量；加 _classify_unsafe_reason；加 7 builder；重写 _derive_goal_status_and_next_action；改写 _sanitize_preview_cli |
| `tests/test_product_goal_parser.py` | parser 测试 | +6 新测试 + 3 既有测试同步 |
| `tests/test_project_goal_guide.py` | guide 测试 | +14 新测试 |
| `docs/superpowers/specs/2026-05-07-product-goal-entry-design.md` | v2.25.0 spec | line 15 / 316 / 352 三处文档收敛 |
| `src/cad_spec_gen/data/tools/*.py` | 构建副本 | dev_sync 自动同步（commit 7 单 commit）|
| `docs/PROGRESS.md` | 项目看板 | 加 v2.25.x cleanup 一行 |
| `docs/superpowers/README.md` | superpowers 看板 | 同步 |

---

## Task 0: Pre-flight 验证

**Goal:** 在动手前再次 grep 全仓 + 实测，确认 spec 引用的代码位置 / 行号 / 既有测试与 spec rev 4 描述对齐，避免 plan 实施过程中再次发现"事实漂移"。

- [ ] **Step 0.1: 验证 git 分支与基线状态**

```bash
git branch --show-current
git log --oneline -5
```

Expected：
```
feat/v2.25-cleanup-followup
0e5869a docs(spec): v2.25.x cleanup spec rev 4 — 经第三轮常见疏漏审查后修订
b7158aa docs(spec): v2.25.x cleanup spec rev 3 — 经第二轮一致性/边界审查后修订
156762f docs(spec): v2.25.x cleanup spec rev 2 — 经 3 reviewer 对抗审查后修订
bd33a16 docs(spec): 增加 v2.25.x cleanup PR 设计文档
9dc08c3 docs(progress): 记录 v2.25.0 + v2.26.0 双 release 完成 + session 交接
```

- [ ] **Step 0.2: 实证 200 厘米现行行为（spec rev 4 line 70 引用）**

```bash
python -c "import sys; sys.path.insert(0,'tools'); from product_goal_parser import parse_product_goal; print(parse_product_goal(text='升降平台 升程 200 厘米').kpis['stroke_mm'].value)"
```

Expected: `2000.0`（确认 I-1 不是修 bug 而是清死代码）

- [ ] **Step 0.3: 验证既有 dataclass 字段名**

```bash
grep -n "subsystem_keywords\|kpi_patterns\b" tools/project_guide_dict/__init__.py
```

Expected：line 17 `subsystem_keywords: dict[...]` + line 18 `kpi_patterns: dict[...]`（确认实际字段名是 `subsystem_keywords` 不是 `subsystem_synonyms`）

- [ ] **Step 0.4: 验证 spec 引用的既有测试位置**

```bash
grep -n "test_kpi_patterns_json_has_3_kpis_per_implemented_subsystem\|test_load_dictionary_raises_on_missing_file\|test_load_dictionary_raises_on_implemented_subsystem_missing_kpis" tests/test_product_goal_parser.py
```

Expected：line 35 / line 62 / line 71（与 spec rev 4 引用一致）

- [ ] **Step 0.5: 跑全量回归确认基线绿**

```bash
pytest tests/ -q
```

Expected：~2470 passed, 0 failed

如果 step 0.1-0.5 任一不符，**停止实施**，回去更新 spec，不要带漂移进 plan。

---

## Task 1: Commit 1 — spec docs（I-2 + I-5）

**Goal:** spec rev 4 已 commit，本 task 在原 v2.25.0 spec 文件 `2026-05-07-product-goal-entry-design.md` 落地 I-2（line 316 软窗口→硬窗口）+ I-5（line 15 / 352 文案模板按 reason 分类）。

**Files:**
- Modify: `docs/superpowers/specs/2026-05-07-product-goal-entry-design.md`

- [ ] **Step 1.1: I-2 — 改 line 316 软窗口表述**

打开 `docs/superpowers/specs/2026-05-07-product-goal-entry-design.md`，找到 line 316（在"距离度量"小节内），改前后：

**改前**（line 316）：
```
- ±20 char 是软窗口；超出窗口仍允许，但 `parser_evidence` 标 `weak`
```

**改后**：
```
- ±20 char 是硬窗口；超出窗口直接不抽（status=missing, rule=no_match）
- 这与代码 `tools/product_goal_parser.py:242 if min_dist > _DISTANCE_WINDOW: continue` 一致
```

- [ ] **Step 1.2: I-5 — 改 line 15 DR-4 文案模板**

找到 line 15（在 rev 3 → rev 4 修订记录里 DR-4 那条），改前后：

**改前**（line 15）：
```
- **DR-4（MINOR）：场景 5 preview_cli_unsafe 降级文案未规范**——补 spec 显式说明降级文案模板：`"<user_text 含特殊字符，请用 --confirm-X flag 直接传值，不通过自然语言>"`。
```

**改后**：
```
- **DR-4（MINOR）：场景 5 preview_cli_unsafe 降级文案未规范**——补 spec 显式说明降级文案模板（按 `_classify_unsafe_reason` 三类）：
  - windows_path: `"<Windows 路径含反斜杠/冒号；请用 forward slash 或加双引号包裹路径>"`
  - chinese_text: `"<user_text 含中文；请用 --confirm-X flag 直接传值，不通过自然语言>"`
  - special_chars: `"<user_text 含特殊字符（引号/换行等）；请用 --confirm-X flag 直接传值，不通过自然语言>"`
```

- [ ] **Step 1.3: I-5 — 改 line 352 测试矩阵 case 5**

找到 line 352（在测试矩阵章节），改前后：

**改前**（line 352）：
```
| 5（含转义字符） | preview_cli_unsafe 降级 | `"<user_text 含 \" 等特殊字符；请用 --confirm-X flag 直接传值，不通过自然语言>"` |
```

**改后**（拆 3 行）：
```
| 5a（chinese_text）| preview_cli_unsafe + unsafe_reason=chinese_text | 文案见 _UNSAFE_MESSAGE_TEMPLATES["chinese_text"] |
| 5b（windows_path）| preview_cli_unsafe + unsafe_reason=windows_path | 文案见 _UNSAFE_MESSAGE_TEMPLATES["windows_path"] |
| 5c（special_chars）| preview_cli_unsafe + unsafe_reason=special_chars | 文案见 _UNSAFE_MESSAGE_TEMPLATES["special_chars"] |
```

- [ ] **Step 1.4: 验证三处改动落地**

```bash
grep -n "硬窗口；超出窗口直接不抽\|按 \`_classify_unsafe_reason\` 三类\|5a（chinese_text）" docs/superpowers/specs/2026-05-07-product-goal-entry-design.md
```

Expected：3 行命中（line ~316 / ~15 / ~352）

- [ ] **Step 1.5: Commit**

```bash
git add docs/superpowers/specs/2026-05-07-product-goal-entry-design.md
git commit -m "$(cat <<'EOF'
docs(spec): 收敛 v2.25.0 spec rev 4 文档（I-2 + I-5）

I-2: line 316 软窗口表述 → 硬窗口，与代码 product_goal_parser.py:242 一致
I-5: line 15 DR-4 文案模板按 _classify_unsafe_reason 三类拆分；line 352 测试矩阵 case 5 拆 3 行（chinese_text / windows_path / special_chars）

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Expected: commit 创建成功；`git log -1 --oneline` 显示新 commit 在 spec rev 4 之上。

---

## Task 2: Commit 2 — 20 RED 测试

**Goal:** 一次性加 20 新测试到 commit 5/6 才存在的符号，所有 import 必须**函数内**。本 commit 后跑 pytest 必有 20 个 fail（RED 阶段）。

**Files:**
- Modify: `tests/test_product_goal_parser.py`（+6 测试）
- Modify: `tests/test_project_goal_guide.py`（+14 测试）

### Step 2.1: 在 `tests/test_product_goal_parser.py` 末尾追加 6 个测试

- [ ] **Step 2.1.1: 加 schema_version 校验测试**

在 `tests/test_product_goal_parser.py` 末尾加：

```python
# ===== §11.I-1: kpi_patterns.json schema v2 =====
def test_kpi_patterns_schema_version_2(tmp_path):
    """RED → GREEN: schema_version 校验在 load_dictionary 入口（不绕过 dataclass）。"""
    import json
    from tools.project_guide_dict import load_dictionary

    # 缺 schema_version → RuntimeError 含 "schema_version"
    (tmp_path / "subsystem_keywords.json").write_text(
        json.dumps({"lifting_platform": {"status": "implemented", "primary_terms": ["升降"], "supporting_terms": []}}),
        encoding="utf-8",
    )
    (tmp_path / "kpi_patterns.json").write_text(
        json.dumps({"lifting_platform": {}}),  # 无 schema_version
        encoding="utf-8",
    )
    with pytest.raises(RuntimeError, match="schema_version"):
        load_dictionary(dict_root=tmp_path)


def test_stroke_200_厘米_to_2000mm():
    """guard: 行程 200 厘米 → 2000mm（实证现状已正确，commit 3 schema 重构后保持）。"""
    from tools.product_goal_parser import parse_product_goal

    result = parse_product_goal(text="升降平台 升程 200 厘米")
    kpi = result.kpis["stroke_mm"]
    assert kpi.value == 2000.0, f"实际：{kpi.value}"
    assert kpi.unit == "mm"
    assert kpi.status == "extracted"


def test_stroke_0_2_米_to_200mm():
    """guard: 行程 0.2 米 → 200mm（中文"米"独立 regex 路径，commit 3 新 schema 添加）。"""
    from tools.product_goal_parser import parse_product_goal

    result = parse_product_goal(text="升降平台 升程 0.2 米")
    kpi = result.kpis["stroke_mm"]
    assert kpi.value == 200.0, f"实际：{kpi.value}"
    assert kpi.unit == "mm"
    assert kpi.status == "extracted"


def test_stroke_200mm_basic():
    """guard: 行程 200mm → 200mm（ASCII 路径不回归）。"""
    from tools.product_goal_parser import parse_product_goal

    result = parse_product_goal(text="升降平台 升程 200mm")
    kpi = result.kpis["stroke_mm"]
    assert kpi.value == 200.0
    assert kpi.unit == "mm"
    assert kpi.status == "extracted"


def test_evidence_token_preserves_fullwidth():
    """RED → GREEN: I-3 — evidence_token 取原文切片，保留全角 ５０ｋｇ。"""
    from tools.product_goal_parser import parse_product_goal

    result = parse_product_goal(text="升降平台 载荷 ５０ｋｇ")
    kpi = result.kpis["load_kg"]
    assert kpi.value == 50.0, f"value 应正常归一为 50：{kpi.value}"
    # 强断言：原文切片必须等于全角字符串
    assert kpi.evidence_token == "５０ｋｇ", f"evidence_token 应保留全角原文：{kpi.evidence_token!r}"


def test_evidence_list_token_preserves_fullwidth():
    """RED → GREEN: I-3 双改 — parser_evidence list 的 "token" 字段也取原文切片。"""
    from tools.product_goal_parser import parse_product_goal

    result = parse_product_goal(text="升降平台 载荷 ５０ｋｇ")
    # parser_evidence 是 list of dict，至少含一项 matched=load_kg
    assert result.parser_evidence, "parser_evidence 不应为空"
    matched_entries = [e for e in result.parser_evidence if e.get("matched") == "load_kg"]
    assert matched_entries, "应有 load_kg 的 evidence 条目"
    assert matched_entries[0]["token"] == "５０ｋｇ", f"evidence list token 应保留全角原文：{matched_entries[0]['token']!r}"
```

- [ ] **Step 2.1.2: 验证文件可解析（不跑测试，仅 collection）**

```bash
python -m py_compile tests/test_product_goal_parser.py
```

Expected：无输出（编译通过）

### Step 2.2: 在 `tests/test_project_goal_guide.py` 末尾追加 14 个测试

- [ ] **Step 2.2.1: 加 7 个 builder 测试**

在 `tests/test_project_goal_guide.py` 末尾加：

```python
# ===== §11.I-4: 7 个 per-status builder =====
def _make_parse_result_for_builder_test():
    """共用 fixture：构造一个 minimal ParseResult-like 对象供 builder 测试。"""
    from tools.product_goal_parser import parse_product_goal
    return parse_product_goal(text="做升降平台")


def test_action_for_needs_product_goal():
    """RED → GREEN: I-4 builder #1。"""
    from tools.project_guide import _action_for_needs_product_goal

    parse_result = _make_parse_result_for_builder_test()
    action = _action_for_needs_product_goal(parse_result)
    assert action["kind"] == "supply_product_goal"
    assert "preview_cli" in action
    assert "--product-goal" in action["preview_cli"]


def test_action_for_needs_subsystem_confirmation():
    """RED → GREEN: I-4 builder #2。"""
    from tools.project_guide import _action_for_needs_subsystem_confirmation

    parse_result = _make_parse_result_for_builder_test()
    action = _action_for_needs_subsystem_confirmation(parse_result)
    assert action["kind"] == "confirm_subsystem"
    assert "preview_cli" in action
    assert "--confirm-subsystem" in action["preview_cli"]


def test_action_for_unknown_subsystem():
    """RED → GREEN: I-4 builder #3 — 不带 preview_cli，必带 supported。"""
    from tools.project_guide import _action_for_unknown_subsystem

    parse_result = _make_parse_result_for_builder_test()
    action = _action_for_unknown_subsystem(parse_result)
    assert action["kind"] == "list_supported_subsystems"
    assert action["supported"] == ["lifting_platform", "end_effector"]
    # 此 builder 不带 preview_cli（spec rev 4 builder 契约表）
    assert "preview_cli" not in action


def test_action_for_not_yet_implemented():
    """RED → GREEN: I-4 builder #4 — 不带 preview_cli，必带 alternatives。"""
    from tools.project_guide import _action_for_not_yet_implemented

    parse_result = _make_parse_result_for_builder_test()
    action = _action_for_not_yet_implemented(parse_result)
    assert action["kind"] == "wait_for_implementation"
    alts = action["alternatives"]
    assert "lifting_platform" in alts["implemented_subsystems"]
    assert "end_effector" in alts["implemented_subsystems"]
    assert "switch_example" in alts
    assert "feedback_url" in alts
    assert "preview_cli" not in action


def test_action_for_needs_kpi_confirmation():
    """RED → GREEN: I-4 builder #5。"""
    from tools.project_guide import _action_for_needs_kpi_confirmation

    parse_result = _make_parse_result_for_builder_test()
    action = _action_for_needs_kpi_confirmation(parse_result, missing=["load_kg", "stroke_mm"])
    assert action["kind"] == "supply_missing_kpis"
    assert action["missing_kpis"] == ["load_kg", "stroke_mm"]
    assert "preview_cli" in action


def test_action_for_needs_design_doc():
    """RED → GREEN: I-4 builder #6。"""
    from tools.project_guide import _action_for_needs_design_doc

    parse_result = _make_parse_result_for_builder_test()
    action = _action_for_needs_design_doc(parse_result)
    assert action["kind"] == "supply_design_doc"
    assert "preview_cli" in action
    assert "--design-doc" in action["preview_cli"]


def test_action_for_ready_for_cad_spec():
    """RED → GREEN: I-4 builder #7。"""
    from tools.project_guide import _action_for_ready_for_cad_spec

    parse_result = _make_parse_result_for_builder_test()
    action = _action_for_ready_for_cad_spec(parse_result, design_doc=Path("docs/design/x.md"))
    assert action["kind"] == "run_cad_spec"
    assert "preview_cli" in action
    assert "cad_pipeline.py spec" in action["preview_cli"]
```

- [ ] **Step 2.2.2: 加 e2e pin 测试**

继续在同文件末尾加：

```python
def test_derive_goal_status_e2e_all_7_paths():
    """guard: I-4 e2e 集成 pin — 7 状态全路径走真实 parse_product_goal 入口。"""
    from tools.product_goal_parser import parse_product_goal
    from tools.project_guide import _derive_goal_status_and_next_action

    cases = [
        # (input, design_doc, expected_status)
        ("",                                   None, "needs_product_goal"),
        ("升降",                                None, "needs_subsystem_confirmation"),
        ("xyzzy 不存在的产品",                   None, "unknown_subsystem"),
        ("做导航 SLAM",                         None, "not_yet_implemented"),
        ("做升降平台",                          None, "needs_kpi_confirmation"),
        ("做升降 50kg 200mm 350x230mm 平台",    None, "needs_design_doc"),
        ("做升降 50kg 200mm 350x230mm 平台",    Path("docs/x.md"), "ready_for_cad_spec"),
    ]
    for text, design_doc, expected_status in cases:
        parse_result = parse_product_goal(text=text)
        status, action = _derive_goal_status_and_next_action(
            parse_result, design_doc, root=Path(".")
        )
        assert status == expected_status, f"text={text!r} 实际 status={status}"
        assert isinstance(action, dict) and "kind" in action
```

- [ ] **Step 2.2.3: 加 5 个 _classify_unsafe_reason 测试**

继续追加：

```python
# ===== §11.M-3: _classify_unsafe_reason =====
def test_classify_unsafe_windows_path():
    """RED → GREEN: M-3 windows_path 路径。"""
    from tools.project_guide import _classify_unsafe_reason

    assert _classify_unsafe_reason("D:\\Work\\design.md") == "windows_path"
    assert _classify_unsafe_reason("C:/User/x") == "windows_path"


def test_classify_unsafe_chinese_text():
    """RED → GREEN: M-3 chinese_text 路径。"""
    from tools.project_guide import _classify_unsafe_reason

    assert _classify_unsafe_reason("做升降平台") == "chinese_text"


def test_classify_unsafe_chinese_with_backslash():
    """RED → GREEN: M-3 优先级 — 中文里随手反斜杠必须归 chinese_text 不归 windows_path。"""
    from tools.project_guide import _classify_unsafe_reason

    assert _classify_unsafe_reason("做\\升降") == "chinese_text"
    # 中文+引号 也走 chinese_text（CJK 优先于 special_chars）
    assert _classify_unsafe_reason('高"精度"') == "chinese_text"


def test_classify_unsafe_special_chars():
    """RED → GREEN: M-3 special_chars 路径（含纯 ASCII 引号 / 换行 / 单反斜杠）。"""
    from tools.project_guide import _classify_unsafe_reason

    assert _classify_unsafe_reason('"abc"') == "special_chars"
    assert _classify_unsafe_reason("\\") == "special_chars"
    assert _classify_unsafe_reason("\n") == "special_chars"


def test_classify_unsafe_safe_passthrough():
    """guard: M-3 safe 路径不变。"""
    from tools.project_guide import _classify_unsafe_reason

    assert _classify_unsafe_reason("lifting_platform") == "safe"
    assert _classify_unsafe_reason("foo-bar_v1.2") == "safe"
```

- [ ] **Step 2.2.4: 加 unsafe_reason 不变量集成测试**

继续追加：

```python
def test_sanitize_preview_cli_unsafe_reason_invariant(tmp_path):
    """RED → GREEN: M-4 不变量 — preview_cli_unsafe=True ⇔ unsafe_reason 存在；safe 时两字段都不写。"""
    from tools.project_guide import write_project_goal_guide

    # chinese_text 触发降级
    report = write_project_goal_guide(tmp_path, product_goal="做升降平台")
    action = report["next_action"]
    if action.get("preview_cli_unsafe") is True:
        assert "unsafe_reason" in action, "preview_cli_unsafe=True 必须有 unsafe_reason"
        assert action["unsafe_reason"] in {"windows_path", "chinese_text", "special_chars"}

    # safe 路径下两字段都不写
    report_safe = write_project_goal_guide(tmp_path, product_goal="lifting_platform")
    action_safe = report_safe["next_action"]
    if not action_safe.get("preview_cli_unsafe"):
        assert "unsafe_reason" not in action_safe, "safe 路径不得有 unsafe_reason"
```

- [ ] **Step 2.2.5: 验证文件可解析**

```bash
python -m py_compile tests/test_project_goal_guide.py
```

Expected：无输出

- [ ] **Step 2.3: 跑全量测试，确认 20 个 RED**

```bash
pytest tests/test_product_goal_parser.py tests/test_project_goal_guide.py -q --no-header 2>&1 | tail -30
```

Expected：~20 fails（精确分布因 builder/classify/sanitize 等命名不存在导致的 ImportError 包在函数内被 pytest 转成 test fail，**不连坐其他测试**）。如果 pytest 显示 collection error 把整个文件标红，**说明某个 import 漏移到模块顶层**——回 step 2.1/2.2 检查 import 都在函数内。

guard 类测试（如 `test_stroke_200_厘米_to_2000mm` / `test_classify_unsafe_safe_passthrough`）应该 fail——因为 `_classify_unsafe_reason` 不存在 / 当前 schema 仍 v1。这是预期。

- [ ] **Step 2.4: Commit**

```bash
git add tests/test_product_goal_parser.py tests/test_project_goal_guide.py
git commit -m "$(cat <<'EOF'
test(v2.25-cleanup): 增加 §11 cleanup 20 RED 测试

- parser +6: schema_version_2 / 200 厘米 / 0.2 米 / 200mm guard / evidence_token 双改 fullwidth
- guide +14: 7 builder + e2e pin + 4 classify_unsafe_reason + 1 safe + 1 sanitize_invariant
- 所有 import 函数内（避免模块顶层 ImportError 连坐既有测试）
- 测试 oracle 强断言（spec rev 4 测试 oracle 强度规范）

本 commit RED 阶段全 fail；commit 3-6 各项 refactor 转绿。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Expected: commit 创建成功。

---

## Task 3: Commit 3 — I-1 unit_normalize → schema v2

**Goal:** kpi_patterns.json schema v1 → v2（regex string[] → [{pattern, factor}]）；load_dictionary 加 schema_version pop + 校验；parser._extract_single_kpi 删 unit_normalize 索引代码改用 factor；同步既有 fixture 测试 + 1 个 既有 tmp_path 测试。

**Files:**
- Modify: `tools/project_guide_dict/kpi_patterns.json`（schema 升级）
- Modify: `tools/project_guide_dict/__init__.py`（load_dictionary）
- Modify: `tools/product_goal_parser.py:_extract_single_kpi`
- Modify: `tests/test_product_goal_parser.py`（既有 fixture sync）

- [ ] **Step 3.1: 改 kpi_patterns.json schema v2**

完全重写 `tools/project_guide_dict/kpi_patterns.json` 为以下内容：

```json
{
  "schema_version": 2,
  "lifting_platform": {
    "load_kg": {
      "regex": [
        {"pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:kg|公斤|千克)", "factor": 1}
      ],
      "context_terms": ["载荷", "承载", "负载", "升起", "举起", "提升", "升"],
      "unit": "kg",
      "slot": "capability_1"
    },
    "stroke_mm": {
      "regex": [
        {"pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:mm|毫米)", "factor": 1},
        {"pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:cm|厘米)", "factor": 10},
        {"pattern": "(\\d+(?:\\.\\d+)?)\\s*m(?![ms])", "factor": 1000},
        {"pattern": "(\\d+(?:\\.\\d+)?)\\s*米(?!\\s*厘)", "factor": 1000}
      ],
      "context_terms": ["行程", "升高", "升程", "stroke", "travel"],
      "unit": "mm",
      "slot": "capability_2"
    },
    "platform_size_mm": {
      "regex": [
        {"pattern": "(\\d+)\\s*[x×]\\s*(\\d+)\\s*(?:mm|毫米)?", "factor": 1}
      ],
      "context_terms": ["平台", "platform", "尺寸", "外形", "包络", "面积"],
      "unit": "mm",
      "value_shape": "pair",
      "slot": "envelope"
    }
  },
  "end_effector": {
    "rot_range_deg": {
      "regex": [
        {"pattern": "[±]?\\s*(\\d+(?:\\.\\d+)?)\\s*[°度]", "factor": 1}
      ],
      "context_terms": ["翻转", "旋转", "rotation", "rotate"],
      "unit": "deg",
      "slot": "capability_1"
    },
    "switch_time_s": {
      "regex": [
        {"pattern": "(\\d+(?:\\.\\d+)?)\\s*(?:s|秒)", "factor": 1}
      ],
      "context_terms": ["切换", "switch"],
      "unit": "s",
      "slot": "capability_2"
    },
    "flange_dia_mm": {
      "regex": [
        {"pattern": "Φ\\s*(\\d+(?:\\.\\d+)?)", "factor": 1},
        {"pattern": "(\\d+)\\s*mm\\s*法兰", "factor": 1},
        {"pattern": "flange\\s*(\\d+)", "factor": 1}
      ],
      "context_terms": ["法兰", "flange"],
      "unit": "mm",
      "slot": "envelope"
    }
  }
}
```

注意：删了所有旧 `unit_normalize` 字段；regex 全改为 `[{pattern, factor}]`；新增"米(?!\s*厘)" regex（factor=1000）让中文"米"独立路径。

- [ ] **Step 3.2: 改 `tools/project_guide_dict/__init__.py:load_dictionary` 加 schema_version pop**

打开 `tools/project_guide_dict/__init__.py`，找到 `load_dictionary` 函数（line 83-100），改前后：

**改前**（line 83-100）：
```python
def load_dictionary(*, dict_root: Path | None = None) -> ProductGoalDictionary:
    """从 JSON 文件加载词典；缺文件 / schema 错 → RuntimeError。"""
    root = dict_root or _DEFAULT_ROOT
    keywords_path = root / "subsystem_keywords.json"
    patterns_path = root / "kpi_patterns.json"

    if not keywords_path.is_file():
        raise RuntimeError(f"subsystem_keywords.json 不存在：{keywords_path}")
    if not patterns_path.is_file():
        raise RuntimeError(f"kpi_patterns.json 不存在：{patterns_path}")

    keywords = json.loads(keywords_path.read_text(encoding="utf-8"))
    patterns = json.loads(patterns_path.read_text(encoding="utf-8"))

    return ProductGoalDictionary(
        subsystem_keywords=keywords,
        kpi_patterns=patterns,
    )
```

**改后**：
```python
def load_dictionary(*, dict_root: Path | None = None) -> ProductGoalDictionary:
    """从 JSON 文件加载词典；缺文件 / schema 错 → RuntimeError。"""
    root = dict_root or _DEFAULT_ROOT
    keywords_path = root / "subsystem_keywords.json"
    patterns_path = root / "kpi_patterns.json"

    if not keywords_path.is_file():
        raise RuntimeError(f"subsystem_keywords.json 不存在：{keywords_path}")
    if not patterns_path.is_file():
        raise RuntimeError(f"kpi_patterns.json 不存在：{patterns_path}")

    keywords = json.loads(keywords_path.read_text(encoding="utf-8"))
    patterns = json.loads(patterns_path.read_text(encoding="utf-8"))

    # schema_version 顶层校验 + pop（patterns 是 json.loads 返回的临时 dict，
    # pop 不泄漏 mutation 给外部 caller）
    schema_version = patterns.pop("schema_version", None)
    if schema_version != 2:
        raise RuntimeError(
            f"kpi_patterns.json schema_version 必须是 2，实际：{schema_version!r}"
        )

    return ProductGoalDictionary(
        subsystem_keywords=keywords,
        kpi_patterns=patterns,
    )
```

- [ ] **Step 3.3: 改 `tools/product_goal_parser.py:_extract_single_kpi`**

打开 `tools/product_goal_parser.py`，找到 `_extract_single_kpi`（line 190-280），改前后：

**改前**（line 205-256，关键段）：
```python
    context_terms = spec["context_terms"]
    unit = spec["unit"]
    unit_normalize = spec.get("unit_normalize") or {}
    value_shape = spec.get("value_shape", "single")

    # ... context 收集 ...

    # 按 yaml 顺序短路 regex（首条命中即返回）
    for regex_idx, pattern in enumerate(spec["regex"]):
        compiled = re.compile(pattern)
        for match in compiled.finditer(normalized):
            # ... distance check ...

            value: float | tuple[float, float]
            if value_shape == "pair":
                value = (float(match.group(1)), float(match.group(2)))
            else:
                raw = float(match.group(1))
                # 单位归一：regex 第 0 条为基准单位（不归一）；
                # 第 i 条（i≥1）按 unit_normalize 第 i-1 个 key 取倍率
                if regex_idx > 0 and unit_normalize:
                    unit_keys = list(unit_normalize.keys())
                    if regex_idx - 1 < len(unit_keys):
                        raw = raw * unit_normalize[unit_keys[regex_idx - 1]]
                value = raw
            # ... 后略 ...
```

**改后**（删 unit_normalize 相关代码，改用 factor）：
```python
    context_terms = spec["context_terms"]
    unit = spec["unit"]
    value_shape = spec.get("value_shape", "single")

    # ... context 收集（不变）...

    # 按 yaml 顺序短路 regex（首条命中即返回）
    for regex_entry in spec["regex"]:
        pattern = regex_entry["pattern"]
        factor = regex_entry["factor"]
        compiled = re.compile(pattern)
        for match in compiled.finditer(normalized):
            # ... distance check（不变）...

            value: float | tuple[float, float]
            if value_shape == "pair":
                value = (float(match.group(1)), float(match.group(2)))
            else:
                raw = float(match.group(1))
                value = raw * factor   # factor=1 时不变
            # ... 后略（注意 evidence dict 里的 regex_index 改为按 enumerate 给出索引或者删掉）...
```

具体 evidence dict 现有 line 258-263：
```python
            evidence.append({
                "token": match.group(0),
                "matched": kpi_name,
                "rule": f"regex+context:{context_terms[0]}",
                "regex_index": regex_idx,
            })
```

由于 `regex_idx` 不再有，把 `for regex_entry in spec["regex"]` 改为 `for regex_idx, regex_entry in enumerate(spec["regex"])`，保留 `regex_idx` 给 evidence。

完整改动（line 205-271 替换）：

```python
def _extract_single_kpi(
    normalized: str,
    kpi_name: str,
    spec: dict[str, Any],
    evidence: list[dict[str, Any]],
) -> KpiExtraction:
    """单 KPI 抽取：regex 短路命中 + ±20 字符 context_terms 距离判定。

    schema v2：spec["regex"] 为 [{pattern, factor}] 对象数组；factor 直接随 entry 携带。
    """
    context_terms = spec["context_terms"]
    unit = spec["unit"]
    value_shape = spec.get("value_shape", "single")

    # 收集所有 context_terms 的出现位置（同 term 多次出现亦记录）
    context_positions: list[int] = []
    for term in context_terms:
        idx = 0
        while True:
            pos = normalized.find(term, idx)
            if pos < 0:
                break
            context_positions.append(pos)
            idx = pos + len(term)

    if not context_positions:
        return KpiExtraction(
            kpi_name=kpi_name,
            value=None,
            unit=unit,
            evidence_token=None,
            rule="no_context",
            status="missing",
        )

    # 按 yaml 顺序短路 regex（首条命中即返回）
    for regex_idx, regex_entry in enumerate(spec["regex"]):
        pattern = regex_entry["pattern"]
        factor = regex_entry["factor"]
        compiled = re.compile(pattern)
        for match in compiled.finditer(normalized):
            number_start = match.start()
            number_end = match.end()
            # 数字块端点距 context 任一端点最近距离
            min_dist = min(
                min(abs(cp - number_start), abs(cp - number_end))
                for cp in context_positions
            )
            if min_dist > _DISTANCE_WINDOW:
                continue

            value: float | tuple[float, float]
            if value_shape == "pair":
                value = (float(match.group(1)), float(match.group(2)))
            else:
                raw = float(match.group(1))
                value = raw * factor   # factor=1 时不变

            evidence.append({
                "token": match.group(0),
                "matched": kpi_name,
                "rule": f"regex+context:{context_terms[0]}",
                "regex_index": regex_idx,
            })
            return KpiExtraction(
                kpi_name=kpi_name,
                value=value,
                unit=unit,
                evidence_token=match.group(0),
                rule=f"regex+context:{context_terms[0]}",
                status="extracted",
            )

    return KpiExtraction(
        kpi_name=kpi_name,
        value=None,
        unit=unit,
        evidence_token=None,
        rule="no_match",
        status="missing",
    )
```

注意：`evidence_token=match.group(0)` 与 `evidence list "token": match.group(0)` 仍是 normalized 串——本 commit 不改这两处（commit 4 才双改原文切片）。

- [ ] **Step 3.4: 同步既有 `test_kpi_patterns_json_has_3_kpis_per_implemented_subsystem`**

打开 `tests/test_product_goal_parser.py`，找到 line 35 `test_kpi_patterns_json_has_3_kpis_per_implemented_subsystem`，改前后：

**改前**（line 35-50）：
```python
def test_kpi_patterns_json_has_3_kpis_per_implemented_subsystem():
    path = DICT_DIR / "kpi_patterns.json"
    assert path.is_file(), f"missing: {path}"
    data = json.loads(path.read_text(encoding="utf-8"))

    assert set(data.keys()) == {"lifting_platform", "end_effector"}

    assert set(data["lifting_platform"].keys()) == {"load_kg", "stroke_mm", "platform_size_mm"}
    assert set(data["end_effector"].keys()) == {"rot_range_deg", "switch_time_s", "flange_dia_mm"}

    # 每个 KPI 必有 regex (list) + context_terms (list) + unit (str)
    for subsystem, kpis in data.items():
        for kpi_name, kpi in kpis.items():
            assert isinstance(kpi.get("regex"), list) and kpi["regex"], f"{subsystem}.{kpi_name} regex 缺"
            assert isinstance(kpi.get("context_terms"), list) and kpi["context_terms"], f"{subsystem}.{kpi_name} context_terms 缺"
            assert isinstance(kpi.get("unit"), str), f"{subsystem}.{kpi_name} unit 缺"
```

**改后**：
```python
def test_kpi_patterns_json_has_3_kpis_per_implemented_subsystem():
    path = DICT_DIR / "kpi_patterns.json"
    assert path.is_file(), f"missing: {path}"
    data = json.loads(path.read_text(encoding="utf-8"))

    # schema v2: 顶层有 schema_version + 子系统 dict
    assert data.get("schema_version") == 2, f"schema_version 应为 2：{data.get('schema_version')!r}"
    subsystems = {k: v for k, v in data.items() if k != "schema_version"}
    assert set(subsystems.keys()) == {"lifting_platform", "end_effector"}

    assert set(subsystems["lifting_platform"].keys()) == {"load_kg", "stroke_mm", "platform_size_mm"}
    assert set(subsystems["end_effector"].keys()) == {"rot_range_deg", "switch_time_s", "flange_dia_mm"}

    # schema v2: regex 是 [{pattern, factor}] 对象数组
    for subsystem, kpis in subsystems.items():
        for kpi_name, kpi in kpis.items():
            regex = kpi.get("regex")
            assert isinstance(regex, list) and regex, f"{subsystem}.{kpi_name} regex 缺"
            for entry in regex:
                assert isinstance(entry, dict) and "pattern" in entry and "factor" in entry, \
                    f"{subsystem}.{kpi_name} regex entry 缺 pattern/factor"
            assert isinstance(kpi.get("context_terms"), list) and kpi["context_terms"], f"{subsystem}.{kpi_name} context_terms 缺"
            assert isinstance(kpi.get("unit"), str), f"{subsystem}.{kpi_name} unit 缺"
```

- [ ] **Step 3.5: 同步既有 `test_load_dictionary_raises_on_implemented_subsystem_missing_kpis`**

找到 line 71 这个测试，改前后：

**改前**（line 71-80）：
```python
def test_load_dictionary_raises_on_implemented_subsystem_missing_kpis(tmp_path):
    from tools.project_guide_dict import load_dictionary

    (tmp_path / "subsystem_keywords.json").write_text(
        json.dumps({"lifting_platform": {"status": "implemented", "primary_terms": ["升降"], "supporting_terms": []}}),
        encoding="utf-8",
    )
    (tmp_path / "kpi_patterns.json").write_text("{}", encoding="utf-8")
    with pytest.raises(RuntimeError, match="lifting_platform.*kpi_patterns"):
        load_dictionary(dict_root=tmp_path)
```

**改后**（在 kpi_patterns.json 加 schema_version=2 让 schema 校验通过，再触发原断言）：
```python
def test_load_dictionary_raises_on_implemented_subsystem_missing_kpis(tmp_path):
    from tools.project_guide_dict import load_dictionary

    (tmp_path / "subsystem_keywords.json").write_text(
        json.dumps({"lifting_platform": {"status": "implemented", "primary_terms": ["升降"], "supporting_terms": []}}),
        encoding="utf-8",
    )
    # schema v2: 必须含 schema_version=2 才能通过 load_dictionary 入口，
    # 否则 schema_version 校验先 raise，原 implemented-subsystem-missing-kpi 断言失败
    (tmp_path / "kpi_patterns.json").write_text(
        json.dumps({"schema_version": 2}),
        encoding="utf-8",
    )
    with pytest.raises(RuntimeError, match="lifting_platform.*kpi_patterns"):
        load_dictionary(dict_root=tmp_path)
```

注意：`test_load_dictionary_raises_on_missing_file`（line 62）**不改**——它走的是缺文件路径在 schema 校验前 raise，不受 schema 升级影响。

- [ ] **Step 3.6: 跑 I-1 相关测试验证转绿**

```bash
pytest tests/test_product_goal_parser.py -k "schema_version_2 or 200_厘米 or 0_2_米 or 200mm_basic or kpi_patterns_json_has_3 or load_dictionary_raises" -v
```

Expected：6 passed（4 个新 RED 转绿 + 2 个既有 fixture 同步通过）

- [ ] **Step 3.7: 跑全量 parser/dict 测试**

```bash
pytest tests/test_product_goal_parser.py -q
```

Expected：所有 parser 测试 pass（含 6 新加 + 既有 ~25 个）

- [ ] **Step 3.8: Commit**

```bash
git add tools/project_guide_dict/kpi_patterns.json tools/project_guide_dict/__init__.py tools/product_goal_parser.py tests/test_product_goal_parser.py
git commit -m "$(cat <<'EOF'
refactor(kpi-patterns): I-1 unit_normalize → schema v2 list[{pattern,factor}]

- kpi_patterns.json 顶层加 schema_version=2；regex 从 string[] 改为 [{pattern,factor}]
- 删除 unit_normalize 字段（消除位置耦合 + 清死代码）
- "厘米"/"米" 各自独立 regex 行（factor=10/1000），不再依赖 dict key 顺序
- load_dictionary 加 schema_version 顶层校验 + pop（无副作用，pop 在 json.loads 临时 dict）
- 既有 test_kpi_patterns_json_has_3_kpis 同步过滤 schema_version 顶层 key
- 既有 test_load_dictionary_raises_on_implemented_subsystem_missing_kpis 修 tmp_path 内容含 schema_version=2

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: Commit 4 — I-3 evidence_token 双改

**Goal:** 加 `_raw_token_for_match` helper；`_extract_single_kpi` 内两处 `match.group(0)` 改为原文切片（KpiExtraction.evidence_token + evidence list "token"）。

**Files:**
- Modify: `tools/product_goal_parser.py`

- [ ] **Step 4.1: 加 `_raw_token_for_match` helper**

在 `tools/product_goal_parser.py` 顶层 `_extract_single_kpi` 之前的位置（约 line 188-189，紧邻 `_extract_kpis_for_subsystem` 后）加：

```python
def _raw_token_for_match(
    raw_text: str,
    normalized: str,
    match: "re.Match[str]",
) -> str:
    """从原文取 match 区间对应的子串。

    NFKC 在 ASCII 路径与全角→半角场景下保持 char 数 1:1
    （len(raw)==len(normalized)），此时 match.start()/end() 在两个串上索引等价，
    可直接切 raw_text。极少数 NFKC 拆分（⅔ → 2/3、㎡ → m2）会让 normalized 比
    raw_text 长，索引域错位 → fallback 到 match.group(0)（normalized 串上 token，
    与现状等价不优于 normalized）。
    """
    if len(raw_text) == len(normalized):
        return raw_text[match.start():match.end()]
    return match.group(0)
```

注意 `re` 是模块级 import；签名里用字符串字面 `"re.Match[str]"` 避免类型解析问题（Python 3.9+ `re.Match` 但若需更老则 string forward ref）。

- [ ] **Step 4.2: 改 `_extract_single_kpi` 签名加 `raw_text` 参数 + 改两处 evidence_token 取值**

`_extract_single_kpi` 现签名只接 `normalized`，但要走原文切片必须也拿到 `raw_text`。改签名：

**改前**（line 190-194）：
```python
def _extract_single_kpi(
    normalized: str,
    kpi_name: str,
    spec: dict[str, Any],
    evidence: list[dict[str, Any]],
) -> KpiExtraction:
```

**改后**（加 `raw_text` 参数）：
```python
def _extract_single_kpi(
    raw_text: str,
    normalized: str,
    kpi_name: str,
    spec: dict[str, Any],
    evidence: list[dict[str, Any]],
) -> KpiExtraction:
```

然后在函数体内，把两处 `match.group(0)` 改为 `_raw_token_for_match(raw_text, normalized, match)`：

**改前**（line 258-263 + line 264-271）：
```python
            evidence.append({
                "token": match.group(0),
                "matched": kpi_name,
                "rule": f"regex+context:{context_terms[0]}",
                "regex_index": regex_idx,
            })
            return KpiExtraction(
                kpi_name=kpi_name,
                value=value,
                unit=unit,
                evidence_token=match.group(0),
                rule=f"regex+context:{context_terms[0]}",
                status="extracted",
            )
```

**改后**：
```python
            raw_token = _raw_token_for_match(raw_text, normalized, match)
            evidence.append({
                "token": raw_token,
                "matched": kpi_name,
                "rule": f"regex+context:{context_terms[0]}",
                "regex_index": regex_idx,
            })
            return KpiExtraction(
                kpi_name=kpi_name,
                value=value,
                unit=unit,
                evidence_token=raw_token,
                rule=f"regex+context:{context_terms[0]}",
                status="extracted",
            )
```

- [ ] **Step 4.3: 改 `_extract_kpis_for_subsystem` 调用点传 `raw_text`**

找到 `_extract_kpis_for_subsystem`（line 174-187），它现在调 `_extract_single_kpi(normalized, kpi_name, spec, evidence)`，需要也传 `raw_text`。但它自己也没有 `raw_text` 参数，需要再追加一层签名：

**改前**（line 174-187）：
```python
def _extract_kpis_for_subsystem(
    normalized: str,
    subsystem_class: str,
    dictionary: ProductGoalDictionary,
    evidence: list[dict[str, Any]],
) -> dict[str, KpiExtraction]:
    """对 implemented 子系统跑所有 KPI 抽取，未命中标 missing。"""
    extractions: dict[str, KpiExtraction] = {}
    kpi_specs = dictionary.kpi_patterns[subsystem_class]

    for kpi_name, spec in kpi_specs.items():
        extracted = _extract_single_kpi(normalized, kpi_name, spec, evidence)
        extractions[kpi_name] = extracted
    return extractions
```

**改后**（加 raw_text 参数 + 透传）：
```python
def _extract_kpis_for_subsystem(
    raw_text: str,
    normalized: str,
    subsystem_class: str,
    dictionary: ProductGoalDictionary,
    evidence: list[dict[str, Any]],
) -> dict[str, KpiExtraction]:
    """对 implemented 子系统跑所有 KPI 抽取，未命中标 missing。"""
    extractions: dict[str, KpiExtraction] = {}
    kpi_specs = dictionary.kpi_patterns[subsystem_class]

    for kpi_name, spec in kpi_specs.items():
        extracted = _extract_single_kpi(raw_text, normalized, kpi_name, spec, evidence)
        extractions[kpi_name] = extracted
    return extractions
```

- [ ] **Step 4.4: 改 `parse_product_goal` 主体调用点传 `raw_text`**

找到 `parse_product_goal` 函数体（约 line 80-95），找到调 `_extract_kpis_for_subsystem` 的地方（应该在 line 86 附近），改：

**改前**（grep `_extract_kpis_for_subsystem` 找）:
```python
        kpis = _extract_kpis_for_subsystem(
            normalized, subsystem_class, dictionary, evidence
        )
```

**改后**：
```python
        kpis = _extract_kpis_for_subsystem(
            text, normalized, subsystem_class, dictionary, evidence
        )
```

注意：`text` 是 `parse_product_goal` 的 raw 输入参数（即 raw_text）。

- [ ] **Step 4.5: 跑 I-3 测试验证双改转绿**

```bash
pytest tests/test_product_goal_parser.py -k "evidence_token_preserves_fullwidth or evidence_list_token_preserves_fullwidth" -v
```

Expected：2 passed

- [ ] **Step 4.6: 跑全量 parser 测试**

```bash
pytest tests/test_product_goal_parser.py -q
```

Expected：所有 parser 测试 pass

- [ ] **Step 4.7: Commit**

```bash
git add tools/product_goal_parser.py
git commit -m "$(cat <<'EOF'
refactor(parser): I-3 evidence_token 取原文切片（双改）

- 加 _raw_token_for_match helper，len(raw)==len(normalized) 时切原文，
  NFKC 拆分场景 fallback 到 match.group(0)
- _extract_single_kpi 签名加 raw_text；KpiExtraction.evidence_token 与 evidence
  list "token" 双改原文切片（避免下游 audit 部分失真）
- _extract_kpis_for_subsystem / parse_product_goal 调用点透传 raw_text

全角输入 ５０ｋｇ 现 evidence_token 保留原文，不再被 NFKC 归一化为 50kg。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: Commit 5 — I-4 拆 7 per-status builder

**Goal:** 加 7 个 `_action_for_*` builder + 重写 `_derive_goal_status_and_next_action` 为纯路由。本 commit 不动 `_sanitize_preview_cli`（让 commit 6 单点改）。

**Files:**
- Modify: `tools/project_guide.py`（line 710-790 重写 + 新增 7 builder）

- [ ] **Step 5.1: 在 `_sanitize_preview_cli` 之后、`_derive_goal_status_and_next_action` 之前加 7 个 builder**

打开 `tools/project_guide.py`，找到 `_sanitize_preview_cli` 结尾（line 707）和 `_derive_goal_status_and_next_action` 起头（line 710）之间，插入：

```python
# ===== I-4: 7 per-status builder（spec rev 4 §I-4） =====
def _action_for_needs_product_goal(parse_result: Any) -> dict[str, Any]:
    return {
        "kind": "supply_product_goal",
        "preview_cli": (
            'python cad_pipeline.py project-guide --product-goal "<描述你的产品>"'
        ),
    }


def _action_for_needs_subsystem_confirmation(parse_result: Any) -> dict[str, Any]:
    return {
        "kind": "confirm_subsystem",
        "preview_cli": (
            'python cad_pipeline.py project-guide --product-goal "..." '
            "--confirm-subsystem <lifting_platform|end_effector>"
        ),
    }


def _action_for_unknown_subsystem(parse_result: Any) -> dict[str, Any]:
    return {
        "kind": "list_supported_subsystems",
        "supported": ["lifting_platform", "end_effector"],
    }


def _action_for_not_yet_implemented(parse_result: Any) -> dict[str, Any]:
    return {
        "kind": "wait_for_implementation",
        "alternatives": {
            "implemented_subsystems": ["lifting_platform", "end_effector"],
            "switch_example": (
                'python cad_pipeline.py project-guide --product-goal "做升降平台 50kg"'
            ),
            "feedback_url": "https://github.com/proecheng/cad-spec-gen/issues/new",
        },
    }


def _action_for_needs_kpi_confirmation(
    parse_result: Any, missing: list[str]
) -> dict[str, Any]:
    return {
        "kind": "supply_missing_kpis",
        "missing_kpis": missing,
        "preview_cli": _build_kpi_preview_cli(parse_result.subsystem_class, missing),
    }


def _action_for_needs_design_doc(parse_result: Any) -> dict[str, Any]:
    return {
        "kind": "supply_design_doc",
        "preview_cli": (
            f"python cad_pipeline.py project-guide "
            f'--product-goal "{parse_result.raw_text}" '
            f"--design-doc docs/design/<chapter>-{parse_result.subsystem_class}.md"
        ),
    }


def _action_for_ready_for_cad_spec(
    parse_result: Any, design_doc: "str | Path"
) -> dict[str, Any]:
    return {
        "kind": "run_cad_spec",
        "preview_cli": (
            f"python cad_pipeline.py spec --subsystem {parse_result.subsystem_class} "
            f"--design-doc {design_doc}"
        ),
    }
```

- [ ] **Step 5.2: 重写 `_derive_goal_status_and_next_action`**

找到 `_derive_goal_status_and_next_action`（line 710-790），整体替换为：

```python
def _derive_goal_status_and_next_action(
    parse_result: Any,
    design_doc: str | Path | None,
    root: Path,
) -> tuple[str, dict[str, Any]]:
    """从 parse 结果派生 status + next_action。

    路由层：决定 status，调对应 builder，最后统一过 _sanitize_preview_cli。
    """
    if not parse_result.raw_text or not parse_result.raw_text.strip():
        status = "needs_product_goal"
        action = _action_for_needs_product_goal(parse_result)
    elif parse_result.subsystem_status == "ambiguous":
        status = "needs_subsystem_confirmation"
        action = _action_for_needs_subsystem_confirmation(parse_result)
    elif parse_result.subsystem_status == "unknown":
        status = "unknown_subsystem"
        action = _action_for_unknown_subsystem(parse_result)
    elif parse_result.subsystem_status == "not_yet_implemented":
        status = "not_yet_implemented"
        action = _action_for_not_yet_implemented(parse_result)
    else:
        # implemented 分支
        missing = [
            name for name, k in parse_result.kpis.items() if k.status != "extracted"
        ]
        if missing:
            status = "needs_kpi_confirmation"
            action = _action_for_needs_kpi_confirmation(parse_result, missing)
        elif not design_doc:
            status = "needs_design_doc"
            action = _action_for_needs_design_doc(parse_result)
        else:
            status = "ready_for_cad_spec"
            action = _action_for_ready_for_cad_spec(parse_result, design_doc)

    return status, _sanitize_preview_cli(action, parse_result)
```

- [ ] **Step 5.3: 跑 I-4 测试验证转绿**

```bash
pytest tests/test_project_goal_guide.py -k "action_for or derive_goal_status_e2e" -v
```

Expected：8 passed（7 builder + 1 e2e pin）

- [ ] **Step 5.4: 跑全量 guide + parser 测试**

```bash
pytest tests/test_product_goal_parser.py tests/test_project_goal_guide.py -q
```

Expected：全 pass（含既有 + 部分新增）；`_classify_unsafe_*` 5 个仍 fail（commit 6 才转绿）

- [ ] **Step 5.5: Commit**

```bash
git add tools/project_guide.py
git commit -m "$(cat <<'EOF'
refactor(project-guide): I-4 拆 _derive_goal_status_and_next_action 为 7 builder

- 加 _action_for_* x 7（needs_product_goal / needs_subsystem_confirmation /
  unknown_subsystem / not_yet_implemented / needs_kpi_confirmation /
  needs_design_doc / ready_for_cad_spec）
- _derive_goal_status_and_next_action 重写为纯路由（80 行 → ~30 行）
- 不动 _sanitize_preview_cli（commit 6 单点改）
- builder 内不调 sanitize（路由层统一调一次）
- e2e pin 测试 test_derive_goal_status_e2e_all_7_paths 锁定端到端契约

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: Commit 6 — M-3 + I-5 unsafe_reason 分类 + 文案落地

**Goal:** 加共享常量 `_SAFE_CLI_PATTERN` / `_CJK_PATTERN` / `_WIN_PATH_PATTERN` / `_UNSAFE_MESSAGE_TEMPLATES`；加 `_classify_unsafe_reason`；改写 `_safe_cli_token` 用共享常量；改写 `_sanitize_preview_cli` 按 reason 分类降级。

**Files:**
- Modify: `tools/project_guide.py`（顶层常量 + `_safe_cli_token` + 加 `_classify_unsafe_reason` + 重写 `_sanitize_preview_cli`）

- [ ] **Step 6.1: 在 `tools/project_guide.py` 顶层加共享常量**

找到现有 import 区结尾（约 line 20-30），加一组模块级常量：

```python
# ===== §11.M-5/M-3: 共享常量（_safe_cli_token 与 _classify_unsafe_reason 共用） =====
_SAFE_CLI_PATTERN = re.compile(r"[A-Za-z0-9_.\-]+")
_CJK_PATTERN = re.compile(r"[一-鿿㐀-䶿]")  # CJK Unified (U+4E00-U+9FFF) + Ext-A (U+3400-U+4DBF)
_WIN_PATH_PATTERN = re.compile(r"^[A-Za-z]:[\\/]")

_UNSAFE_MESSAGE_TEMPLATES = {
    "windows_path": (
        "<Windows 路径含反斜杠/冒号；请用 forward slash 或加双引号包裹路径>"
    ),
    "chinese_text": (
        "<user_text 含中文；请用 --confirm-X flag 直接传值，不通过自然语言>"
    ),
    "special_chars": (
        "<user_text 含特殊字符（引号/换行等）；"
        "请用 --confirm-X flag 直接传值，不通过自然语言>"
    ),
}
```

注意 `re` 已 module-level imported（line ~10），如未则需补 `import re`。

- [ ] **Step 6.2: 改写 `_safe_cli_token` 用共享常量**

找到 `_safe_cli_token`（line 630-631），改：

**改前**（line 630-631）：
```python
def _safe_cli_token(value: str) -> bool:
    return bool(re.fullmatch(r"[A-Za-z0-9_.-]+", value or ""))
```

**改后**：
```python
def _safe_cli_token(value: str) -> bool:
    """token 校验（subsystem 名等用）。

    与 _classify_unsafe_reason 共用 _SAFE_CLI_PATTERN，避免字面发散；
    注意：empty 在此返 False（不算合法 token），而 _classify_unsafe_reason("") 返 "safe"
    （含义"无需降级"），两者语义有别。
    """
    return bool(_SAFE_CLI_PATTERN.fullmatch(value or ""))
```

- [ ] **Step 6.3: 在 `_safe_cli_token` 之后加 `_classify_unsafe_reason`**

紧跟 `_safe_cli_token` 之后插入：

```python
def _classify_unsafe_reason(value: str) -> str:
    """分类 user_text 不安全原因，给出精确下一步提示。

    返回值（按优先级）：'safe' | 'chinese_text' | 'windows_path' | 'special_chars'。
    优先级（互斥）：safe > chinese_text > windows_path > special_chars。

    Empty string 与 None：value 必须为 str；调用方（_sanitize_preview_cli）已用
    `or ""` 兜底 None。empty 时返 "safe"——含义"无 unsafe 内容需要降级"，与
    _safe_cli_token("") = False 不同（后者用于 token 校验，empty 不算合法 token）。

    日文 Kanji 命中 CJK Unified 范围会归 chinese_text；Hangul / 假名归
    special_chars——本管线针对中文用户的产品边界。
    """
    if not value:
        return "safe"
    if _SAFE_CLI_PATTERN.fullmatch(value):
        return "safe"
    if _CJK_PATTERN.search(value):
        return "chinese_text"
    if _WIN_PATH_PATTERN.match(value):
        return "windows_path"
    return "special_chars"
```

- [ ] **Step 6.4: 重写 `_sanitize_preview_cli`**

找到 `_sanitize_preview_cli`（line 681-707），整体替换为：

```python
def _sanitize_preview_cli(
    action: dict[str, Any],
    parse_result: Any,
) -> dict[str, Any]:
    """检查 next_action.preview_cli 是否含 user text；含且 unsafe 则降级。

    设计原则（spec rev 4 DR-4 + M-4）：
    - 当 preview_cli 真的把 raw_text 拼进去时，必须用 _classify_unsafe_reason 校验
    - reason 分类对应 _UNSAFE_MESSAGE_TEMPLATES 三类文案
    - 不变量：preview_cli_unsafe=True ⇔ unsafe_reason 存在；safe 路径下两字段都不写
    """
    raw_text = getattr(parse_result, "raw_text", None) or ""
    if not raw_text:
        return action
    cli = action.get("preview_cli")
    if not isinstance(cli, str):
        return action
    if raw_text not in cli:
        return action
    reason = _classify_unsafe_reason(raw_text)
    if reason == "safe":
        return action
    action["preview_cli_unsafe"] = True
    action["unsafe_reason"] = reason
    action["preview_cli"] = _UNSAFE_MESSAGE_TEMPLATES[reason]
    return action
```

- [ ] **Step 6.5: 跑 M-3 + 不变量测试验证转绿**

```bash
pytest tests/test_project_goal_guide.py -k "classify_unsafe or sanitize_preview_cli_unsafe_reason_invariant" -v
```

Expected：6 passed（4 classify + 1 safe + 1 invariant）

- [ ] **Step 6.6: 跑既有 preview_cli_unsafe 测试确认向后兼容**

```bash
pytest tests/test_project_goal_guide.py -k "preview_cli_unsafe" -v
```

Expected：既有 `test_preview_cli_unsafe_when_text_contains_special_chars`（line 126）+ 类似硬编码 `preview_cli_unsafe is True` 断言的测试都仍 pass（M-4 不变量保留）

- [ ] **Step 6.7: 跑全量回归 + lint + mypy**

```bash
pytest tests/ -q
ruff check tools/ tests/
ruff format --check tools/ tests/
mypy --strict src/
```

Expected：
- pytest：~2490 passed, 0 failed
- ruff check：All checks passed
- ruff format --check：无差异
- mypy：Success（注：mypy 检查 `src/` 不直接检查 `tools/`，dev_sync 后会同步）

如 mypy 在 `src/` 有问题：暂留待 commit 7 dev_sync 后再跑（src 可能含旧代码与新代码混合）。

- [ ] **Step 6.8: Commit**

```bash
git add tools/project_guide.py
git commit -m "$(cat <<'EOF'
refactor(project-guide): M-3 _classify_unsafe_reason + I-5 文案落地

- 加共享常量 _SAFE_CLI_PATTERN / _CJK_PATTERN / _WIN_PATH_PATTERN（M-5：避免字面发散）
- 加 _UNSAFE_MESSAGE_TEMPLATES 单点定义三类降级文案（windows_path / chinese_text / special_chars）
- 加 _classify_unsafe_reason，优先级 safe > chinese_text > windows_path > special_chars
  - CJK 优先于 windows_path：避免"做\升降"等中文里反斜杠误归 windows_path
  - windows_path 收紧到 ^[A-Za-z]:[\\/]，不 OR \\
  - 日文 Kanji 落 chinese_text；Hangul / 假名落 special_chars（产品边界）
- _safe_cli_token 改用共享常量
- _sanitize_preview_cli 按 reason 分类降级；不变量 preview_cli_unsafe ⇔ unsafe_reason
  同生共灭，safe 路径两字段都不写

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: Commit 7 — dev_sync 镜像同步

**Goal:** 跑 `scripts/dev_sync.py` 把 root `tools/` 改动同步到 `src/cad_spec_gen/data/tools/`，验证 `--check` 干净，再跑全量 pytest 确认 src 镜像 import 路径也通过。

**Files:**
- Modify: `src/cad_spec_gen/data/tools/*.py`（自动同步）

- [ ] **Step 7.1: 跑 dev_sync 同步 root → src**

```bash
python scripts/dev_sync.py
```

Expected：输出列出已同步文件（`product_goal_parser.py` / `project_guide.py` / `project_guide_dict/__init__.py` / `project_guide_dict/kpi_patterns.json`）

- [ ] **Step 7.2: 跑 dev_sync --check 验证镜像干净**

```bash
python scripts/dev_sync.py --check
```

Expected：exit 0（无变更需 sync）

- [ ] **Step 7.3: 跑全量 pytest 验证 src 镜像 import 路径**

```bash
pytest tests/ -q
```

Expected：~2490 passed, 0 failed（src 镜像也通过）

- [ ] **Step 7.4: 跑 mypy 验证 src 路径**

```bash
mypy --strict src/
```

Expected：Success

- [ ] **Step 7.5: Commit**

```bash
git add src/cad_spec_gen/data/tools/
git commit -m "$(cat <<'EOF'
chore(dev_sync): 同步 root tools/ → src/cad_spec_gen/data/tools/ 镜像

机械同步 commit 3-6 的改动到构建副本路径。reviewer 阅读指引：跳过本 commit
直接看 commit 3-6 的 root tools/ diff 即可，本 commit 仅验证 dev_sync.py
的 hash 匹配 hatch_build COPY_DIRS。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: Commit 8 — PROGRESS docs

**Goal:** 在 `docs/PROGRESS.md` + `docs/superpowers/README.md` 加 v2.25.x cleanup PR 记录。

**Files:**
- Modify: `docs/PROGRESS.md`
- Modify: `docs/superpowers/README.md`

- [ ] **Step 8.1: 读现有 PROGRESS.md 和 superpowers/README.md 模板**

```bash
head -40 docs/PROGRESS.md
head -40 docs/superpowers/README.md
```

观察现有 v2.25.0 / v2.26.0 entry 的格式（一行表格或 section heading）。

- [ ] **Step 8.2: 在 `docs/PROGRESS.md` 顶部加 v2.25.x cleanup 记录**

按 v2.25.0 / v2.26.0 既有模板加 entry：

```markdown
## 2026-05-08 — v2.25.x cleanup PR（feat/v2.25-cleanup-followup）

§11 follow-up 6 项单 PR 集成：I-1 unit_normalize → schema v2 list[{pattern,factor}]；
I-2 spec line 316 软窗口→硬窗口；I-3 evidence_token 双改原文切片；I-4 拆 7
per-status builder；I-5 降级文案按 reason 分类；M-3 _classify_unsafe_reason
（CJK 优先 + windows_path 收紧）。

- spec：rev 4（3 轮自审 + 实测，11 BLOCKER + 18 MAJOR + 5 MINOR 已闭）
- 测试：+20（6 parser + 14 guide）TDD RED → GREEN
- commit：8 个（spec → RED → I-1 → I-3 → I-4 → M-3+I-5 → dev_sync → progress）
- 北极星 5 gate：全过（零配置 / 稳定可靠 / 结果准确 / SW 装即用 / 傻瓜式）
```

具体行号 / 模板按既有 PROGRESS.md 格式调整。

- [ ] **Step 8.3: 在 `docs/superpowers/README.md` 加 spec/plan 链接**

按既有模板在合适位置加：
```markdown
- [v2.25.x cleanup spec rev 4](specs/2026-05-08-v2.25-cleanup-design.md)（2026-05-08）
- [v2.25.x cleanup plan](plans/2026-05-08-v2.25-cleanup-followup.md)（2026-05-08）
```

- [ ] **Step 8.4: Commit**

```bash
git add docs/PROGRESS.md docs/superpowers/README.md
git commit -m "$(cat <<'EOF'
docs(progress): 记录 v2.25.x cleanup PR 完成

§11 follow-up 6 项闭合：I-1/I-2/I-3/I-4/I-5/M-3 单 PR 集成；3 轮自审
11 BLOCKER + 18 MAJOR + 5 MINOR 全闭；+20 测试 TDD RED → GREEN；
8 commits 含 dev_sync 单 commit 镜像同步。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## 完成验证

- [ ] **跑全量回归确认 8 commits 全过**

```bash
git log --oneline main..HEAD
pytest tests/ -q
ruff check . && ruff format --check .
mypy --strict src/
python scripts/dev_sync.py --check
```

Expected：
- `git log` 显示 8 个 commit（spec rev 4 之后 + 含 spec rev 1/2/3 = 12 个 ahead of main）
- pytest：~2490 passed
- ruff/mypy/dev_sync：全过

- [ ] **Push 远端 + 开 PR**（用户决定时机）

```bash
git push -u origin feat/v2.25-cleanup-followup
gh pr create --title "v2.25.x cleanup — §11 follow-up 6 项" --body "..."
```

PR body 引用 spec rev 4 + 3 轮自审记录 + commit 8 PROGRESS 摘要。

---

## 工作流约束（项目 CLAUDE.md 强制）

- TDD 铁律：commit 2 先全 RED；commit 3-6 每个 refactor 把对应 test 转绿
- 中文输出 + 中文 commit message 描述
- dev_sync：commit 7 单 commit 跑
- 每轮收尾：commit 8 更新 PROGRESS.md + superpowers/README.md
