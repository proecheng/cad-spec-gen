"""Per-phase command modules for cad_pipeline.py.

The root-level cad_pipeline.py remains the CLI compatibility entry point.
These modules provide importable phase seams so command implementations can
move out of the large script incrementally.
"""
