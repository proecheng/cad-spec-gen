"""
NEMA23 闭环步进 ≥1.0Nm (SLP-C07) — 真实 STEP 导入件

Auto-generated by codegen/gen_std_parts.py via parts_resolver
Category: motor | Make/Buy: 外购
Material: —
Source: STEP:std_parts/user_provided/SLP-C07_NEMA23_stepper.step
Geometry source: REAL_STEP
Geometry quality: A
Validated: true
Hash: sha256:adf4a616ac7f0a2e55fc3ae3d9e9a3c63f79cfbca980e09078fdc2efbd0a6832
Path kind: project_relative
Requires model review: false

NOTE: Imported from a STEP model selected by parts_resolver.
      Verify upstream license and dimensional fit before manufacturing.
"""

import cadquery as cq


def make_std_c07() -> cq.Workplane:
    """SLP-C07: NEMA23 闭环步进 ≥1.0Nm — imported from STEP file.

    Source: STEP:std_parts/user_provided/SLP-C07_NEMA23_stepper.step
    """
    import os
    _here = os.path.dirname(os.path.abspath(__file__))
    _step_path = os.path.join(_here, "..", "..", 'std_parts/user_provided/SLP-C07_NEMA23_stepper.step')
    _step_path = os.path.normpath(_step_path)
    if not os.path.isfile(_step_path):
        raise FileNotFoundError(
            f"STEP file missing for SLP-C07: {_step_path}")
    _model = cq.importers.importStep(_step_path)
    _bbox = _model.val().BoundingBox()
    _origin_shift = (
        -(_bbox.xmin + _bbox.xmax) / 2.0,
        -(_bbox.ymin + _bbox.ymax) / 2.0,
        -_bbox.zmin,
    )
    return _model.translate(_origin_shift)


if __name__ == "__main__":
    import os
    out = os.path.join(os.path.dirname(__file__), "..", "output")
    os.makedirs(out, exist_ok=True)
    r = make_std_c07()
    p = os.path.join(out, "SLP-C07_std.step")
    cq.exporters.export(r, p)
    print(f"Exported: {p}")
