# Python >= 3.10 (CadQuery requires 3.10+)
#
# Core (required for spec extraction and BOM parsing — no pip install needed):
#   - only Python stdlib
#
# Optional (install as needed for specific pipeline stages):
#   cadquery>=2.0        # 3D parametric modeling (STEP/GLB export)
#   ezdxf>=0.18          # 2D engineering drawings (DXF export)
#   matplotlib>=3.5      # DXF-to-PNG rendering, font discovery
#   Pillow>=9.0          # Component label annotation (annotate_render.py)
