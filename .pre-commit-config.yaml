# v2.10: dev_sync.py pre-commit hook
# 安装：pip install pre-commit && pre-commit install
repos:
  - repo: local
    hooks:
      - id: dev-sync
        name: Sync root sources to data/
        entry: python scripts/dev_sync.py
        language: python
        always_run: true
        pass_filenames: false
